Closes #3322

BET 1's #1451/#1500 machinery was dark: 0 landing_page_* events (7d), 0 delivered digest items (24h), the #2908 wipe (2026-09-11) emptied the substrate, and the #1451 guard's own history recorded ratio 0.0000 on 09-07/08/09 with nothing after.

## What this PR does (phase-1 of the 3322 plan, .fleet/plan-3322.md)

1. **Root cause — receipts, not vibes** (.fleet/phase-1-receipts.md, unit-6 2026-09-13T22:12Z): the signup→redemption 0/20 failures were NOT TTL (verification.expiresAt = issuance + 15:00.000 exactly — the ~90s lore is dead), NOT cookie mechanics (staging 302 stages `f9_better_magic` with Max-Age=900, and the confirm POST sends it — (a)/(b) both verified), NOT pacing. The burst redeemed the D1 `verification.identifier`, which better-auth 1.7.1 stores as the HASH (`storeToken: "hashed"`) — one-way, unrecoverable from D1. The dispatched URL (the only copy of the raw token) persists NOWHERE readable: no D1 table, no KV/R2, no email-ingest worker (P2 verdict).
2. **The code shipped here (18 lines, app/lib/better-auth.server.ts, prior-unit d76fedce5 — proven this unit)**: a #1500-class `magic_link_dispatched` structured line (email/mode/URL) on the sendMagicLink success path, never-throw. Post-merge+deploy this IS the redemption mechanic: capture the URL from Workers logs, then the burst (`.fleet/burst-3322.sh`; its curl mechanics verified good by the unit-6 discriminator) completes signup→watchlist→activation for the 20-competitor cohort. NO other code change needed — cookie/TTL/pacing are all cleared by receipts.
3. **The #1451 rail: re-armed, both legs proven** — installed as **systemd --user** units exactly as the acceptance prescribes (the provision-*.sh as written wants root: recorded as a finding; the fleet's 0509-canary precedent is user units, and no sudo exists in this unit's lane):
   - installed `ops/digest-headline-ratio-guard/0509-digest-headline-ratio-guard-{run.sh,service,timer}` → `~/.local/0509-digest-headline-ratio-guard/` + `~/.config/systemd/user/` (service: sed `__NODE_BIN_DIR__`→`/home/nish/.local/bin`, ExecStart→the installed run.sh, `User=/Group=` dropped for the user manager; timer VERBATIM: OnCalendar 09:53 UTC daily, Persistent=true, after the 04:00Z digest rail).
   - smoke = the REAL scheduled invocation: Started→Finished, ExecMainStatus=0, verdict `sample window 24h: no delivered digest items; nothing to measure today / rolling: 0 measured day(s), ratio 0.000 / verdict: ok` — the guard's own checkout synced origin/main (f026d4248..cff30e908) and ran the REAL remote sample. Not armed — RAN.
   - **alarm leg, fired once (below-floor drill)**: isolated `DIGEST_HEADLINE_STATE_DIR=~/.local/state/0509-digest-headline-ratio/drill-3322-20260913T220842Z`, `--input` fixture (8×ad_churn + 2×landing_page_cta_changed) with `--record --file-issue --no-commit` → `guardFired: true`, ratio 0.2 < 0.5, **DRILL-EXIT=1**, and --file-issue executed: `auto-file skipped: an open digest-headline-ratio-guard incident already exists (dedupe)` — the guard's designed marker-dedupe (#3322/#3380 both carry the marker; #3380's Weekly-Review close re-opens fresh filing). Timer verified: `systemctl --user list-timers` → `Mon 2026-09-14 15:23:00 IST 0509-digest-headline-ratio-guard.timer`.

## What remains (physically real waits → follow-up #33XX)

The 72h observe-to-close (acceptance 3) cannot complete inside this unit: the redemption needs the deploy, the briefs need the 04:00Z / first-brief rail, and FREE second scans need the weekly window (next: Mon 2026-09-21 05:00-08:00 IST). The follow-up issue carries: the post-deploy burst (1-20) via the captured `magic_link_dispatched` URL, the delivered briefs' composition + cta events recorded HERE on #3322, and the guards' first non-vacuous measurements. Coordinate-not-duplicate: #3380 (BET 7) owns the signup→first-run→first-brief LOOP proof; this follow-up owns the GUARDS' measurements.

Verification: (all REAL runs, 2026-09-13T22:0x–22:1xZ)
- `node scripts/canary-cta-detector.mjs --json` → exit 1, 0 landing_page_cta_changed/7d (the BET-1-dark receipt; expected while the cohort = 0 redeemed — root cause above)
- scheduled smoke → exit 0 + the verdict lines quoted above (real remote D1 sample)
- below-floor drill → exit 1 + `guardFired: true` + the executed --file-issue dedupe (receipt above)
- affected tests: `npx vitest run --configLoader runner --project node --changed origin/main` → VitestResultLine
- scope: `git diff origin/main...HEAD --name-only` = .fleet/* + app/lib/better-auth.server.ts ONLY — no migrations/**, no .github/**, no tests removed/skipped (acceptance 5; no test-removal-justified needed)

run-proof: 0509-digest-headline-ratio-guard.timer enabled + listed (ENABLE-OK; next fire Mon 2026-09-14 15:23:00 IST); 0509-digest-headline-ratio-guard.service smoke ran to a real verdict (journal 2026-09-14 03:38:46 IST, ExecMainStatus=0); alarm leg = drill exit 1 + guardFired:true + the --file-issue dedupe message; guard-checkout sync f026d4248..cff30e908.

loose-ends: 72h-observe (follow-up #33XX: post-deploy burst + the guards' first non-vacuous measurements; acceptance-3 evidence comment records today's composition on #3322)

Test plan: affected-tests mode only, node project, no coverage (CI owns coverage/typecheck, fleet-ops#4891). The workers project is not needed: no migrations/** and no tests/integration/** touched.
