## Summary

Closes the #3198 X slice of the mentions epic (#3171) by completing its PROOF, not its machinery. The #3255-landed X connector (`app/lib/presence-connectors/x.server.ts`: recent-search poll, registry dispatch, `PRESENCE_X_ROLLOUT` + `X_API_BEARER_TOKEN` + `X_PAID_ACCESS` gates, `wrangler.jsonc:86` shipping the flag dark) needed zero code. What the issue's termination and acceptance demanded that main did not yet have:

1. REQUIRED research — "existing open-source collectors, cite searched + rejected": PLAN.md §8's X entry cited only the API terms and the pay-per-use pricing coverage. Now sourced (both project READMEs read directly, 2026-09-13): **Nitter** ([zedeus/nitter](https://github.com/zedeus/nitter) — unofficial-API, no-developer-account front-end; X Corp cease-and-desist letters went to the project and its public instances on 2026-08-24, the project announced it will continue, details still pending; its RSS mode is "instance-specific, often disabled due to abuse") and **snscrape** ([JustAnotherArchivist/snscrape](https://github.com/JustAnotherArchivist/snscrape) — scraper over the unofficial surfaces, shipped as a Python CLI/library; wrong runtime for our Workers/TypeScript collectors, same no-contract rate posture). Conclusion, now cited: the lawful, documented, contract-backed X read surface is the official X API v2 recent search — pay-per-use since Feb 2026, NO free read tier. That is this issue's documented no-(free-)surface entry, the only exclusion the epic's termination allows: the flag stays off until the MONEY decision (#3255).
2. Termination "deduped by canonical URL" — never proven for X: ONE new integration test (real workerd/D1, real migrations). First poll + upsert inserts 2; a second identical poll (fresh, no `since_id`) re-observes the same posts; the re-upsert inserts 0 and updates 0 (the contentHash covers title+excerpt+author+publishedAt, which the identical fixture pins — no revision churn, no tombstone), and the target still owns exactly the two original rows. The `UNIQUE (source_target_id, url_hash)` identity — the table-level "deduped by canonical URL" — does the work; the test proves it.
3. Acceptance "/status per-source row" — the status-route test pinned the Threads and GDELT rows (issue #3205's acceptance) but never X: the X row is now pinned in BOTH the loader result (`sourceId: "x"` present, `productionStatus: "gated"`) and the rendered markup (the note's pay-per-use honesty reaches the page).
4. Coverage note — one added sentence states the no-free-read-tier fact and cites the research (docs/mentions/PLAN.md §8), so the public /status row says what the public surface covers and why the flag stays off. `productionStatus` stays `"gated"` (pinned, unchanged).

Phase shipped: this issue is ONE phase and it is complete — no `migrations/**` change at all (the `source_target.connector_id` CHECK has carried `'x'` since migration 0055; 0093/0099/0100 keep it), so nothing expand/contract, nothing follows. The shared interface is untouched (`presence-types.ts`, the registry, the connector — all #3255-owned).

Diff: exactly this source's tests + coverage note + PLAN.md + the `.lane` evidence record — 4 tracked files, +77/−1. No gate-owned path, no existing migration, no plan/entitlements, no connector, no dependency.

net-positive-because: every added line closes a named acceptance/termination gap (research citations, the dedup proof, the /status pin, the honest note) — no mechanism, no wrapper, no abstraction; the only source-line change is one sentence of honesty.

## Verification (final head, this lane)

- Termination-adjacent literal: `./node_modules/.bin/vitest run --configLoader runner --project workers tests/integration/x-mention-search.integration.test.ts` → exit 0, 17/17 (16 prior + the new "dedupes re-observed posts by canonical URL — no second row, no revision churn"), real workerd + real migrations
- `--project node tests/status.route.test.ts` → 12/12, exit 0; `--project node tests/presence-source-coverage.test.ts` → 25/25, exit 0 (the coverage-module consumer)
- Affected node pass: `--project node --changed origin/main` → 8 files / 102 tests, exit 0. Affected workers pass: `--project workers --changed origin/main` → 8 files / 141 tests, exit 0. One suite at a time, `--maxWorkers` never passed, no coverage/typecheck locally (CI owns both; the workers-affected run covers the integration side because the diff touches tests/integration/**)
- `sgscan --base origin/main` → "No new security findings.", exit 0

run-proof: prove-one-run-check — this PR adds no unit, timer or workflow; its proof of work is the proven end-to-end termination-adjacent run: the integration suite exits 0 (17/17 on real workerd + real migrations, including the new dedup proof), plus both affected passes (node 102/102, workers 141/141) and sgscan ("No new security findings.", exit 0), all at final head

research: the collector survey above — the Nitter cease-and-desist of 2026-08-24 is LIVE, read from the zedeus/nitter README the same day this lane ran (the project's continuity was literally in question three weeks prior); snscrape's README likewise; the X pay-per-use conclusion matches PLAN.md §2's existing row (developer terms + heise/postproxy), now completing the epic's "searched + rejected" duty for this source.

help-first: no new bin/ file in this PR; nothing hand-built that already exists — the proof rides the shipped connector, the shipped `UNIQUE (source_target_id, url_hash)` dedup and the shipped /status catalog; zero new mechanisms.

organ-heartbeat: none — this PR touches no organ (no scheduled unit/timer/heartbeat file; the poll orchestrator is #3255/registry-owned and untouched).

token: fleet-token-efficiency-check — no token-efficiency anti-patterns; no prompt assembler touched.

loose-ends: activation (flipping `PRESENCE_X_ROLLOUT` + `X_PAID_ACCESS` + the `X_API_BEARER_TOKEN` secret) is #3255's MONEY/rollout decision, deliberately untouched — the flag stays dark here, the issue's only-allowed exclusion; CI owns typecheck + coverage.

Closes #3198

Relates to #3171
