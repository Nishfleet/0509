Closes #3442

## What

The #3408/#3435 `+x` gate read only `deploy-production.yml`. This adds a sibling test in the same suite (`tests/deploy-production-gate.test.ts`, right after the 0509#3330 gate) that walks every `run: ./scripts/…` ref in the three evidence workflows and asserts the script is executable in the checkout:

- `d1-remote-restore-evidence.yml` — 3× `ci-verify-provider-main-cas.sh`
- `d1-backup-r2.yml` — 2× `ci-verify-production-candidate.sh`
- `d1-restore-proof-auto-refresh.yml` — 2× `ci-verify-production-candidate.sh`

Same shape as the existing gate: first token of each direct `run:`, `statSync` + `mode & 0o111`, failure message names the workflow, the script, and exit 126. The named refs are asserted too, so a workflow rename fails loudly instead of passing on an empty walk. Test-only diff (48 lines added, one file); no workflow, gate-owned path, or script touched.

net-positive-because: the issue's acceptance is a new walking gate — the +48 lines are the detector itself (one sibling test), not incidental bloat; no code paths added.

## Verification

- Targeted run: `npx vitest run --configLoader runner --project node -t "0509#3442" tests/deploy-production-gate.test.ts` → 1 passed (56s total file: 56/56 later).
- Detector red-proof: `chmod -x scripts/ci-verify-provider-main-cas.sh` → the new test fails with `…d1-remote-restore-evidence.yml: ./scripts/ci-verify-provider-main-cas.sh lost its +x bit … dies with exit 126 (0509#3442)`; bit restored, re-run green. The gate detects the exact 09-13 outage class (#3409/#3413), it does not just pass.
- Affected suite: `npx vitest run --configLoader runner --project node --changed origin/main` → 1 file, 56/56 passed.
- `sgscan` (changes since origin/HEAD ce9fe5517) → No new security findings.

run-proof: vitest targeted run 1 passed; vitest --changed origin/main 56/56 passed; sgscan clean.

loose-ends: none — single test file, no skipped tests, no assert-count drop (net +7 assertions).
