## Summary

Ships the senior reviewer's Act-on finding from #3408's review round (the unit that carried it died after committing; salvage banked it at `wip/pi-issue-0509-3330-20260913T212101Z`). #3408's gate test walked only the `deploy` job, so a lost +x bit on a script invoked from the evidence/cas jobs — `./scripts/ci-bind-remote-restore-candidate.sh`, `./scripts/ci-prepare-remote-restore-evidence.sh`, `./scripts/ci-verify-provider-main-cas.sh` (3 of the 5 unique `./scripts/` refs in deploy-production.yml) — would pass every suite and still kill production deploys with the same exit 126 that #3408 fixed for the ledger. The gate now walks every job in the workflow and takes the first token of each `run:` so a future multiline or argful step fails with the gate's message, not an ENOENT.

Mode-only regression class, same gate suite, no new mechanism. Relates to #3330 (does not close it — the acceptance green run is still pending on the deploy chain).

## Verification

Real runs, this worktree @ `caa89fca8` (one heavy toolchain at a time; no coverage, no typecheck — CI owns those):

- Fails-before (the widening's whole point): `chmod -x scripts/ci-prepare-remote-restore-evidence.sh` + the `-t "0509#3330"` run → **1 failed** — `AssertionError: ./scripts/ci-prepare-remote-restore-evidence.sh lost its +x bit — the Actions checkout runs it as ./scripts/… and dies with exit 126 (0509#3330)`. The narrow gate on main passes that same sabotage silently (it never looks outside the deploy job) — that is the hole this closes. Bit restored after the proof; worktree clean.
- Passes-after: `npx vitest run --configLoader runner --project node tests/deploy-production-gate.test.ts -t "0509#3330"` → **1 passed | 54 skipped** against the real `.github/workflows/deploy-production.yml` (all 8 `run: ./scripts/…` refs across every job executable).
- No tests removed, renamed, skipped or `.only`'d — the existing #3330 gate case is widened in place, +1 assertion path.

run-proof: 1 vitest red-proof (sabotaged evidence script) + 1 green run (-t 0509#3330, 1 passed/54 skipped), this worktree @ caa89fca8 (+11/-3: one existing gate test widened); 0 new timers/workflows/units in this diff — prove-one-run-check

organ-heartbeat: none — test widening only — not-an-organ: no checker process added or changed, the gate rides the existing deploy-production-gate suite

research: none needed — no new bin/ files. help-first: n/a — no new tool.

net-positive-because: +11/-3 — zero new machinery: the widening reuses the #3408 gate case in the existing deploy-production-gate suite so the exit-126 class that killed five production deploys (#3413/#3409) can no longer hide in the evidence/cas jobs; the alternative (another lost-bit incident) costs a production deploy each.

loose-ends: the #3330 acceptance green Deploy production run is still pending on the deploy chain (Gate B J1 mobile red at run 34786485796, production_meta #3392, proof_cleanup #3337, Gate B J2 #2513, D1-restore #3314) — this PR only hardens the gate, it does not claim the green run. test-removal-justified: n/a — zero tests removed, renamed, skipped or .only'd.

Relates to #3330, #3408 (the review round this Act-on came from), #3409/#3413 (the exit-126 class)

## Reviewer round (senior seat: litellm/senior — find_senior_seat 2026-09-13T23:0xZ, never the worker seat)

Verdict: clean — "Nothing blocks landing". Full gate file 55/55 re-verified by the reviewer, the widened net proven to hold all 5 unique `./scripts/` refs across 6 jobs including the three evidence/cas scripts, no weakening/skips/drive-bys. Findings, one bucket each:

- **Consider** (→ follow-up, not re-delegated): the first-token net misses a `run: |` block invoking `./scripts/foo.sh` on line 2+ — zero such lines today (all 8 refs single-line; multiline blocks use `node scripts/…`). One-line hardening: tokenize per line.
- **Noted** (→ follow-up, pre-existing gap, not this PR's scope): the same direct-exec pattern lives in `d1-remote-restore-evidence.yml`, `d1-backup-r2.yml`, `d1-restore-proof-auto-refresh.yml`; this gate reads deploy-production.yml only, which is what #3330 scoped.
- **Dismissed-with-reason**: "54 skipped" in the `-t` targeted output is the name filter, not skips (full file runs 55/55); `split(/\s+/)[0]!` non-null assertion safe on trimmed input; test-title rename grep-clean; untracked `.pr-body-3330-widen.md` is the PR draft, not diff content.
- Act on: none.
