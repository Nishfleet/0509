# AUDIT / REVIEW — 2026-08-21 — 0509 (Five to Nine)

## Headline

The fleet-scout candidates file (`var/scout/CANDIDATES-2026-08-21-0509.md`, the
`/home/nish/fleet2/var/scout/` deterministic output) was pure noise: 363 KB of
"Open TODOs / FIXMEs" grep, virtually all vendored `node_modules/**js.map`
comments. The candidate set was reconstructed directly from the product
workspace; every survivor carries cited evidence (file path, commit, or GitHub
PR/issue).

**Revenue reality:** Five to Nine monetizes via plan subscriptions
(Scout/Starter/Agency USD anchors + proof-capture top-up packs) with Dodo
localized checkout. Revenue impact is scored by how directly an item surfaces
pricing transparency / demand-capture (indexable pricing + SEO compare pages)
or protects current subscriber value (paid-alert proof payloads, quiet-week
perceived value).

Scout timestamp: 2026-08-21T02:32:22.659Z. Workspace audited:
`/home/nish/workspaces/products/0509` @ `main` HEAD `4a4a56a0`
(`origin/main`, fetched 2026-08-21).

## Ranked survivors (max 5, priority=revenue)

1. **`capture/pricing-page-404`** (highest revenue impact — the pricing-research
   surface is a live 404 for paid-intent buyers).
   `GET https://0509.io/pricing` → **HTTP 404** (measured 2026-08-21) while
   `GET /` → 200; no `app/routes/pricing.tsx` on `main`; pricing exists only as
   the `/#pricing` homepage anchor. PR #837 (indexable, sitemap, canonical
   `/pricing`) is OPEN.
   Evidence: live `/pricing` 404 (measured), `app/routes/` listing,
   PR #837 (OPEN), `app/lib/seo.ts`.

2. **`revenue/published-prices`** (transparency/conversion — main still paints
   "Localized at checkout" as first paint until a lazy `/api/pricing-preview`
   fetch resolves, and no real USD anchors are on `main`).
   PR #808 (published Scout/Starter/Agency anchors + 8x annual toggle usable at
   first paint, Dodo buyer-currency still overrides) is OPEN **and MERGEABLE**.
   Evidence: `app/lib/pricing.ts` (`monthlyLabel: "Localized at checkout"`, no
   `PUBLISHED_PLAN_PRICES_USD`), `app/routes/marketing.tsx`, PR #808 (OPEN).

3. **`acquisition/compare-rivals`** (top-of-funnel SEO capture of rival search
   intent). PR #845 ships honest `/compare` pages for Visualping, Spyland,
   Pulzifi, Foreplay following the existing `compare.meta-ad-library` pattern.
   Evidence: PR #845 (OPEN), `app/routes/compare.meta-ad-library.tsx` /
   `compare.magicbrief.tsx` on `main` (the four rival pages are not).

4. **`retention/visual-diff-alerts`** (paid-alert proof value — before/after
   screenshot pair not yet in digest/instant-alert emails). The watchlist-event
   half shipped via PR #715 (`feb1d460`); the alert-payload half is PR #810,
   OPEN and MERGEABLE.
   Evidence: PR #810 (OPEN, MERGEABLE), `app/lib/digest-orchestration.server.ts`
   (`loadDigestScreenshotPairs` not on `main`), PR #715 (merged).

5. **`retention/brief-retention-frame`** (quiet-week is the core retention
   case; all-quiet/triage digest emails must carry material-delta + owner +
   confidence + expiry, not silence). PR #809, OPEN and MERGEABLE.
   Evidence: PR #809 (OPEN, MERGEABLE), goal `goal-0509-brief-retention-loop`
   (TIER1 m4), `app/lib/digest-email.server.ts`.

## INBOX lines (survivors to append, priority=revenue + slug)

```
[0509] priority=revenue Publish the standalone /pricing route so paid-intent buyers can research plans: https://0509.io/pricing returns HTTP 404 while / is 200 and pricing exists only as the /#pricing homepage anchor; merge PR #837 (indexable, sitemap, canonical); slug=capture/pricing-page-404; evidence=live-404-2026-08-21, PR #837, app/routes/, app/lib/seo.ts

[0509] priority=revenue Publish real plan prices as first paint instead of 'Localized at checkout': on main app/lib/pricing.ts still anchors on checkout preview, so paid buyers see no USD/plan prices until a lazy fetch resolves; merge PR #808 (Scout/Starter/Agency anchors, 8x annual toggle usable at first paint, Dodo buyer-currency override preserved); slug=revenue/published-prices; evidence=app/lib/pricing.ts, app/routes/marketing.tsx, PR #808

[0509] priority=revenue Ship honest /compare pages for Visualping, Spyland, Pulzifi, Foreplay to capture rival search-intent top-of-funnel SEO; merge PR #845 following the existing compare.meta-ad-library pattern; slug=acquisition/compare-rivals; evidence=PR #845, app/routes/compare.meta-ad-library.tsx

[0509] priority=revenue Wire the before/after screenshot pair into digest + instant alert emails; the watchlist-event half shipped via PR #715 but the BL-053 alert half (PR #810) is not on main; merge it; slug=retention/visual-diff-alerts; evidence=PR #810, PR #715, app/lib/digest-orchestration.server.ts

[0509] priority=revenue Add the retention frame (material delta, owner, confidence, expiry) to all-quiet and triage digest emails so a quiet week reads as 'checked, nothing moved'; merge PR #809 per goal-0509-brief-retention-loop (m4); slug=retention/brief-retention-frame; evidence=PR #809, goal-0509-brief-retention-loop, app/lib/digest-email.server.ts
```

## Market-signal role

**No new competitor moves or pricing shifts visible in the candidates file.**
The fleet-scout candidates file is noise (vendored node_modules TODOs); it
carries no competitor or pricing signal, and the audit worker ignored it for
signal purposes. The product workspace shows no competitor pricing change:
Five-to-Nine's own plan anchors (Scout/Starter/Agency) are unchanged across the
2026-08-21 window, Dodo localized checkout remains the buyer-currency source of
truth, and no competitor-price delta was recorded in the reconstructed set. The
four `/compare` rivals in PR #845 (Visualping, Spyland, Pulzifi, Foreplay) are
Five-to-Nine's own inbound/SEO positioning, not a competitor move against
Five-to-Nine. **No strong new market signal this cycle.**

## Needs-Nish routing

C6 (`revenue/dodo-plan-change-smoke`) and C7 (`ops/retired-provider-cleanup`)
are owner/credential actions that are not worker-shippable; they are recorded in
`var/scout/NEEDS-NISH.md`, not kept in the max-5 cap.

## Rejected / dedup

- `node_modules/**js.map` compiled-source TODOs — vendored dependency source
  maps; ignored.
- Node_modules/wrangler comments — tooling; ignored.
- Prior scout line echoes — process artifacts, not candidates.
- C6/C7 — real but Nish/operator-owned (credentials, external dashboard, or
  internal-subscription smoke); routed to NEEDS-NISH, outside the max-5 cap.

## Files

- Reconstructed input: `var/scout/CANDIDATES-2026-08-21-0509.md`
- Audit output: `var/scout/AUDIT-2026-08-21-0509.md`
- Needs-Nish items: `var/scout/NEEDS-NISH.md`