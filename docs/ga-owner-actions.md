# GA Owner Actions

Last updated: 2026-07-13 (dated evidence snapshot; current Gate B/C refresh pending)

These are a dated owner-action and external-proof ledger. They distinguish fixed-candidate/local evidence from current Gate B/C engineering and provider/operator proof; Gate D is target-buyer market validation only. This ledger does not by itself establish current GA.

Completed release actions (dated fixed-candidate/runtime evidence): PR #251 merged to `main` as `629fb14`; the compatible Worker was deployed; release and post-cleanup D1 exports were uploaded to the private R2 backup prefix; migration `0060_remove_legacy_billing_provider.sql` applied; aggregate post-cleanup evidence confirmed plan rows and Dodo linkage remained, legacy billing columns were gone, and no retired-provider webhook table remained; billing, proof/email, prod, and provider bakeoff canaries passed after cleanup. Follow-up commercial proof gates shipped through PR #271 and production deploy `ad02524f-866a-4ef8-b2b6-d58040e94679`; monthly checkout and top-ups passed the live Dodo pricing canary. Follow-up Dodo provider repair corrected Scout/Starter monthly and annual USD/GBP pricing with Dodo localized pricing, and the live pricing canary passed monthly, annual, and top-ups in IN, US, and GB. Follow-up Dodo portal repair created the Five to Nine Product Collection with Scout/Starter monthly and annual products for Customer Portal plan-change eligibility, and the repo has owner-only in-app switching through Dodo's documented plan-change preview/change endpoints. Follow-up ops proof imported the post-cleanup backup into isolated local SQLite and confirmed aggregate invariants without recording backup object names or customer/provider identifiers here; remote scratch restore remains unverified. Follow-up uptime hardening added a no-secret GitHub Actions health workflow for `https://0509.io/api/health`; scheduled workflow runs `28548096175`, `28552452662`, and `28555610571` later passed on `main`. A 2026-07-02 owner-operated D1-to-R2 backup uploaded successfully to the private backup prefix, and `node scripts/validate-d1-backup.mjs` passed through migration `0062`. On 2026-07-13, Actions dispatch run `29225583866` completed validation, export, and upload after the required Cloudflare secrets were added.

| Action | Status | Owner | Risk | Launch impact | Next step |
| --- | --- | --- | --- | --- | --- |
| Dodo Scout/Starter localized pricing | DONE | Nish/operator | None currently known | Scout/Starter monthly and annual checkout can sell after live Dodo validation | Redacted Dodo API product proof confirmed Scout/Starter monthly and annual products are `by_currency` with active USD/GBP localized rules; annual equals exactly 8x monthly in IN, US, and GB; `npm run canary:pricing` passes |
| Dodo in-app plan switching and portal cancellation | REPO CONFIGURED / INTERNAL SUBSCRIPTION NEEDED | Nish/operator | Real provider behavior still needs one internal subscription smoke before claiming live customer proof | Scout/Starter can sell and linked paid subscriptions can request owner-only plan changes from `/app/billing` through Dodo's plan-change API; cancellation remains portal/support-backed until checked | Remote D1 aggregate check on 2026-07-02 found no linked Scout/Starter subscriptions, so there is no safe internal target yet; create/complete one internal paid Scout or Starter subscription, then switch Scout/Starter or monthly/annual once, confirm signed webhook updates the account, and separately confirm cancellation remains available in the Dodo portal |
| Uptime health workflow | SCHEDULED PASS / ALERT UNPROVEN | Nish/operator | GitHub scheduled workflow failures may notify the workflow creator or cron modifier; failure alert path is not observed yet | Launch trust gate has a repo-configured health check, but alert receipt remains owner-verified | Manual run `28540913266` passed on `main`; scheduled runs `28548096175`, `28552452662`, and `28555610571` passed on `main`; confirm failed-run notifications reach the intended inbox, or keep/add UptimeRobot if a separate external monitor is required |
| D1-to-R2 scheduled backup | PROVEN DISPATCH 2026-07-13 / FUTURE OBSERVATION OPEN | Nish | D1 export blocks database requests while it runs; future schedule and retention still need observation | First Actions backup is proven, but current Gate B/C restore evidence is separate | 2026-07-02 manual `npm run backup:d1:r2` uploaded successfully while secrets were absent; Actions dispatch `29225583866` completed validation/export/upload on 2026-07-13 after `CLOUDFLARE_ACCOUNT_ID` and `CLOUDFLARE_API_TOKEN` were added; observe a future scheduled object and record retention |
| Restore drill | DONE LOCAL / REMOTE SCRATCH OPEN | Nish/operator | Remote D1 scratch restore hit statement-size limits on large inserts on 2026-07-13 | Local SQLite import smoke passed; production-like D1 rebuild still needs transformed import, `SQLITE_TOOBIG` regression, parity, and cleanup proof | Download a current backup, preserve candidate/source fingerprints, run the documented transform and isolated remote scratch drill, then delete temporary copies and scratch resources |
| Presence website canary | DONE FOR GA | Nish/operator | Internal-only rollout still requires the private workspace value if production is rolled back to `internal` | Presence GA code/config and local canary are aligned | `npm run canary:presence` now runs the GA-safe website coverage without requiring the old internal workspace value |
| Agency fan-out proof | DATED DISPATCH PASS / EXTERNAL SCAN WATCH | Nish/operator | Synthetic proof targets can fail scanning even when fan-out dispatch is healthy | Agency checkout was opened on dated dispatch evidence; current scan health remains external proof | Production cron queued 78 fan-out jobs for the internal Agency-scale proof workspace with 0 dispatch failures and 8 max concurrency slots; synthetic proof watchlists were deactivated after proof |
| Cloudflare Email activity/log visibility | PARTIAL | Nish/operator | Email sends are proven app-side; dashboard log visibility is not captured here | No blocker for email delivery, but ops visibility incomplete | Fresh proof canary sent email and aggregate D1 shows recent `cloudflare_email` sends; still confirm Email Service Logs/Activity in Cloudflare dashboard |
| WhatsApp orphaned targets | REVIEWED / PRESERVE | Nish/operator | Stale phone-bearing rows remain stored but unsupported | No public blocker; UI/API hides WhatsApp while non-GA | Aggregate-only review found stale unsupported WhatsApp target/config rows and no send-attempt evidence; preserve unless owner approves anonymization/cleanup |
| Retired billing-provider dashboard cleanup | EXTERNAL BLOCKED | Nish/operator | Old provider dashboard could still send webhooks or expose obsolete payment links | Repo runtime/schema is clean, provider-side cleanup still needed | Repo/D1/Worker secrets have no retired-provider surface; disable/remove old webhooks, subscriptions, payment links, and live products in the retired provider dashboard |

Do not include secrets, provider ids, customer ids, webhook URLs, or payment identifiers in this file.
