# GA Owner Actions

Last updated: 2026-07-01

These are the remaining launch actions and operator proof items that cannot be fully proven from repo code alone.

Completed release actions: PR #251 merged to `main` as `629fb14`; the compatible Worker was deployed; release and post-cleanup D1 exports were uploaded to the private R2 backup prefix; migration `0060_remove_legacy_billing_provider.sql` applied; aggregate post-cleanup evidence confirmed plan rows and Dodo linkage remained, legacy billing columns were gone, and no retired-provider webhook table remained; billing, proof/email, prod, and provider bakeoff canaries passed after cleanup. Follow-up commercial proof gates shipped through PR #271 and production deploy `ad02524f-866a-4ef8-b2b6-d58040e94679`; monthly checkout and top-ups pass the live Dodo pricing canary, while annual Scout/Starter checkout remains intentionally fail-closed until Dodo annual prices are corrected. Follow-up ops proof imported the post-cleanup backup into isolated local SQLite and confirmed aggregate invariants without recording backup object names or customer/provider identifiers here.

| Action | Status | Owner | Risk | Launch impact | Next step |
| --- | --- | --- | --- | --- | --- |
| Dodo annual SKU pricing | OWNER | Nish | Annual Scout/Starter checkout stays fail-closed | Monthly checkout and top-ups can sell; annual checkout cannot be called live | In Dodo, set `scout_annual_v1` and `starter_annual_v1` annual prices to exactly 8x the localized monthly amount in each pricing context, then rerun `npm run canary:pricing` |
| Dodo customer portal subscription updates | OWNER | Nish | Customers may need support for plan changes/cancellation | Scout/Starter can sell, but billing is not fully self-serve | In Dodo, put the Scout/Starter subscription products in the same Product Collection, enable **Allow Subscription Updates** in **Settings -> Subscriptions**, then confirm `/app/billing` portal shows plan changes; separately confirm cancellation remains available in subscription details |
| External uptime monitor | OWNER | Nish | No independent outage alert proof | Launch trust gate remains owner-verified only | UptimeRobot or equivalent monitor for `https://0509.io/api/health`, about 5 minute interval, outage and recovery alerts, no token in URL |
| D1-to-R2 scheduled backup | REPO CONFIGURED / OWNER SECRET | Nish | GitHub scheduled run will fail until Cloudflare GitHub secrets exist; D1 export blocks database requests while it runs | Trust page must stay conservative until first scheduled object is observed | `.github/workflows/d1-backup-r2.yml` runs weekly at 22:17 UTC Sunday and manually; add `CLOUDFLARE_ACCOUNT_ID` and `CLOUDFLARE_API_TOKEN` repository secrets, run it once, confirm a new R2 object, then record retention |
| Restore drill | DONE LOCAL | Nish/operator | Remote D1 scratch restore hit statement-size limits on large inserts | Local SQLite import smoke passed; production-like D1 rebuild still needs split/import proof | Post-cleanup backup imported into isolated local SQLite; keep restore files out of git and delete temporary copies after drills |
| Presence website canary | DONE FOR GA | Nish/operator | Internal-only rollout still requires the private workspace value if production is rolled back to `internal` | Presence GA code/config and local canary are aligned | `npm run canary:presence` now runs the GA-safe website coverage without requiring the old internal workspace value |
| Agency fan-out proof | DONE DISPATCH / WATCH SCAN HEALTH | Nish/operator | Synthetic proof targets can fail scanning even when fan-out dispatch is healthy | Agency checkout is open; monitor nightly scan health | Production cron queued 78 fan-out jobs for the internal Agency-scale proof workspace with 0 dispatch failures and 8 max concurrency slots; synthetic proof watchlists were deactivated after proof |
| Cloudflare Email activity/log visibility | PARTIAL | Nish/operator | Email sends are proven app-side; dashboard log visibility is not captured here | No blocker for email delivery, but ops visibility incomplete | Fresh proof canary sent email and aggregate D1 shows recent `cloudflare_email` sends; still confirm Email Service Logs/Activity in Cloudflare dashboard |
| WhatsApp orphaned targets | REVIEWED / PRESERVE | Nish/operator | Stale phone-bearing rows remain stored but unsupported | No public blocker; UI/API hides WhatsApp while non-GA | Aggregate-only review found stale unsupported WhatsApp target/config rows and no send-attempt evidence; preserve unless owner approves anonymization/cleanup |
| Retired billing-provider dashboard cleanup | EXTERNAL BLOCKED | Nish/operator | Old provider dashboard could still send webhooks or expose obsolete payment links | Repo runtime/schema is clean, provider-side cleanup still needed | Repo/D1/Worker secrets have no retired-provider surface; disable/remove old webhooks, subscriptions, payment links, and live products in the retired provider dashboard |

Do not include secrets, provider ids, customer ids, webhook URLs, or payment identifiers in this file.
