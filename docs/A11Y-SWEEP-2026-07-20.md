# Accessibility sweep — 2026-07-20

Branch: `audit/a11y-sweep`

Method: static scan of `app/routes/*.tsx` and `app/components/**`, plus fixture-style SSR markup checks already covered by the Vitest suite (`renderToStaticMarkup` route/component tests). No e2e or scripts changes.

## Scope checklist

| Criterion | Method | Result |
|---|---|---|
| Every `<img>` has meaningful `alt` or `aria-hidden` | Static regex over app routes/components | Already correct (4 images: ad-thumb, report-view, branding forms) |
| Every form input has an associated label | Nested `<label>` walk + `aria-label` / `htmlFor` | Already correct (implicit `label.f9-field` / checkbox-row pattern) |
| Icon-only buttons have `aria-label` | Button body / name heuristics | Already correct (`?` shortcuts control, icons default `aria-hidden`) |
| Landmark structure (main/nav/aside once per surface) | Route + shell inventory | Already correct for public shells; app shell provides single `<main>` + labeled `<nav>` / `<aside>` |
| Heading hierarchy never skips levels | File + `DashboardPageHeader` injection outline | Fixed where page outline skipped h2 |
| Interactive elements are buttons/links | `onClick` on non-interactive tags | Already correct |
| `aria-live` for async status (search, scan, save) | Message/action-feedback inventory | Fixed remaining save/status gaps; search/scan already covered |

## Findings table

| ID | Area | Location | Finding | Status |
|---|---|---|---|---|
| H-01 | Heading hierarchy | `app/routes/ads.$domain.tsx` | Page `h1` then ad cards as `h3` | **fixed** → card titles are `h2`; CSS keeps prior size |
| H-02 | Heading hierarchy | `app/routes/share.$token.tsx` | Shared digest/collection/watchlist list items `h1`→`h3` | **fixed** → list/card titles are `h2` |
| H-03 | Heading hierarchy | `app/components/report-view.tsx` | Report title `h1` then decision/AI/coverage sections as `h3` | **fixed** → those sections are `h2`; per-row event titles stay `h3` under row `h2` |
| H-04 | Heading hierarchy | `app/routes/app.ops.tsx` | Ops page started at `h2` with no page `h1` | **fixed** → `DashboardPageHeader` (`h1`) + section items `h2`; metric values no longer use a heading |
| L-01 | Live region / save feedback | `app/routes/app.digests.tsx` | Action message lacked live role | **fixed** |
| L-02 | Live region / save feedback | `app/routes/app.notifications.ui.tsx` | Action message lacked live role | **fixed** |
| L-03 | Live region / save feedback | `app/routes/app.source-access.ui.tsx` | Action message lacked live role | **fixed** |
| L-04 | Live region / save feedback | `app/routes/app.sources.tsx` | Action + new-key messages lacked live roles | **fixed** |
| L-05 | Live region / save feedback | `app/routes/app.account.tsx` | Session action / controls messages lacked live roles | **fixed** |
| L-06 | Live region / save feedback | `app/components/account-branding-section.tsx` | Brand profile action message lacked live role | **fixed** |
| L-07 | Live region / scan status | `app/routes/app.watchlists.tsx` | Consecutive-scan-failure banner lacked alert role; first-scan banner missing `role="status"` | **fixed** |
| L-08 | Live region / billing | `app/routes/app.billing.tsx` | Several checkout-blocking error banners lacked `role="alert"` | **fixed** |
| L-09 | Live region / developer access | `app/routes/app.developer-access.ui.tsx` | Create-disabled reason lacked alert role | **fixed** |
| L-10 | Live region / auth | `app/routes/auth.better.magic-link.tsx` | Magic-link error lacked alert role | **fixed** |
| L-11 | Live region / support | `app/routes/app.support.tsx` | Missing-case error lacked alert; empty/member notices lack status role | **fixed** |
| A-01 | Images | `ad-thumb`, `report-view`, branding forms | Meaningful alts present | **already-correct** |
| A-02 | Form labels | Widespread `label.f9-field` / checkbox-row / `aria-label` | Inputs programmatically named | **already-correct** |
| A-03 | Icon-only controls | Search `?` keyboard control, icon rail, theme select | Named via `aria-label` / visible text / sr-only | **already-correct** |
| A-04 | Landmarks | `DashboardShell`, marketing/legal/auth shells | Single `<main>`; labeled nav/aside; skip link | **already-correct** |
| A-05 | Clickable non-buttons | Routes/components `onClick` audit | No bare clickable `div`/`span` | **already-correct** |
| A-06 | Search async announcements | `search.tsx` + `search-answer-panel` | `aria-live` / `role="status"` for results and load state | **already-correct** |
| A-07 | Shared ActionFeedback | `action-feedback.tsx` | Save feedback already live + atomic | **already-correct** |
| A-08 | Route change announcement | `dashboard-shell.tsx` | Polite live region for route name | **already-correct** |
| D-01 | Component subtrees start at `h3` | creative-wall, dossier, trends, digest packets, insight panel | Intentionally nested under page `h1` + section `h2` on desk routes | **already-correct** (context-dependent; do not promote to `h2` without checking each host page) |
| D-02 | Multiple labeled navs in app shell | Desktop rail + mobile primary + mobile utility | Required for responsive IA; each has distinct `aria-label` | **already-correct** |
| D-03 | Touch target / 44×44 density | Dense desk lists and rail icons | Not fully remediable without layout redesign | **needs-design-decision** |
| D-04 | Color-only status pills | Longevity / status chips | Text labels generally present; contrast not re-measured in this pass | **needs-design-decision** (pair with visual contrast audit) |
| D-05 | Nested document outline in long watchlist detail | Many sibling `h2`/`h3` groups | Hierarchy is valid; outline length may still be noisy for SR users | **needs-design-decision** (optional sectioning / collapsible regions) |

## CSS note

Heading level changes keep visual parity by extending existing selectors (e.g. `.f9-result-card h2,h3`, `.f9-proof-packet > div > h2`, `.f9-ops-metric-value`). No intentional visual redesign.

## Verification

```bash
node ./node_modules/vitest/vitest.mjs run
# Test Files  349 passed (349)
# Tests       3690 passed (3690)
```

No test markup assertions required updates (ARIA additions were additive; heading text content unchanged).

## Out of scope (per brief)

- `e2e/`
- `scripts/`
- Live browser AT walkthrough (VoiceOver/NVDA)
- Full WCAG contrast re-measurement
