Closes nothing — `Relates to #3409` only; #3409's legal close waits for the green Deploy run (deploy-fault-gate, fleet-ops#5785).

## Why

PR #3420 (the #3409 fixture fix) carried a stray file onto main: `.pr-body-3409.md`, 37 lines of worker scratch — the salvage tooling banks the drafted PR body into the claim branch, and this one rode along into the merge commit 16bb31659. It sits at the repo root of a product repo and nothing reads it.

## Scope

One file deleted, zero lines of code touched. `grep -rn "pr-body-3409"` across `*.ts *.tsx *.mjs *.yml *.sh` finds no reference outside the file itself.

## Verification

- `git ls-tree origin/main .pr-body-3409.md` → present (100644, 11b85d34); this branch's tree → absent.
- `npx vitest run --configLoader runner --project node --changed origin/main` → `No test files found, exiting with code 0` — the deletion touches no test, no code, no migration.
- No workflow, no gate, no product path: nothing to attest, nothing to re-verify.

run-proof: production CI already proves the surrounding state — the #3409 faults this debris came from are fixed and green on main: run 34787898963 and run 34789573515 show `✓ tests/no-time-bomb-fixtures.test.ts` passing in the Deploy production Test step (the fixture fix from #3420), and no run since 34783711074 shows the ledger `Permission denied`/exit 126 (the +x fix, 896793d87). The deploy pipeline's remaining red is the D1 restore-evidence step (`source_backup_migration_ledger_stale`, runs 34789676184/34790254270/34791191086) — tracked in #3438, not this PR.

research: n/a — deletion of a file this unit itself introduced; no new tooling, no new bin/ file.
help-first: n/a — no new command added.

loose-ends: #3409 acceptance (green Deploy production run newer than 34780512552 + deploy-ledger.jsonl row + /status version from that run) still pending on #3438's restore-evidence fix; #3190 stays the green-run owner umbrella.
