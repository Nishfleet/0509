## Summary

- Migrations 0079 and 0081 are pure data seeds (5 demo brands + 25 sitemap brands) that prod already applied. They stay in the chain byte-for-byte — no migration file is added, deleted, or edited.
- `tests/integration/apply-migrations.ts` now skips 0079 and 0081 by name, so every fresh integration-test D1 is schema-only.
- The seed rows live in `scripts/seed-demo-brands.mjs`, exported as `DEMO_BRAND_SEED_SQL` and `SITEMAP_BRAND_SEED_SQL` (byte-for-byte copies of the migration INSERTs). The script doubles as a CLI runbook (`node scripts/seed-demo-brands.mjs --local|--remote`).
- The two integration tests that asserted the migration-seeded rows now import and apply the seed SQL directly, preserving every original assertion and adding schema-only + idempotency checks.

## Reviewer round (as-of the review on this branch)

Reviewer: cursor/cursor-grok-4.6-high (senior seat, resolved via find_senior_seat).

Adjudication (one bucket per finding):
- **Act on** — none. Reviewer found no Critical (must-fix) items.
- **Act on (drift warning)** — reviewer flagged that nothing guards the seed SQL staying in sync with migrations 0079/0081. Addressed by a `KEEP IN SYNC` comment in `scripts/seed-demo-brands.mjs` pointing future editors to both files. No test added (house deletion-first rule; drift is a documentation coupling, not a new organ).
- **Consider** — the two integration-test edits and the `tsconfig.node.json` registration sit outside the literal `files:` list. Recorded: these are functionally required to satisfy the `accept` (integration tests run against schema-only migrations — the 5/25 seeded rows otherwise break the existing tests) and to keep CI `tsc -b` green on the new import, per the repo's established convention for test-imported `.mjs` files (`canary-proof-screenshot-rate.mjs`). 
- **Consider** — `runWranglerD1` unbounded `--remote` + hardcoded `DATABASE_NAME`. Noted: safe today because `INSERT OR IGNORE` makes a remote re-run a no-op (prod already has the rows).
- **Noted** — `native` child_process import in the workers project matches the existing `canary-proof-screenshot-rate.mjs` pattern and passes CI. Other tests referencing `demo_backfill`/`sitemap_brand_seed` seed their own fixtures or read the migration file, unaffected by the skip. No `d1-migration-sync-check.lib.mjs` change needed (0079/0081 stay in the ledger).
- **Dismissed-with-reason** — "weakened/skipped test": the refactor preserves the proof-gate, honest-artifact, and rollback assertions and adds schema-only + idempotency checks; coverage is not reduced.

## Follow-up fix (this PR, after a prior worker opened PR #2617 that failed CI typecheck)

- `scripts/seed-demo-brands.mjs`: added JSDoc types to `runWranglerD1(sql, target)` — fixes `TS7006` (implicit `any` params).
- `tsconfig.node.json`: registered `scripts/seed-demo-brands.mjs` in the node project include — fixes `TS6307` (file imported by `tests/integration/**` but not in the project file list). This is the repo's established convention: every `.mjs` that tests import (e.g. `canary-proof-screenshot-rate.mjs`) is registered there.

## Verification

- `npx vitest run --configLoader runner --project workers tests/integration/demo-brand-timeline-backfill.integration.test.ts tests/integration/sitemap-brand-timeline-backfill.integration.test.ts` → 2 files, 13 tests passed
- `npx vitest run --configLoader runner --project workers tests/integration/offer-timeline.integration.test.ts` → 1 file, 4 tests passed
- `npx vitest run --configLoader runner --project workers tests/integration/landing-page-snapshot.test.ts tests/integration/llms-full-feed.integration.test.ts tests/integration/timeline-sitemap-parity.integration.test.ts tests/integration/archive.integration.test.ts` → 3 files, 9 tests passed
- `npx vitest run --configLoader runner --project node tests/bet3-live-verification.test.ts tests/offer-timeline-agent-tools.test.ts` → 2 files, 44 tests passed
- `npx vitest run --configLoader runner --project node tests/d1-migration-sync-check.test.ts` → 1 file, 6 tests passed
- The typecheck fix is verified by the CI typecheck jobs (`codex-node-checks`, `preview-assert`) — CI owns typecheck; I did not run `tsc -b` in the worker.

## run-proof

- Seed SQL verified byte-for-byte against migrations 0079 and 0081 via a Node comparison script (both `match: true`).
- `bin/fleet-no-agent-names-check --commit-range origin/main..HEAD` → OK: no agent attribution detected.
- Files touched: 5 (1 new script + 2 typecheck-adjacent config/script edits + 2 test files), all within issue `files:` / `do:` scope plus the mechanical tsconfig registration the new script requires.

## Test plan

- [x] Integration tests run against schema-only migrations (0079/0081 skipped by name)
- [x] Demo seeds exist only where the script ran (tests import and apply seed SQL)
- [x] Seed SQL is byte-for-byte identical to the original migration INSERTs
- [x] No migration file added, deleted, or edited

Closes #2344

net-positive-because: the seed SQL is a byte-for-byte copy of two migration INSERTs (0079 + 0081) moved into a standalone runbook; the line count is the data, not new logic.