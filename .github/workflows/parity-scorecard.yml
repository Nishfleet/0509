# Trigger stub. Monday 03:30 UTC is 09:00 IST. The scorecard lives once in
# Nishfleet/fleet-ops and posts to issue 4267.
name: Parity scorecard
on:
  schedule:
    - cron: "30 3 * * 1"
  workflow_dispatch:
jobs:
  scorecard:
    uses: Nishfleet/fleet-ops/.github/workflows/parity-scorecard.yml@main # same ref runner group 3 enforces for agent.yml; a SHA pin here drifts from agent.yml@main and strands jobs (0509#5231)
    permissions:
      issues: write
      actions: read
      contents: read
      pull-requests: read
    with:
      issue: 4267
