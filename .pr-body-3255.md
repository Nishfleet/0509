## mentions: X recent-search mention search — gated, spend-metered, paid/pending (issue #3255)

Salvage resume of lane `claim/issue-3255` (previous worker died; the work was banked as two unpushed commits on the branch). While this lane was dead, the parallel mention lanes landed on `main` (+123 commits: Bluesky #3252, GDELT #3251, #3306 test fixes, #3178 salvage). This round: rebased both commits onto current main `1cc79b200`, resolved the single conflict (`app/lib/env.server.ts` — main's Bluesky block vs this issue's `X_PAID_ACCESS`/`X_API_BASE_URL` block; both kept), re-proved every gate. Deletion-first: no new module, no new mechanism — the diff rides the existing `x` connector, registry, coverage and customer-copy surfaces, and the issue's MUST NOT TOUCH list is respected (`migrations/**` untouched: query targets ride `source_target.target_key`/`metadata_json`, and migration 0055's CHECK already allows the `x` connector).

**Shipped:**

- `app/lib/presence-connectors/x.server.ts` — a tracked entity's match phrase becomes an X recent-search query target (`GET /2/tweets/search/recent`) alongside the existing handle targets. The money flag `X_PAID_ACCESS=approved` (mirrors the plan doc's `paid_source_pending_nish`) gates BOTH `poll` and `healthCheck`: until it is set, neither issues any paid request — `poll` returns the paid-pending result, `healthCheck` reports pending with `errorCode: paid_source_pending_nish`, independent of the existing rollout/`X_API_BEARER_TOKEN` gate. When approved, every paid read is metered: a `meteredReads` ledger (per-UTC-day, `{requests, posts}`) is carried in `presence_poll_cursor.cursor_json`, merged forward from the prior record and pruned to a bounded day window — spend is observable per entity per day before any activation. Requests go through the existing `presenceSafeFetch` (SSRF-checked) with bearer auth; empty result sets return `ok: true, items: []`. The issue's AI-advisory items (query operators, `max_results`, cadence) stay advisory and bounded by the meter.
- `app/lib/presence-connector-registry.server.ts` — the `x` dispatch now passes the target and the prior `cursor_json` (`record`) into `poll`; the added `cursor.record` option field is additive, every other connector's dispatch unchanged.
- `app/lib/presence-source-coverage.server.ts` + `app/lib/presence-customer-copy.ts` — coverage copy stays honest: X mention search is labelled a paid, metered source whose activation is pending a spend decision, with the new `paid_source_pending_nish` reason surfaced ("This source is a paid source and is not active yet."); nothing implies it is live.
- `app/lib/env.server.ts` — `X_PAID_ACCESS` and `X_API_BASE_URL` (test/ops hook only, still SSRF-checked per request).
- `tests/integration/x-mention-search.integration.test.ts` (new) — the issue's termination test: real migrations on real local D1 via the workers project; a registered query-type `source_target` for the `x` connector; asserts the recent-search call uses the entity's match phrase and maps posts to `presence_item` rows (canonical status URL, author handle, `publishedAt`, `contentHash`); empty results → `ok: true, items: []`; metered-read usage recorded in `presence_poll_cursor.cursor_json`; `healthCheck` reports gated while `PRESENCE_X_ROLLOUT` is unset or `X_API_BEARER_TOKEN` is missing, and paid-pending coverage until the money flag clears. The X API is mocked at the fetch seam (no paid calls in tests).
- `tests/presence-source-coverage.test.ts` — coverage pins updated for the paid/pending state.
- `.lane/reports/claim-issue-3255.md` — lane evidence: rounds 1–3, acceptance mapping, conflict resolution.

**Phase shipped:** the whole issue is one gated wiring phase — no migration, no dependency, production stays parked. Activation (setting `X_PAID_ACCESS=approved` + the bearer token in production) is the MONEY decision the issue reserves; it is wiring-only once the spend decision lands.

Verification:
- (fresh, this resume, after rebasing onto current main `1cc79b200` — neither shipped code file was touched by the 123 intervening main commits)
- Termination command: `npx vitest run tests/integration/x-mention-search.integration.test.ts` → **16/16 passed, exit 0** (real workerd, real D1, real migrations; X API mocked at the fetch seam)
- `npx vitest run --configLoader runner --project node --changed origin/main` → 370 files / 4603 tests, all passed, exit 0
- `npx vitest run tests/no-time-bomb-fixtures.test.ts` → 1/1 passed, exit 0 (the integration test's 2 historical fixture instants carry the documented `fixed-date:` markers)
- Targeted workers project (x + bluesky + gdelt mention, mention-source-activation, presence-poll-targets, mention-digest-resweep — the suites reachable by the registry/service/coverage diff): 6 files / 70 tests, all passed, exit 0
- `sgscan --base origin/main` → "No new security findings"
- Local crgate: skipped — CodeRabbit is not signed in on this machine (`coderabbit auth login` is interactive; this headless unit has no session); the PR-level CodeRabbit/CI review gates cover this diff.

run-proof:
- unit pi-issue-0509-3255 (resume of the same lane) ran the termination commands locally to green at head `cb0929396` on `claim/issue-3255` (rebased onto origin/main `1cc79b200`); CI runs the repo test script (node + workers vitest projects) as the verification workflow.

research:
- reuse-first: the diff extends the existing `x` connector (handle-target poll, `PRESENCE_X_MOCK` seam, `presenceSafeFetch`), the existing registry dispatch, coverage copy and customer-copy reason maps — no new module, no new dependency, no duplicated mechanism. Query targets reuse `source_target.target_key`/`metadata_json` (no schema change, per the issue). X API v2 recent search per the official docs referenced in the issue.

help-first:
- no new tooling built; the poll contract types (`XPollTarget`, `XPollCursor`) are exported from the existing connector module, not a new helper surface.

loose-ends: prod-activation (setting `X_PAID_ACCESS=approved` + `X_API_BEARER_TOKEN` in production awaits the MONEY spend decision — by issue design, not a code gap; until then production reports paid_source_pending_nish and issues no paid calls)

net-positive-because: the issue's deliverable is new, required coverage — the termination integration test (+449) and the x connector's gated query-target/meter path (+424); beyond those two required surfaces nothing is added (no new module, no new mechanism, 12 lines deleted — every other hunk extends an existing surface).

Closes #3255
Relates to #3171
