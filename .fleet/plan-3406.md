# Plan — Nishfleet/0509#3406 (triage eight stale-red assertions as one class)

Metric: all eight of #3081 #3097 #3124 #2944 #2703 #2865 #3212 #2797 closed,
each with a reproduced-and-fixed red on current main OR a proof-of-green on
current main. Worktree: /home/nish/workspaces/agent-worktrees/issue-0509-3406
on claim/issue-3406 @ origin/main 7ecb3c6dd.

- [x] phase 1 (accept 1): TRIAGE — run each issue's named test/spec on current
  main; record per-issue verdict STILL-RED (repro output) or PROOF-GREEN
  (command + observed output + sha) in `.lane/reports/claim-issue-3406.md`.
  Unit files: tests/public-status-counters.test.ts (#3212),
  tests/marketing-proof-brief.test.tsx (#3097/#3124/#3081),
  tests/brands-category.loader.test.ts (#2865),
  tests/digest-partial-failure-status.test.ts (#2797). E2E: inspect
  e2e/journey-3-release.spec.ts + e2e/local-authenticated.spec.ts
  /app/digests assertions (#2703); check recent Deploy production run logs +
  git log on journey-2 spec/layout for #2944.
- [x] phase 2 (accept 1+2): no STILL-RED unit tests — #3212 green (fake
  timers), the #3097/#3124/#3081 family green (withCaptureAge fixture fix on
  main), #2865 fixed-by-5b6dcc811, #2797 fixed-by-7533fc018. Nothing to fix;
  amended: phases 2+3 collapse — the only code change is the #2703 remnant.
- [x] phase 3 (accept 1): #2703 remnant DONE by resume worker —
  e2e/local-authenticated.spec.ts:882 now asserts /app/briefs (live-proven:
  authed GET /app/digests -> 302 /app/briefs on e2e:serve:local); dashboard
  link at app/routes/app.dashboard.tsx:1036 now points at
  /app/briefs?firstrun=1 directly. #2944 fixed-by-602b86723 (measurement-race
  fix; run 34779307565 passed journey-2 mobile). Bonus: journey-1 trial-CTA
  nav budget fix already committed as 6bf7f07d7.
- [x] phase 4 (accept 1+2+3): close-out — proof comments + closes posted on
  the seven green/fixed issues (#3212, #3097, #3124, #3081, #2865, #2797,
  #2944); PR body Closes #2703. Prevention: not all-stale — one real remnant
  landed; standing prevention is #3359 required-context aggregation + #3246
  e2e freshness scan (USED, no new machinery).
- [x] phase 5 (accept 4+5): ship — DONE via PR #3463 (merged 16bd92843,
  2026-09-14T02:22Z): diff confined to e2e/**, app/**, tests/**, lane docs;
  no tests removed/skipped; no migrations/**, no gate-owned paths.
- [ ] phase 6 (accept 1): durable close-out for #2944 + #2865 — both were
  closed 01:40Z with fixed-by evidence and reopened by the deploy-fault
  gate: their delivery merge SHAs (9cc3f3bab / 61e3cae75) compare `diverged`
  vs rebuilt main, so no green run can ever contain them (verified live:
  `deploy_fault_has_proof` rc=1 for both). This lane lands a bookkeeping PR
  carrying `Closes #2944` + `Closes #2865`; the squash merge mints a
  contained delivery SHA on the current line, GitHub auto-close performs
  the close, and the next green `Deploy production` makes it gate-legal.
  Green run 34798996355 (16bd92843) already proves both fixes live.
  Manager note: remaining work was bookkeeping (lane evidence + plan), so
  it was executed inline per the proportionality clause — no worker phase.
