# Plan — issue 3380 (claim/issue-3380)

Signup→first-run→first-brief loop (BET 7) for the 2026-09-11..13 watchlist. Free scheduled cadence = Monday 03:00 UTC only (plan-entitlements WEEKLY_SCAN_UTC_DAY=1/HOUR=3), so Friday→Sunday 0 runs is by design; the 5-minute promise is the FIRST-SCAN queue, missing on two creation paths (auto-competitor-bulk-accept, manual-add route). #2908 disabled nothing.

- [x] phase 1: (A1) wire the existing queueFirstWatchlistScan into the two watchlist-creation paths that lack it (app/lib/auto-competitor-bulk-accept.server.ts, app/lib/watchlist-route-actions.server.ts manual-add) — smallest durable fix, no new machinery, no schema change; record the root-cause chain (free=weekly-Monday-03:00Z scheduled cadence + first-scan missing on those paths; #2908 disabled nothing) for the PR body.
  DONE 2026-09-14: salvage wiring (ac2b52c9f) + all four `createWatchlistWithinLimit` callsites verified queue-adjacent; cadence verified in app/lib/plan-entitlements.ts:324-325 (WEEKLY_SCAN_UTC_DAY=1, HOUR=3 — the 09-11T21:35Z cohort had no Monday-03:00Z slot yet when the issue was filed; #2908 was the data wipe, it disabled nothing). Inner-loop fallout caught+fixed: 2 tests in tests/auto-competitor-suggested-panel.test.ts hit the node project's DB-stub (no `.prepare`) through the new single-accept queue call — fixed with the bulk-accept precedent's module-seam mock + once-only queue assertions; 2 files / 33 tests green, exit 0 (r2). Manager amendment: ONE cumulative reviewer round after phase 2 instead of one per phase — reason: this unit died 5x at StartLimitBurst; each extra subagent round-trip risks another dead claim.
- [ ] phase 2: (A2) add tests/integration/monitoring-pickup.test.ts — real migrations, workspace+user+watchlist via a production creation path, run the pickup, assert a watchlist_run row (joined on the watchlist id) plus the digest/all-quiet outcome; prove it fails on origin/main's behavior (no git stash — the repo's checkouts hold other agents' stashes).
- [ ] phase 3: (A3+A4) record in the PR body: no-migration justification (migrations/ untouched), the #2919-answered-by-#2908 note, cross-links #3322/#3176/#2778/#2734, and the physically-real wait: the prod watchlist_run/digest proof lands at the next Monday 03:00 UTC tick after deploy (termination: npx wrangler d1 execute 0509 --remote ... | jq -e '.[0].results[0].runs > 0' — a date-gate, not a block).

## Notes

- Reviewer/stall: Act-on findings → ONE fresh-worker retry per phase; Consider/Noted/Dismissed recorded here. A phase with no box ticked in 10 minutes closes with what works + `stalled: phase N — <reason>` here. Manager may amend with a one-line reason.
- Memory (fleet-ops#4891, MemoryMax=4G): no --coverage, no typecheck, no tsc -b; ONE vitest at a time; tests via `npx vitest run --configLoader runner --project workers tests/integration/monitoring-pickup.test.ts`.
- D1: ONE phase per PR; expected diff = code + test only, migrations/ untouched (expand-only + #908 senior process + ledger-checked number ONLY if a migration proves necessary).
- Test-removal: none planned; if any test is skipped/renamed, add `test-removal-justified: <true reason>` to the commit.
- Lane evidence: `.lane/reports/claim-issue-3380.md` (unique path; `git add -f` if .lane is .git-info-excluded). Never a shared report file.