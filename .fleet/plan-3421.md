# Plan — issue #3421: route `/guides/can-ChatGPT-monitor-competitor-ads`

Owned answer to the buyer's first question — "can my AI (ChatGPT/an agent) just check this?" Slug is EXACT: uppercase `ChatGPT` stays in the PATH; only the signup-source MARKER is lowercase (`/^[a-z0-9][a-z0-9-]{0,39}$/` forbids uppercase). Lane discipline: #3166 owns deploy/404s, #3304/#3353 own AI-visibility measurement, #3326 owns edge-serve — note overlaps, absorb nothing. No migrations, no D1, no gate-owned edits. One checkbox per acceptance bullet; each phase ends runnable and green.

## Phase 1 — route + content + signup marker

- [x] phase 1: create `app/routes/guides.can-ChatGPT-monitor-competitor-ads.tsx` following `app/routes/guides.how-to-track-competitor-ads.tsx` as nearest precedent, with the `guideSearchPreviewPath` export and the FAQ entries export
- [x] phase 1: SEO posture per acceptance 1 — #3098 og:image PNG auto-derived by publicSeoMeta for /guides/<slug> (no per-page card code) + structured data (WebPage + Article + FAQPage JSON-LD, canonicalLinks + buyerSurfaceHreflangLinks)
- [x] phase 1: fact block (a) the 403 — a plain HTTP client gets 403 from the Meta Ad Library, re-verified live 2026-09-13; record that date on the page
- [x] phase 1: fact block (b) one point in time — an AI sees now, not what the offer said on 12 June
- [x] phase 1: fact block (c) unattended vigilance — somebody has to be there at 03:00; "we checked 24 ads at 03:00 and nothing moved" is a claim only a system that ran can make
- [x] phase 1: honest conclusion + exit — what an AI does well (summarising, drafting angles) vs what only always-on capture owns; exit to the free anonymous /search preview (source marker, not a demo form); all content from already-public copy, no new claims/numbers
- [x] phase 1: add `app/routes/$locale.guides.can-ChatGPT-monitor-competitor-ads.tsx` — $locale mirror re-exporting the EN route, canonical→EN, following the 7-existing-guides re-export precedent (cluster registration happens in phase 2)
- [x] phase 1: `app/lib/signup-source.ts` — new lowercase marker constant `guide-can-chatgpt-monitor-ads` + ALLOWED_SIGNUP_SOURCES entry (marker pattern forbids uppercase; only the PATH slug is uppercase)

## Phase 2 — registry wiring (8 mechanical items)

- [x] phase 2: `app/routes.ts` EN line — `route("guides/can-ChatGPT-monitor-competitor-ads", "routes/guides.can-ChatGPT-monitor-competitor-ads.tsx")` (~line 204 guide area)
- [x] phase 2: `app/routes.ts` `$locale.` cluster line (~line 277 area)
- [x] phase 2: `app/lib/locale-markets.ts` — BUYER_SURFACE_GUIDE_PATHS +1
- [x] phase 2: `app/lib/seo.ts` — SITEMAP_PATHS +1 (~line 1079–1097 guide block)
- [x] phase 2: `app/lib/public-markdown.ts` — LLMS_PAGE_DETAILS +1 (REQUIRED: the `_llmsDetailsCoverSitemap` mapped type makes a missing description a compile error under CI typecheck)
- [x] phase 2: `app/routes/guides.tsx` — GUIDE_ENTRIES +1 card (section-parents.test.ts pins exact set equality with sitemap paths)
- [x] phase 2: `app/routes/docs.tsx` — one `to="/guides/can-ChatGPT-monitor-competitor-ads"` Link (inbound link, #3167 invariant)
- [x] phase 2: `app/routes/competitor-monitoring.tsx` — one `to="/guides/can-ChatGPT-monitor-competitor-ads"` Link (inbound link, #3167 invariant)

> Manager resume note (2026-09-14): phase 2's 8 items were committed by the prior
> run's worker (salvage commit 4b2559781) but the boxes were never ticked before
> the unit died. Manager verified all 8 items present in the diff, rebased onto
> origin/main aaa189b80, re-ran guides-routes/section-parents/how-to-track
> baselines green (51 tests), and ticked the boxes. Phase 3 (tests) delegated to
> a fresh worker.

## Phase 3 — tests + verify-to-green

- [ ] phase 3: extend `tests/guides-routes.test.ts` registry with the new route
- [ ] phase 3: add `tests/guides-can-ChatGPT-monitor-competitor-ads.route.test.ts` in the `tests/guides-how-to-track.route.test.ts` posture — route renders, meta/og/structured data present, sitemap entry — pinning the EXACT uppercase slug
- [ ] phase 3: uppercase-slug fact (verified) — the registry's mechanical corpus regexes (`[a-z0-9-]+`) simply don't match the uppercase slug, so triple-agreement gates stay green; parseSocialCardPathname and guideSocialCardForPathname use `[^/]+` and accept it; section-parents.test.ts compares runtime arrays so it sees it fine — no gate reshaping expected
- [ ] phase 3: no test deleted; if an assertion must be reshaped rather than extended, add the #3414 convention `test-removal-justified:` trailer to the committing commit
- [ ] phase 3: memory rules — bare vitest only, no `--reporter` flag (Vitest 4 removed `basic`), respect VITEST_MAX_WORKERS, never run coverage/typecheck/tsc in the worker (CI owns typecheck)
- [ ] phase 3: verify-to-green — run the Verify command below; both files pass

## Verify

```
npx vitest run tests/guides-routes.test.ts tests/guides-can-ChatGPT-monitor-competitor-ads.route.test.ts
```

Run in the worktree.

## Rollback

Single-PR revert: one route file + its test + registry/sitemap lines. No data.
