# Plan — issue #3301 (CSP-nonce gap: /search streaming-hydration + JSON-LD inline scripts)

Manager: pi-issue-0509-3301 (difficulty: heavy). Worktree: `/home/nish/workspaces/agent-worktrees/issue-0509-3301` @ claim/issue-3301 (base 4bf178e30).

Verified root causes (live, 2026-09-12):
- A: `app/entry.server.tsx` calls `renderToReadableStream(<ServerRouter/>, {onError})` without the `nonce` option. react-dom's edge build stamps `options.nonce` onto EVERY runtime inline script it emits (`$RT` timing script `id="_R_"`, the `$RB/$RV/$RC` boundary-completion helpers). Missing → CSP-blocked → streamed-hydration bridge never installs → React #419 → full client re-render. (ServerRouter's own nonce covers only react-router's handoff chunks — #2724.)
- B: `app/lib/seo.ts` `jsonLdScriptProps(data)` has no nonce; `app/routes/search.tsx:2104` spreads it bare → JSON-LD CSP-blocked.
- Prod ground truth: CSP `script-src 'self' <beacon> 'nonce-<per-response>'` (single nonce, no hashes, no unsafe-inline); 11 inline scripts, exactly 3 nonceless (ld+json, `_R_`, `$RB`). Reproduced by curl 2026-09-12.

Manager adjustments to planner draft (decided):
- Money-path nonce wiring extends ONLY to the 4 surfaces the walk covered (search, result = phase 2; pricing + signup-start = phase 5 contingent). /login, /brands, /marketing, compare/guides/privacy/terms → follow-up issue, NOT this PR (scope: issue acceptance is /search surfaces; accept-4 options are "comment" or "follow-up").
- Guard collects BOTH `page.on("console")` errors AND window `securitypolicyviolation` events (no class filters, no Chromium severity dependence) + `pageerror`. Note: hero-fold's "zero console errors" is actually pageerror-only — our spec is the stronger collection; do not weaken it to match.

- [ ] phase 1: Prevention gate first, proven RED — playwright.config.ts `chromium` + `mobile-chromium` testMatch → `/(hero-fold|search-console-hygiene)\.spec\.ts/`; new `tests/search-console-hygiene.spec.ts` (hero-fold posture; helpers from e2e/helpers/release-experience where they fit; NO overflow assertion — not in acceptance): journeys (a) `/search?q=zzqqxx9noresult&country=all` (empty; bare /search 302s to /brands), (b) `/search?q=nike&country=all` (populated), (c) populated + click first result → `?selected=` proof state; each: zero console errors, zero securitypolicyviolation, zero pageerrors. Structural half via request fixture on RAW served HTML (not hydrated DOM): extract nonce from the response's CSP header, assert every src-less `<script>` carries that exact nonce, ld+json present AND nonced, script-src has no unsafe-inline/no hashes. Then `E2E_PROD_BASE_URL=https://0509.io npx playwright test tests/search-console-hygiene.spec.ts` → RED (guard fires; 3 nonceless + CSP violations). Save red output into this file. Commit.
- [ ] phase 2: The fix, both root causes + unit guard — entry.server.tsx: add `nonce: cspNonce` to renderToReadableStream options (undefined-safe; no-nonce path preserved). seo.ts: `jsonLdScriptProps(data, options?: { nonce?: string })`, nonce spread into props only when defined (existing 50+ callers unchanged). search.tsx: import `cspNonceForRender` from ~/root; compute SSR-only nonce via `cspNonceForRender(rootData?.cspNonce, typeof document === "undefined")` (hydration-safe: "" on client — the #34567890736 / tests/root-csp-nonce-hydration.test.tsx contract); pass `{nonce}` to the jsonLdScriptProps call. tests/entry-server-csp-nonce.test.ts: vi.mock react-dom/server passthrough capturing (element, options); assert options.nonce === context nonce, and === undefined when context absent; keep all existing assertions. Budget: `npx vitest run --configLoader runner --project node --changed origin/main` (NO coverage/typecheck in-worker). Commit.
- [ ] phase 3: Local green-loop — `E2E_START_LOCAL_SERVER=1 E2E_PROD_BASE_URL=http://127.0.0.1:4179 npx playwright test tests/search-console-hygiene.spec.ts` → exit 0 (all 6 journeys: zero console errors, zero CSP violations, zero pageerrors, no #419). If the local release-proof harness needs a different seeded query for a populated result, use the seeded one (check e2e/journey-4-release.spec.ts / tests/release-hydration-bridge.test.ts) — never weaken an assertion to get there. Also: local structural check — 0 nonceless inline scripts on both /search surfaces, CSP header still single-nonce. If red with a NON-CSP console class: that is phase-5 adjudication material, never a filter. Save green output into this file. Commit.
- [ ] phase 4: PR + review + arm — push claim/issue-3301; PR body = plan file + run-proofs (red-on-prod, green-on-local, unit green) + Verification + `Closes #3301`; gate receipts (bin/prove-one-run-check, bin/fleet-exec-review-canary --body, review-adjudication buckets, no agent names — bin/fleet-no-agent-names-check); reviewer round (product repo; bin/fleet-review-arm-check → senior seat via find_senior_seat, ONE round, blocked-by-judge label if BLOCKING on a gate-touch diff — playwright.config + the new spec ARE gate-adjacent); then `gh pr merge --auto --squash`. Post-merge prod-green (termination command) is the next scout's life-canary + posted to the issue; NOT this unit's exit criterion (packet: exit after arm).
- [ ] phase 5: Sweep + adjudication of the 8 suppressed findings (pre-merge, prod = still pre-fix = the right adjudication context) — unbounded console sweep (no MAX_FINDINGS) across the 4 walked surfaces (search empty, populated+selected, /pricing, signup start), BOTH viewports, TEXT logs + screenshots into `.fleet/sweep-3301/` (committed) — never PNG-only. findings.md: one row per finding — class, URL, evidence, bucket (Act on / Consider / Noted / Dismissed-with-reason). CSP/nonce class on pricing/signup-start → wire the same phase-2 `{nonce}` (contingent: only if rootData/nonce is reachable without new plumbing — else Dismissed-to-follow-up). Any DIFFERENT class → its own line: issue comment with evidence path, or a follow-up issue (plain, no labels). Post the adjudication table on #3301. NOT silently absorbed, nothing truncated.

Notes:
- termination (issue): `E2E_PROD_BASE_URL=https://0509.io npx playwright test tests/search-console-hygiene.spec.ts` — fully green only post-merge+deploy; PR proves red-on-prod + green-on-local + unit green.
- rollback: revert the nonce/CSP change; page falls back to today's behavior. App/config only — no migrations, no data.
- stalled: (phase — reason) — none yet.
