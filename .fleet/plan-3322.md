# Plan — issue 3322 (BET 1 dark: digest headline ratio 0%, zero cohort, guards unarmed)

Manager: pi-issue-0509-3322. Written directly by the manager from a completed
investigation (the read-only planner agent had no writable output and the
investigation receipts are in the manager context; planner spawn skipped for
the 10-minute stall rule — reason recorded here per the manager amend rule).

## Goal

Re-populate BET 1's substrate (≥20-competitor watcher cohort via the product's
own surface), fix the top landing-page-extraction bail-out, prove both #1451
guards pass non-vacuously, and arm the #1451 systemd guard with its alarm leg
proven once. No migrations/**, no .github/**, tests added only.

## Investigated receipts (2026-09-12T23:15-23:50Z, production D1 via wrangler,
token sourced from ~/.config/cloudflare/deploy-ci.env — never printed)

- Funnel `cta_pipeline_stage_counts` (day >= 2026-09-05): checks_started 1946,
  page_fetch_succeeded 711, validity_passed 1, dom_extracted 712,
  diff_computed 1, event_emitted 0 (no rows).
- `cta_pipeline_bail_reason_counts` (09-10..09-12): only stage
  `page_fetch_succeeded` rows, 1233 bails: landing_http_error 221+ (09-11),
  landing_blocked 148+, landing_content_empty_or_oversized 84+,
  landing_challenge_page, landing_partial_spa...
- Production substrate: watchlist=1 row ("Launch readiness canary", 0509.io,
  created 2026-09-11T17:56Z, is_active=1, last_scanned_at=NULL — never scanned
  in ~30h despite the 3-hourly tick carrying includeScans:true);
  watch_event=0; digest_item=0; watchlist_run=1; delivery_attempt=4, ALL
  lane='internal' (the #1451 guard samples lane='customer' only -> zero
  sampleable deliveries).
- Schedule (workers/schedule.ts): 0 */3 * * * = monitoring with
  includeScans:true + includeDigests:true (weekly cadence); 0 4 * * * =
  monitoring, digests only, digestCadence "daily"; */5 = status_probes only;
  13 * * * * = monitoring, includeScans:true, no digests.
- #2077 (in-code note, app/lib/cta-pipeline-stage-counts.server.ts): volume
  paths (selection_enrichment, backfill, canary) share the recorder but NEVER
  run the diff stage -> they legitimately never emit events. The 1946
  checks/day are consistent with the launch-readiness canary probing, not
  monitored watchlist checks.
- #2443 finding M9 (in-code note): `baseline_established` reaches the diff
  stage and carries `no_baseline_first_scan` field bails — a first scan
  establishes the baseline and emits nothing by design.
- #2908 (discarded, senior panel): the 11->0 watchlist drop was a deliberate
  canary/telemetry fixture cleanup, "no data restoration attempted (none
  needed)", zero real customers. So the pre-wipe 09-07..09 guard history
  (8/8/3 items, 0 landing_page_*) was fixture substrate: consistent with
  baseline-only first scans + ad-churn events + no surviving second scans.
- #2873 (OPEN, BET 4): capture-validity gate hardening + capture_failed
  state. Coordinate, do not duplicate: if validity bails dominate on the live
  cohort, cross-link, else stay out.
- Guard mechanics (scripts/canary-digest-headline-ratio.mjs): samples
  digest_item joined to delivery_attempt (status 'sent', lane 'customer',
  last 24h via COALESCE(sent_at, created_at) >= cutoff); writes history only
  for real remote samples (or --record); STATE_DIR is DIGEST_HEADLINE_STATE_DIR
  overridable; --file-issue dedupes on open issues whose body carries the
  `digest-headline-ratio-guard:` marker (a drill issue therefore blocks real
  incidents until closed — the drill artifact must say so and the follow-up
  must carry the close duty); exit 1 + issue only when fired AND
  (sampledFromProd || --record).

## Working root-cause hypothesis (to VERIFY, not assume)

Zero landing_page_* events since 09-05 because (a) every check since the
#2908 wipe is a volume/canary path that never diffs (#2077), and (b) the
pre-wipe fixture cohort emitted nothing because first scans establish
baselines (#2443) and the #2908 cleanup removed the targets before
second scans could diff. If the LIVE cohort's second+ scheduled scans still
produce zero landing_page_* events, THAT is a code bug to find via the
#1500/`cta_pipeline_*` telemetry and fix. The "top bail-out" may legitimately
resolve to "substrate, not code" — recorded with receipts either way.

## Phases (one per acceptance bullet; the 10-minute stall rule applies)

- [ ] phase 1 (accept 2 + substrate for accept 1): re-seed a watcher cohort of
  >= 20 competitors through the product's own surface (app signup + watchlist
  creation, or the audited /api/v1 watchlist.create action — NOT direct D1
  inserts), as a real inspectable cohort (not disposable fixtures); then prove
  the scheduled 3-hourly tick scans it (watchlist_run rows, watchlist
  last_scanned_at, cta_pipeline_stage_counts on the monitored path). If the
  tick does not scan the cohort, root-cause why (plan-feature-gate? workspace
  status? launch-readiness special-case?) — that finding feeds phase 2.
- [ ] phase 2 (accept 1): with the live cohort's receipts, name the stage +
  reason where landing-page extraction bails (cta_pipeline_* tables; #1500
  lp_run_audit design; #2157 reason vocabulary; note 09-07..09 logs are past
  retention) and fix the top bail-out. If it is a code bug: smallest durable
  fix + tests added only. If it is substrate (no second scans / no monitored
  path): the fix is the mechanism itself, recorded in the PR. Cross-link #2873
  if validity-gate bails dominate its capture_failed scope.
- [ ] phase 3 (accept 4 + the metric): get a customer-lane digest delivered
  (04:00Z daily rail, or the product's faster surface if one legitimately
  exists — provisional first-brief / instant-batch path), prove BOTH guards
  non-vacuous (canary-cta-detector >= 1 landing_page_cta_changed in 7d;
  digest-headline-ratio totalItemCount >= 1 and ratio >= 0.5, NOT the
  "delivered no digest items" quiet branch); run
  ops/digest-headline-ratio-guard/provision-digest-headline-ratio-guard.sh
  (systemd --user, NO .github/** edits), prove the timer in
  `systemctl --user list-timers | grep digest-headline-ratio`, then force the
  below-floor drill once: DIGEST_HEADLINE_STATE_DIR-isolated fixture via
  --record --file-issue, link the filed drill issue here, and mark it as a
  drill artifact that must be closed so it cannot block real incidents.
- [ ] phase 4 (accept 3 + 5): record the delivered briefs' day-0 headline
  composition + cta events on issue 3322; the 72h observe is physically real —
  file the follow-up issue that carries the 72h evidence duty (guards' history
  + a same-format evidence comment) so the issue can close on merge without
  dropping the observation; then the inner loop to green, repo tests (node
  project, --changed origin/main only; CI owns coverage/typecheck), sgscan,
  crgate, the product-repo reviewer round (bin/fleet-review-arm-check; skip
  -> `review: skipped, no capable seat`), PR body contract
  (Verification:/run-proof:/loose-ends: 72h-observe), arm.

## Risks

- The 3-hourly tick may not scan a FREE/co-canary workspace (plan-feature-gate
  or delivery-policy provisional logic) — phase 1 resolves this before phase 2.
- The 04:00Z rail is ~4h after unit start: if no faster legitimate surface
  delivers a customer-lane digest, phase 3 records the expectation + the
  follow-up issue carries the proof; the PR says exactly what is proven.
- A drill issue with the guard's body marker dedupes future REAL incidents
  until closed — the drill artifact must say so (see phase 3).
- Watch the 42-min unit budget and the 4G MemoryMax: one heavy process at a
  time, no coverage, no typecheck, vitest --changed only.
