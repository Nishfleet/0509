# Plan — issue 3123: ads publisher seed-cohort supply

## Goal
Grow the `/ads/` published surface past 150 URLs by adding curated seed cohorts driven through the existing seed:publisher pipeline — no new machinery, no migrations, no hand-painted pages.

## Phases
1. **Seed cohorts** — add ≥2 curated seed lists under `data/seed-lists/`, same schema as `sneaker-resale.json`, themed on existing `/brands/` hub categories or a cited demand signal.
2. **Dry-run gate** — each new list validated before relying on the nightly.
3. **Nightly publish** — after the nightly tick, sitemap.xml lists ≥150 `/ads/` URLs.
4. **Publish floor test** — pin the floor at the list-input boundary.
5. **Ship** — push claim/issue-3123, PR with plan + reviewer output.

Track:
- [ ] phase 1: ≥2 new curated seed lists under data/seed-lists/ (same schema as sneaker-resale.json: cluster, asOf, sourceNote, domains[] {domain, brand}), themed on existing /brands/ hub categories or a cited demand signal
- [ ] phase 2: each new list validated by the existing dry-run gate: `npm run seed:publisher -- --list=<new-cohort> --dry-run --min-publish=15` exits 0
- [ ] phase 3: sitemap.xml lists ≥150 /ads/ URLs via the existing nightly publisher (observed after nightly tick; no new machinery, no migrations, no hand-painted pages)
- [ ] phase 4: tests/ads-domain-publisher.test.ts extended to pin the publish floor at the list-input boundary (verified+likely=0 -> no publish; >=1 -> publish)
- [ ] phase 5: push claim/issue-3123, PR with plan + reviewer output; memory budget: never vitest --coverage or npm run typecheck; use `npx vitest run --configLoader runner --project node --changed origin/main`

## Files to Modify
- `data/seed-lists/<new-cohort-1>.json`, `data/seed-lists/<new-cohort-2>.json` — new lists
- `tests/ads-domain-publisher.test.ts` — publish-floor pin

## Evidence
`.fleet/` and `.lane/` are evidence locations. Stalled phases close with what works plus a `stalled: phase N — <reason>` note.
