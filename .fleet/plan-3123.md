# Plan — issue 3123: ads publisher seed-cohort supply

## Goal
Grow the `/ads/` published surface past 150 URLs by adding curated seed cohorts driven through the existing seed:publisher pipeline — no new machinery, no migrations, no hand-painted pages.

Manager amendments (2026-09-12, pi-issue-0509-3123 manager): phase 1 now includes SEED_LISTS registration — the nightly publisher's registry is a hardcoded import map (app/lib/ads-domain-publisher.server.ts), so unregistered JSON files are dead input (precedent 5d54db4ff); phase 2 split into per-list boxes so progress ticks inside the 10-min stall rule; phase 4 records the live baseline (sitemap /ads/ count was 114 at session start, up from 91 at issue time).

## Phases
1. **Cohorts + registry** — finalize the two banked curated seed lists under `data/seed-lists/` (same schema as `sneaker-resale.json`), register both in `SEED_LISTS`, pin registry tests following the #2140 precedent pattern.
2. **Dry-run gate** — each new list validated before relying on the nightly: `npm run seed:publisher -- --list=<new-cohort> --dry-run --min-publish=15` exits 0 (paced live probes, 20 req/10 min IP budget; save output to `.fleet/probe-<list>.log`).
3. **Publish floor test** — pin the floor at the list-input boundary.
4. **Sitemap observation** — record growth evidence.
5. **Ship** — push claim/issue-3123, PR with plan + reviewer output.

Track:
- [x] phase 1: ≥2 new curated seed lists under data/seed-lists/ (same schema as sneaker-resale.json: cluster, asOf, sourceNote, domains[] {domain, brand}), themed on existing /brands/ hub categories or a cited demand signal, AND both registered in SEED_LISTS (import + entry) with registry pin tests per the #2140 precedent (validate clean, ≤ cap per list, isSeededBrandDomain)
- [x] phase 2a: `npm run seed:publisher -- --list=beauty-personal-care --dry-run --min-publish=15` exits 0 (evidence in .fleet/probe-beauty-personal-care.log)
- [x] phase 2b: `npm run seed:publisher -- --list=saas-software --dry-run --min-publish=15` exits 0 (evidence in .fleet/probe-saas-software.log)
- [x] phase 3: tests/ads-domain-publisher.test.ts extended to pin the publish floor at the list-input boundary (targeted run: verified+likely=0 domain → skip, never publish; ≥1 → publish), reusing the #2210/#2361 mock infrastructure
- [x] phase 4: sitemap.xml /ads/ count recorded with spot-checks (baseline 114 at 2026-09-12 session start; observed 115 at 2026-09-12T12:11:54+05:30, spot-checks: elfcosmetics.com/sephora.com/ulta.com/glossier.com present — the /ads/ surface tracks coverage-primed domains, so probed beauty domains appear pre-merge; new saas pages publish through the existing nightly publisher post-deploy; no new machinery, no migrations, no hand-painted pages)
- [ ] phase 5: push claim/issue-3123, PR with plan + reviewer output; memory budget: never vitest --coverage or npm run typecheck; use `npx vitest run --configLoader runner --project node --changed origin/main`

## Files to Modify
- `data/seed-lists/beauty-personal-care.json`, `data/seed-lists/saas-software.json` — new lists (banked by salvage commit)
- `app/lib/ads-domain-publisher.server.ts` — SEED_LISTS registration only
- `tests/ads-domain-publisher.test.ts` — registry pins + publish-floor inclusion test

## Evidence
`.fleet/` and `.lane/` are evidence locations. Stalled phases close with what works plus a `stalled: phase N — <reason>` note.

## Phase 1 adjudication (reviewer, manager pi-issue-0509-3123)
- Act on (fixed in cd44438ee): "placeholder" brand string on credobeauty.com → "Credo Beauty" + pin hardened to reject placeholder/whitespace brands; festive cross-list dupes sugarcosmetics.com + mcaffeine.com dropped from the beauty list (festive byte-stable), counts 31→29 / ~121→~119.
- Consider (recorded, not re-delegated): mamaearth.in degrades to the "More brands" hub bucket (brandCategoryForDomain knows mamaearth.com, not .in) — deliberate degradation, page still publishes; revisit only if hub cross-linking becomes a metric.
- Noted: registry-order claim verified (new lists appended after festive/sneaker, live cursor offsets 0–53 untouched); fixture swaps to wayfair.com/bestbuy.com verified absent from all lists; assertions net-up (it 94→102, expect 283→301); no gate-owned paths touched.

## Phase 2 gate results (2026-09-12, detached unit pi-issue-0509-3123-probes)
- 2a beauty-personal-care: 27 publishable / 29 domains (PASS, gate 15), probe2a-exit=0
- 2b saas-software: 24 publishable / 36 domains (PASS, gate 15), probe2b-exit=0
- deliverable .fleet/probes-2a2b-done.log present; unit stopped clean
