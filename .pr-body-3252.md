## Bluesky mention connector — mentions MVP source 3/3 (#3252, epic #3171)

Free public social-search coverage via the Bluesky appview: a tracked entity's match phrase becomes an authenticated `app.bsky.feed.searchPosts?q=<phrase>&sort=latest` query (raw JSON XRPC client — two endpoints, so `@atproto/api` was evaluated and deliberately skipped: `research: raw XRPC fetch client vs @atproto/api SDK — raw chosen, zero new npm dependencies; help-first: deterministic entry point is npx vitest run tests/integration/bluesky-mention-connector.integration.test.ts`).

**Shipped: phase 1 (additive MVP)** per the D1 expand/contract rule — expand-only CHECK widen; no dual-write/backfill/drop needed since `bluesky` rows are net-new.

net-positive-because: this is the issue's specified net-new MVP source — a new connector file, an expand-only migration, and a new 19-assertion integration test that the issue's deterministic termination command requires; app/lib edits are thin wiring (types, registry, gates, coverage).

### What changed
- NEW `app/lib/presence-connectors/bluesky.server.ts` — `id: "bluesky"`, `supportedModes: ["self", "competitor"]`; session via `com.atproto.server.createSession` (env-held fleet app-password), search via `app.bsky.feed.searchPosts`, bounded 2-page cursor pagination, SSRF-hardened fetch (`resolvePublicHttpUrl` + `fetchWithTimeout` + bounded response reads) on both calls
- NEW `migrations/0098_widen_source_target_connector_bluesky.sql` — expand-only CHECK widen, 0093 table-rebuild pattern with child-row snapshot/restore; rollback removes code, never data
- Registry / coverage / access gates / env types wired: `PRESENCE_BLUESKY_ROLLOUT` + `BSKY_IDENTIFIER`/`BSKY_APP_PASSWORD` credential check; surfaces as `gated` in `presenceSourceCoverageForDocs()`
- NEW integration test (real workerd, real D1 migrations): migration READ **and** WRITE path asserted through the real `source_target` CHECK

### Credentials
App password never logged, never in any URL, never persisted — it only rides the createSession request body; the ephemeral access JWT is cleared before poll returns. Credential *storage* in `source_connection.encrypted_credentials` is the existing subsystem (unchanged, read-only from this PR's perspective).

### Gate paths
No `.github/workflows/**`, `.github/scripts/**`, `scripts/ci-*`, CODEOWNERS, or gitleaks/semgrep files touched. No existing migration modified. No existing connector touched. No test removed or skipped; assertion count is net-positive (19 new integration tests + 2 updated catalog fixtures).

### Verification
- Termination command (real run): `npx vitest run tests/integration/bluesky-mention-connector.integration.test.ts` → **19/19 passed** (Test Files 1 passed, Duration 7.64s)
- Affected suite: `npx vitest run --configLoader runner --project node --changed origin/main` → 4546 passed / **3 failed** in `tests/launch-readiness-guard.route.test.ts` (#3146 canary) — **PRE-EXISTING**: reproduced the same 3 failures on a clean `origin/main` checkout with this diff absent; not touched by this PR
- `sgscan`: "No new security findings."

### run-proof
- Unit: `pi-issue-0509-3252` (systemd worker, unattended); tests executed in worktree `/home/nish/workspaces/agent-worktrees/issue-0509-3252-r` against real workerd (vitest-pool `workers`, `cloudflare:workers` D1 binding = real local D1 with the full migration chain applied in setup)
- Deterministic termination command exits 0 on this branch (clean checkout of `claim/issue-3252` @ 5036afde0)

### Test plan
- Covered above: migration applies cleanly with child-row preservation; 'bluesky' accepted by the real CHECK (write) and readable (read); empty result → `ok:true, items:[]`; canonicalUrl is the public `bsky.app/profile/<handle>/post/<rkey>`; `contentHash`/`publishedAt`/`author` on every item; session + search through the SSRF path; unauthenticated search impossible (auth failures short-circuit before any search call); healthCheck gated when rollout/credentials missing, healthy when searchPosts answers, degraded when unreachable

### Review
- review: skipped, no capable seat

loose-ends: live-credential rollout (BSKY_* secrets + PRESENCE_BLUESKY_ROLLOUT flip is a separate rollout decision, per the issue's UNKNOWNS — proven against fixtures here, live-verified at rollout)
