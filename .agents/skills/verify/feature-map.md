# Feature map

What exists, how a user reaches it, what it does. Nothing else.

This file is the answer to "an agent can run the app but does not know what the
user meant" (Lauren Tan, *2,500 PRs*, 11:02). It is materialised memory: a route
that is not here cannot be reached, and a row here that no route serves is a
lie. `app/routes.ts` is the only registry — start there, not from a guess.

**Every PR that adds, removes or changes a route updates this file in the same
PR.** The Opus reviewer checks it against `app/routes.ts` and the e2e titles
before approving. There is no test that reads this file; a test about a doc is
a test about the fleet, and `docs/REBUILD-DONE.md` §D forbids those.

Legend: **Reach** is how a human gets there — the route or entry point, the
element's role and accessible name, and the keyboard path with no pointer.
**Does** is what it does. **Proof** is the e2e test or journey that asserts it.

---

## Public

| Route | File | Reach | Does | Proof |
|---|---|---|---|---|
| `/` | `public/index.html` | `https://0509.io` (the `/` route); `Tab` to the one link, the footer address `support@0509.io`; keyboard: `Tab` | The quiet rebuild notice. One `h1` (copy owned by `public/index.html`, not asserted verbatim), one contact link to `support@0509.io`. Static file: no loader, no client state. `robots: noindex` until the gate lifts. The real homepage waits at `/design/landing` until launch (Nish, 2026-09-24). | `e2e/smoke.spec.ts` — headline, contact link, no horizontal scroll at 390, no console errors |
| `/login` | `app/routes/login.tsx` | the `/login` URL; nothing links to it yet. `Tab` to the email text field, type, `Enter`; the **Sign in with a passkey** button (role `button`) is the next stop after submit; the footer links `support@0509.io`, **Privacy** (`/privacy`) and **Terms** (`/terms`) are the last stops | One input plus a "Sign in with a passkey" button. Submitting the input POSTs to the same route; better-auth mints and sends a magic link, then the page swaps to "Check your email". Never reveals whether the address exists. The passkey button runs better-auth's authenticate ceremony (`generate-authenticate-options` → `verify-authentication`) and lands on `/app`, which redirects to `/onboarding` until the workspace has a self entity. The footer carries `support@0509.io`. | `e2e/smoke.spec.ts` — heading, labelled input, enabled buttons including the passkey control, footer address. Sending is **J1** — `e2e/j1-magic-link.spec.ts`; the passkey ceremony is **J2** — `e2e/j2-passkey.spec.ts`; the never-reveals-existence claim is `e2e/magic-link-expiry.spec.ts` |
| `/privacy` | `app/routes/privacy.tsx`, copy in `app/lib/legal/privacy.ts`, drawn by `app/components/legal-page.tsx` | the `/privacy` URL, and **Privacy** in every page footer. `Tab` to the home mark (link to `/`), the **On this page** links (one per section plus Contact), the **Contact** address, then the footer links `support@0509.io`, **Privacy** and **Terms** | The privacy policy, in plain words. Last-updated date, then: the short version; who we are; what we collect about customers (email, passkey public half, sessions with IP and browser, workspace, scrambled agent keys, billing status, support emails, cookieless page views and scrubbed error reports); what we collect about tracked brands (public only; never private individuals, minors, private accounts or anything behind a login); how we use it (never sold, never advertising); who helps run 0509 (Cloudflare, AI model providers, the payment provider, Sentry, Gmail, GitHub, each with what it sees); sharing is a picture, no public pages; agent keys are read-only; two cookies and no banner; how long each thing is kept; deleting (remove and forget); rights; security; children; the 72-hour removal for brands; changes. The operator name, governing law and every number are constants in `app/lib/legal/document.ts`. Every sentence is backed by the code or `docs/REBUILD-GUARDRAILS.md`; the thirty-day feed rule is not stated. | `e2e/privacy.spec.ts` — heading, who we refuse, what we collect about customers and brands, the six helpers, picture-only sharing and no cookie banner, one-year retention, remove and forget, the 72-hour removal, the rights, a contents link for every `h2`, the footer address, the wordmark loads `/`, no horizontal overflow, no console errors, one JSON-LD graph. The suite runs at 1440 and 390. `tests/unit/privacy-classes.test.ts` — every colour and type utility on the legal page compiles |
| `/terms` | `app/routes/terms.tsx`, copy in `app/lib/legal/terms.ts`, drawn by `app/components/legal-page.tsx` | the `/terms` URL, and **Terms** in every page footer; keyboard as `/privacy`, plus the in-section link to `/privacy` under Your data | The terms of service, in plain words, with the same layout and date as `/privacy`: the short version; who can use 0509 (18+, business use); what it does (public sources, no completeness promise, degraded sources marked, not advice); fair use; agent keys read-only and revocable; sharing is a picture; plans and payment kept generic (no plan names or prices until billing is decided: cancel any time, access to the end of the paid period, price-change notice); your data; what belongs to whom; removal requests; availability; the liability cap; ending the agreement; changes; law and disputes. | `e2e/terms.spec.ts` — heading and ISO date, business use and no-advice wording, read-only agents, no public pages, cancel any time with no euro price on the page, the liability cap and governing law, the `/privacy` link, a contents link for every `h2`, the footer links, no horizontal overflow, no console errors, one JSON-LD graph |
| `/api/health` | `app/routes/api.health.ts` | `GET /api/health`; no interactive element; keyboard: — | `{ status, app, timestamp }`. Reads nothing on purpose: a health check that queries the database reports the database. | `e2e/smoke.spec.ts` — 200, `status: "ok"`, parseable timestamp |
| `/robots.txt` | `app/routes/robots[.]txt.ts` | `GET /robots.txt`; no interactive element; keyboard: — | Generated from the public-route manifest in `app/lib/public-routes.ts`: allows `/`, disallows the manifest prefixes (`/app`, `/api`, `/mcp`, `/u`, `/login`, `/onboarding`, `/oauth`, `/design`) and points crawlers at `/sitemap.xml`. `/` keeps its `noindex` meta until the landing ships, so `/` is not in `PUBLIC_PATHS` and not in the sitemap. | `e2e/seo.spec.ts` — 200, `text/plain`, the disallow rows, the sitemap line; `tests/public-routes.test.ts` — fails on a route in `app/routes.ts` not classified in the manifest |
| `/sitemap.xml` | `app/routes/sitemap[.]xml.ts` | `GET /sitemap.xml`, also named by the `Sitemap:` line in `/robots.txt`; no interactive element; keyboard: — | A sitemaps.org `urlset` of `PUBLIC_PATHS` from `app/lib/public-routes.ts`, with absolute URLs built from the request origin. No customer page is ever listed: there is no public customer page (sharing is a picture, `docs/REBUILD-STANDING-CARD.md`). | `e2e/seo.spec.ts` — 200, `application/xml`, the `urlset`, the `/privacy` loc, no `/` loc while `/` is noindex; `tests/public-routes.test.ts` — every public path listed and every top-level route whose `"/" + path` is in `PUBLIC_PATHS` present, plus the escape of `&` |
| `/llms.txt` | `app/routes/llms[.]txt.ts` | `GET /llms.txt`; no interactive element; keyboard: — | The llmstxt.org summary an AI agent reads: an H1, a one-paragraph blockquote of what the product is, the plans with the trial terms, the MCP URL, the live sources from `app/lib/coverage.ts`, the homepage questions from `app/lib/faq.ts`, an **Agents** section linking the MCP server and the API reference, and one link per `PUBLIC_PATHS` entry with its summary. Generated by `llmsTxt` in `app/lib/public-routes.ts`; a public path without a summary fails `npm run typecheck`. No `llms-full.txt`. | `e2e/seo.spec.ts` — 200, `text/plain`, the H1, the blockquote, the `/privacy` link; `tests/public-routes.test.ts` — spec shape, MCP URL, a link line for every public path; `tests/unit/coverage.test.ts` — no kind without a live source is named |
| `/u/:token` | `app/routes/u.$token.tsx` | the **List-Unsubscribe** link inside a brief email; GET renders a confirm page, `Tab` to the **Unsubscribe** button (role `button`), `Enter`; the RFC 8058 `List-Unsubscribe=One-Click` POST from the mail client hits the same route; the footer links `support@0509.io`, **Privacy** (`/privacy`) and **Terms** (`/terms`) are the last stops | Opening the link writes nothing: mail scanners open links on their own, so a GET that unsubscribed would drop people who never clicked (0509#4593). GET shows "Stop the weekly brief?" and one **Unsubscribe** button. Only a POST, the button or the one-click header, writes `email_suppression` through `app/lib/data/email_suppression.server.ts`, then the page says "You're unsubscribed". Every response is `Cache-Control: no-store` and `robots: noindex, nofollow`. Unknown or rotated tokens get the identical page and the identical 200 with no row, because a 403 or a different response would confirm a token exists (charter `#3842`: opaque token lookup only, never derived or signed). The send lane's pre-render suppression check then skips the next send with no `send_attempt` row. Making `/u` reachable in production needs a Cloudflare Access bypass for `/u/*`, a provider change that is Nish's to authorize. | `e2e/unsubscribe.spec.ts` — GET is a 200 `no-store` confirm page with the heading and no "You're unsubscribed", the button leads to "You're unsubscribed", and a raw one-click POST is a 200; `tests/integration/unsubscribe.integration.test.ts` — exactly one `email_suppression` row per POST, a second POST keeps the first `created_at`, unknown and missing tokens write no row, and `deliver` then reports `suppressed` with zero messages and no `send_attempt` rows |
| `/api/auth/*` | `app/routes/api.auth.$.ts` | the browser follows the magic link here; no interactive element of its own; keyboard: — | better-auth's whole surface, mounted whole: magic-link request and verify, passkey registration and assertion, session, sign-out. Nothing is reimplemented above it. | **J1** — `e2e/j1-magic-link.spec.ts`; **J2** — `e2e/j2-passkey.spec.ts`; single-use replay, real-TTL expiry and request-path existence opacity — `e2e/magic-link-expiry.spec.ts`; the sign-in email's copy and its 600 px light/dark rendering — `e2e/magic-link-email.spec.ts` (J1) |
| any other path | `app/routes/unmatched.tsx`, drawn by `app/root.tsx` and `app/components/error-page.tsx` | type a URL that is not a page, for example `/this-page-is-not-here`; `Tab` to the one link, `Enter` | 404 in the product skin. The heading says the page is not here and names the path. One action. Signed in, that link goes to `/app`. Otherwise it goes to the landing at `/`. The heading is not a status code. | `e2e/not-found.spec.ts` — 404 status, heading, one action, no horizontal scroll, no app console errors, at 1440 and 390. The console-error filter matches the 404 by **status code**, never by reason phrase: production is HTTP/2 (no reason phrase, Chromium logs `404 ()`), the local webServer is HTTP/1.1 (`404 (Not Found)`), so pinning the phrase was true only in preview (0509#4244) |
| `/design/brand-chips` | `app/routes/design.brand-chips.tsx` | the `/design/brand-chips` URL; `Tab` through the chips (role `link`), `Enter` on one | Six brands in one wrapping row: you with the accent monogram, a logo that loads, a logo that fails closed onto its monogram, a long name, and an off brand dashed and dimmed, then "+ Add a competitor". No session. | `e2e/brand-chip.spec.ts` — failed-logo fallback, no horizontal scroll, CLS under 0.05, at 1440 and 390. The chip points at the session-gated `/app` route (a stub today, J7/J10), so the spec asserts the `href` and that the click is a client-side navigation (`__stay`); `requireSession` sends both modes to `/login`, because preview applies the D1 migrations before the Worker starts (0509#4244) |
| `/design/brand-switch` | `app/routes/design.brand-switch.tsx` | the `/design/brand-switch` URL; `Tab` to a switch (role `switch`), `Space` toggles it; the You switch is skipped as disabled | Three rows, one per switch state: you (green wash, YOU, not operable), Kindred on, Casetta off (dimmed, dashed chip, 'paused 22 Sept · history kept'). Toggling is local state only; nothing is written. No session. | `e2e/brand-switch.spec.ts` — three states, 44px hit area, Tab + Space, no horizontal scroll, light and dark screenshots at 1440 and 390 |
| `/design/capture-plates` | `app/routes/design.capture-plates.tsx` | the `/design/capture-plates` URL; `Tab` to a plate (role `button`, named "Open before and after: …"), `Enter` | Three capture plates: a plate whose before/after pair loads, a plate that says the capture failed (`Capture failed: page timed out`), and a plate whose object is missing so it falls back to `Screenshot unavailable`. Clicking a plate opens the pair — a centred dialog at 860px and up, a full-width bottom sheet below — closed with `Escape`. No session. | `e2e/capture-plate.spec.ts` — plate size and eager first image, the capture-failed reason, the missing-object fallback, pair centring vs sheet, `Escape` closes, no horizontal scroll, no console errors, CLS under 0.05, at 1440 and 390 |
| `/design/landing` | `app/routes/landing.tsx` | the `/design/landing` URL, behind the staff gate; `Tab` to the wordmark, the five in-page links (hidden below 768px), **Sign in**, then **Start watching €10/mo** (role `link`, to `/login`), the price section's repeat of it, and the footer links `support@0509.io`, **Privacy** and **Terms** | The homepage, staged here until launch while `/` keeps the rebuild notice (Nish, 2026-09-24). In order: header, hero (h1 "Know where you stand. And who's gaining on you.", the priced action, the trial terms, up to three example marks), the mark, how it works, what we watch, built for your agents, the three plans from `app/lib/billing/plans.ts`, questions from `app/lib/faq.ts`, footer. Meta: title, description, canonical `https://0509.io/`, Open Graph with `/og.png`, one JSON-LD graph (Organization, WebSite, SoftwareApplication with one Offer per plan, FAQPage matching the visible questions). Every coverage claim (hero line, example marks, what we watch, plan lines, meta description, FAQ, JSON-LD `featureList`) comes from `app/lib/coverage.ts` and shows only sources marked live; a live source with a `sourceKey` must be an enabled `source` row and every enabled row must be claimed. No ticker and no live standing card yet: both show only real rows, which need the public workspace. Launch is three edits: `index("routes/landing.tsx")` in `app/routes.ts`, delete `public/index.html`, add `/` to `PUBLIC_PATHS`. | `e2e/landing.spec.ts` — one h1, section order, the priced action's name and `/login` target, the meta and JSON-LD with FAQ matching the page, `/og.png` served, no horizontal scroll, at 1440 and 390; no console errors in `e2e/console-clean.spec.ts`; `tests/unit/structured-data-offers.test.ts` — offers priced from the plan module, no rating, no "free"; `tests/unit/coverage.test.ts` — copy names no kind without a live source; `tests/integration/coverage.integration.test.ts` — live claims match enabled source rows both ways |
| `/design/nav` | `app/routes/design.nav.tsx` | the `/design/nav` URL; `Tab` through the four places, `Enter` on one | The four-place nav (`app/components/nav.tsx`) alone: a left rail at 860px and up, a fixed bottom tab bar below. No session; every link points at a session-gated `/app` route. | `e2e/nav.spec.ts` — four links in order, 44px targets, rail vs bar by breakpoint, no horizontal scroll, no console errors, Tab order, focus ring, Enter follows, at 1440 and 390 |

## Signed in

Every route below goes through `requireSession` (`app/lib/require-session.server.ts`),
which redirects to `/login` when there is no session. There is no half-authenticated
state.

| Route | File | Reach | Does | Proof |
|---|---|---|---|---|
| `/app` | `app/routes/app.home.tsx` | the Home place in the nav (left rail at 860px and up, bottom tab bar below). After sign-in, once the workspace has a self entity. Until then the loader redirects to `/onboarding`; `Tab` to Home in the nav, `Enter`; then `Tab` to the button, `Enter` | Home. Today's date in mono, then the greeting by the workspace's own clock with the rank on the green marker ("Good morning. You're #2 of 6 this week.") and the why-line, then "This week's standing": one row per ranked brand in rank order with position, monogram, name, domain and movement worded as the brief words it ("up 1", "down 2", "holding steady", "new", "first week"); your row sits on the green wash and reads "You". The week is the newest weekly `digest`, the same frozen week the brief emailed (`app/lib/home-standing.server.ts`). Fewer than two ON brands: "Add a competitor to see where you stand." with a link to `/onboarding/competitors`. Two or more but no brief yet: "We're gathering the first week. Your first standing comes with the brief on Monday 08:00." with the workspace's real brief time. When ranked, a **Share my rank** button under the standing makes the share picture (`/app/share.png`): the phone share sheet opens with it, anywhere else it downloads, and a failure says "We could not make the picture. Try again in a minute." Below, the signed-in email and an "Add a passkey" button that runs better-auth's register ceremony (`generate-register-options` → `verify-registration`) against the live session. | **J3**, **J4**; the standing, its rows and both empty states — `tests/unit/home-standing.test.ts`; the reader against D1 — `tests/integration/home-standing.integration.test.ts`; the register ceremony is **J2** — `e2e/j2-passkey.spec.ts`; nav and active place — `tests/unit/app-shell.test.ts`; the signed-in walk by tap and Tab+Enter at 1440 and 390 — `e2e/app-nav.spec.ts` (production lane) |
| `/app/share.png` | `app/routes/app.share[.]png.ts` | the **Share my rank** button on `/app`, shown only when the workspace is ranked; `Tab` to it, `Enter` | The share picture (`docs/REBUILD-STANDING-CARD.md`): a 1080x1080 PNG of `app/components/share-image.tsx` with the own brand's monogram and name, "#2 of 6 this week", the week label and the `05|09` wordmark with `0509.io`, drawn from the same frozen week as Home and screenshotted by Browser Rendering (`app/lib/share-image.server.ts`). Never a competitor's name or domain, never the why-line. `private, max-age=3600`. No ranking: 404 "There is no ranking to share yet." Browser Rendering busy or down: 503 with `retry-after: 60`. | `tests/unit/share-image.test.ts` — the card's brand, rank, total and week; no card without a ranking; the page carries the base href, the built stylesheet and light mode, never Kindred, Casetta, their domains or the why-line, and escapes the brand name; `e2e/share-image.spec.ts` — signed out, a redirect to `/login` and no PNG |
| `/app/competitors` | `app/routes/app.competitors.tsx` | the Competitors place in the nav (left rail at 860px and up, bottom tab bar below); `Tab` to Competitors in the nav, `Enter` | The tracked set: every competitor with its one-line reason and the ON/OFF switch (off dims the row and reads "paused <date> · history kept"), then the maybes with Watch and Dismiss, then "Add one we missed" (a website; a handle or a taken-down brand gets a one-line reason instead). No competitors reads "Add a competitor to see where you stand. We also look for new ones every night." Suggestions come from the nightly `competitor-discovery` Workflow (`workers/workflows/discovery.ts`). | **J6**; nav and active place — `tests/unit/app-shell.test.ts`; the signed-in walk by tap and Tab+Enter at 1440 and 390 — `e2e/app-nav.spec.ts` (production lane); writes and reads — `tests/integration/discovery/discovery-write.integration.test.ts` |
| `/app/competitors/:entityId` | `app/routes/app.competitor.tsx`, `app/components/competitor-frame.tsx`, logic in `app/lib/competitor-page.server.ts` | tap a competitor's name on `/app/competitors`; `Tab` to the tracking switch (role `switch`), `Space`; `Tab` to a capture plate (role `button`, named "Open before and after: …"), `Enter` | One competitor of this workspace (another workspace's id, or the own brand, is a 404). Breadcrumb, name, domain and the ON/OFF switch with its consequence ("Off pauses tracking · history kept"); off adds "Paused <date>" under the name and a rule across the feed. **This week**: site changes in the last 7 days, pages watched, and when the site was last read. **The week's biggest move**: the 7-day change with the most words changed, drawn as the mark (the removed sentence struck, the added one on the green marker, from the stored diff hunks) with its capture plate; none reads "Nothing moved on their site in the last 7 days…". **Developments**: every site change of the last 90 days (up to 30), newest first, each with the mark (or "Added: …" / "Removed: …" when only one side changed), the word counts, the day, and the capture plate that opens the before/after pair. Empty, it says when the first change can land. **Sources on this brand**: the website, read every night, and when it was last read. `private, no-store`. | **J7**; the frame, the mark and both empty states — `tests/unit/competitor-page.test.ts`, `tests/unit/site-change.test.ts`; the reads against D1 and R2, workspace isolation and the paused brand's history — `tests/integration/site/site-changes.integration.test.ts`; signed out — `e2e/competitor-page.spec.ts` |
| `/app/changes/:signalId/:side` | `app/routes/app.change-shot.ts`, logic in `app/lib/site-changes.server.ts` (`readChangeShot`) | no link of its own — every capture plate on `/app/competitors/:entityId` and `/app/alerts` loads it as an image; keyboard: — | The before (`:side` = `before`) or after screenshot of one site change, streamed from R2. Only for a signed-in owner of the workspace the change belongs to, and only keys under `snapshot/site/`; anything else is a 404 "That screenshot isn't here." `private, max-age=31536000, immutable`, `nosniff`. Signed out: a redirect to `/login`, never an image. | `tests/integration/site/site-changes.integration.test.ts` — own screenshots served, another workspace's never; `e2e/competitor-page.spec.ts` — signed out |
| `/app/alerts` | `app/routes/app.alerts.tsx` | the Alerts place in the nav (left rail at 860px and up, bottom tab bar below); `Tab` to Alerts in the nav, `Enter` | What changed. Shows own-site incidents first (`alert.kind = 'own_site_broken'`, written by the own-site check below): the title, "We check it again every hour and email you once it's fixed." or "Fixed <day>.", and the day. Then takedown notes (`alert.kind = 'takedown'`, written by the `takedown_fan_out` trigger in `migrations/0009_takedown.sql` when a subject this workspace tracked asks to be removed) and undeliverable briefs (`alert.kind = 'delivery_failed'`, written by the `send-email-dlq` consumer, which also marks the digest `failed` so nothing resends it): a plain sentence that we stopped trying, never the provider's error; the day as "today", "yesterday" or "4 days ago"; and **Read the brief**, which opens the brief drawn by `app/components/brief-view.tsx`. After the incidents come **website changes** of the last 30 days (up to 30) on every brand that is on and on the workspace's own site: "<Brand> changed its homepage" or "Your homepage changed", the mark with its capture plate, the word counts and the day, the same object the competitor page draws (`app/components/site-change-item.tsx`). Off brands produce nothing here. After the website changes comes **news that matters** about a brand that is on (`alert.kind = 'mention'`, written by the nightly `mentions-sweep` Workflow in `workers/mentions/sweep.ts` from GDELT and Hacker News once Jev says the story is about that brand and worth knowing): the headline links out to the article in a new tab, with the publisher under it. With none of these, it says "Nothing has interrupted you. When your own site breaks you'll get an email; everything else waits here." Other alert kinds are not listed yet. | **J7**, **J8**; website changes — `tests/integration/site/site-changes.integration.test.ts`; news alerts — `tests/integration/mentions/sweep.integration.test.ts`; nav and active place — `tests/unit/app-shell.test.ts`; the signed-in walk by tap and Tab+Enter at 1440 and 390 — `e2e/app-nav.spec.ts` (production lane) |
| `/app/settings` | `app/routes/app.settings.tsx`, `app/components/account-settings.tsx`, logic in `app/lib/account-delete.server.ts` | the Settings place in the nav (left rail at 860px and up, bottom tab bar below); `Tab` to Settings in the nav, `Enter`; then `Tab` to **Sign out** (role `button`), or to the **Type <email> to confirm** field and **Delete my account** (role `button`) | Workspace settings. Links to Agents and API. **Sign out** ends the session through better-auth's `signOut` and lands on `/login`. **Delete my account** needs the signed-in email typed exactly ("That doesn't match your email…" otherwise) and a session under a day old ("For your safety, sign out and sign back in…" otherwise, better-auth's `freshAge`). It deletes the user's API keys, then the user through better-auth's `deleteUser`, which removes the sessions and cascades the workspace and every row it owns; then it starts the account-delete Workflow for the stored files, revokes every connected AI app's grant, and lands on `/login`. | **J13**, **J14**; deletion and the stale-session refusal — `tests/integration/account-delete.integration.test.ts`; nav and active place — `tests/unit/app-shell.test.ts`; the signed-in walk by tap and Tab+Enter at 1440 and 390 — `e2e/app-nav.spec.ts` (production lane) |
| `/app/settings/agents` | `app/routes/settings.agents.tsx`, `app/components/agent-settings.tsx`, logic in `app/lib/agent/access.server.ts` | the "Agents and API" link on `/app/settings`; `Tab` to the **Name** field, type, then the **Make a key** button (role `button`), `Enter`; each key and each connected app has its own **Delete** / **Disconnect** button | Shows the MCP address to paste into Claude, ChatGPT or Cursor and links the API reference. **Connected apps** lists every AI app the user said yes to (from the OAuth grants in `OAUTH_KV`), each with **Disconnect**, which revokes the grant and its tokens. **API keys** lists the user's better-auth keys (name, first characters, made, last used) with **Delete**; **Make a key** mints a `0509_…` key and shows it once, never again. `Cache-Control: no-store`. | `e2e/agents.spec.ts` (production lane) — make a key, read over MCP and REST, delete it, the key then gets 401; `tests/integration/agent-oauth.integration.test.ts` — disconnect revokes the token |
| `/oauth/authorize` | `app/routes/oauth.authorize.tsx`, logic in `app/lib/agent/consent.server.ts` | an AI app (Claude, ChatGPT, Cursor) opens it when the user adds the MCP server; signed out, `requireSession` sends the visitor to `/login?next=<this URL>` and sign-in returns here (`safeReturnTo` in `app/lib/agent/paths.ts` accepts only this path). `Tab` to **Allow** or **Cancel** (role `button`), `Enter` | The consent screen: "Let <app> read your Five to Nine?", what it will see (brief and rank, competitors, alerts), that it can't change anything and sees only this workspace, and the address the user returns to, so a look-alike app is visible. **Allow** grants the `read` scope through `@cloudflare/workers-oauth-provider`'s `completeAuthorization` with props `{ userId, clientId }`; **Cancel** returns `error=access_denied` with the app's `state`. A tampered or unregistered return address is shown as an error here and never redirected to. `no-store`, `frame-ancestors 'none'`, `X-Frame-Options: DENY`, `noindex`. | `e2e/agents.spec.ts` — signed out, a 302 to `/login?next=/oauth/authorize…`; `tests/integration/agent-oauth.integration.test.ts` — the ask, the tampered-address refusal, cancel, and allow through token exchange to `/mcp` |
| `/onboarding` | `app/routes/onboarding.tsx` | the first signed-in request, when the workspace has no self entity. `/app` redirects here. A self entity sends this loader back to `/app`; the input is focused | One input, placeholder "your website, or a handle", and the signed-in email. "Add a passkey" runs the same register ceremony as Home. Above it, the mono step bar marks step 1. Submitting the input POSTs to the same route; a subject on the takedown list re-renders with the one line "this brand asked not to be tracked, so we can't set it up"; any other non-empty value redirects to `/onboarding/identity?subject=<value>` untouched (the identity route normalises it), and an empty submit re-renders with the one line "we couldn't find anything for that, try the main website" and the input focused. | **J1** — `e2e/j1-magic-link.spec.ts`; the passkey control is **J2** — `e2e/j2-passkey.spec.ts`; the input is `e2e/onboarding-input.spec.ts` |
| `/onboarding/identity` | `app/routes/onboarding.identity.tsx` | submitting the one input on `/onboarding` redirects here with `?subject=<value>`; a workspace that already has a self entity is sent to `/app`. `Tab` through the name and about fields, then to "That's me", `Enter` | Screen 2, "This is you. Fix anything we got wrong." The mono step bar marks step 2. The site row shows the registrable domain (or the handle) at once; name, logo, about and socials each read "looking on the site" until the homepage read lands (`app/lib/identity/card.server.ts`: the fetch-then-browser transport, the extractor, the name and logo cascades, cached 24 h in `IDENTITY_CACHE`). Name and about are edited in place. A site that cannot be read shows "we couldn't find anything for that, try the main website" above the still-editable card; an input that does not parse shows the one input again with that line. A subject on the takedown list goes back to `/onboarding`. "That's me" saves the card as the workspace's self entity and lands on `/onboarding/competitors`; an empty name re-renders with "add your brand's name, then tap That's me". | `e2e/onboarding-identity.spec.ts` — signed out lands on `/login`; signed in (production lane) the card fills, is edited and confirmed at 1440 and 390; `tests/integration/identity/card.integration.test.ts` |
| `/onboarding/competitors` | `app/routes/onboarding.competitors.tsx` | after the card is confirmed on `/onboarding/identity`, which also starts today's `competitor-discovery` Workflow instance. `Tab` to the button, `Enter` | Screen 3, "Who you're up against", under the mono step bar marking step 3: ON competitors with a one-line reason each ("Named alongside you by 3 publishers"), maybes below with Watch and Dismiss (never Jev's probability, and never a taken-down, dismissed or own brand). While the Workflow runs it reads "We're reading the news for brands named alongside you" and re-reads every 3 s; once it finishes it stops re-reading, and an empty result reads "We didn't find anyone named alongside you yet. Add one you know and we'll keep looking every night." "Add one we missed" takes a website. One action, "Start watching", with no price, which lands on `/app`. | `e2e/onboarding-competitors.spec.ts`: signed out, the URL lands on `/login`; `tests/integration/discovery/discovery-write.integration.test.ts` |

## Agents

The "AI agent native" surface: a remote MCP server and a small read-only REST API over the same three reads (`app/lib/agent/read.server.ts`, shapes in `app/lib/agent/schemas.ts`). Every read takes a workspace id resolved from the caller's own user id at request time (`readWorkspaceIdForOwner`); nothing accepts a workspace id from the caller. `workers/app.ts` wraps the Worker in `@cloudflare/workers-oauth-provider` (`app/lib/agent/oauth.server.ts`), which owns `/oauth/token`, `/oauth/register`, `/.well-known/oauth-authorization-server` and `/.well-known/oauth-protected-resource[/mcp]` and hands every other request to React Router with a `RouterContextProvider` (`app/lib/agent/context.server.ts`). Both surfaces are behind the `AGENT_LIMIT` rate limit (120 a minute per user) and answer `Cache-Control: no-store`.

| Route | File | Reach | Does | Proof |
|---|---|---|---|---|
| `/mcp` | `app/routes/mcp.ts`, `app/lib/agent/mcp.server.ts` | add `https://0509.io/mcp` as a connector in Claude, ChatGPT or Cursor; or send `Authorization: Bearer <0509_ key>`; keyboard: — | Streamable HTTP MCP, stateless (`createMcpHandler` from `@modelcontextprotocol/server`). The OAuth provider checks the bearer first: an OAuth access token it issued, or an API key through `resolveExternalToken` → better-auth `verifyApiKey`. A missing or bad token is a 401 whose `WWW-Authenticate` names the protected-resource metadata, which is how an MCP client finds sign-in. Three read-only tools, each with an output schema: `get_brief`, `list_competitors`, `list_alerts` (which includes the same website changes as `/app/alerts`, kind `site_change`, with the before and after sentences and the page URL). A signed-in user without a workspace gets 403. | `e2e/agents.spec.ts` — the 401 challenge, a made-up token refused, both metadata documents; `tests/integration/agent-access.integration.test.ts` — tools listed as read-only, a call reads only the caller's workspace, 403 without a workspace |
| `/api/v1/brief`, `/api/v1/competitors`, `/api/v1/alerts` | `app/routes/api.v1.*.ts`, `app/lib/agent/serve.server.ts` | `GET` with `Authorization: Bearer <0509_ key>` from `/app/settings/agents`; keyboard: — | The same three reads as JSON. No key or a bad key: 401 with `WWW-Authenticate: Bearer`. | `e2e/agents.spec.ts` — 401 and `no-store` without a key, and (production lane) 200 with one; `tests/integration/agent-access.integration.test.ts` — the key owner's data and never another workspace's |
| `/api/v1/openapi.json` | `app/routes/api.v1.openapi[.]json.ts`, `app/lib/agent/openapi.ts` | `GET /api/v1/openapi.json`, linked from `/app/settings/agents` and `/llms.txt`; keyboard: — | OpenAPI 3.1 for the three endpoints, generated by `zod-openapi` from the same zod schemas the MCP tools publish. Public, cached an hour. | `e2e/agents.spec.ts`; `tests/agent-openapi.test.ts` |

## Not a route

These eleven surfaces have no user route: no URL in the app maps to them. Some are reached by a harness rather than a user — an e2e spec reads the e2e inbox (`e2e/inbox.ts`), an integration test renders the fixture site (`tests/integration/fixture-site.test.ts`) and the send lane (`tests/integration/send-lane.integration.test.ts`), the error page is thrown by a route, and the cron and the toast layer are triggered by the system. Their **Reach** cell says what reaches them. They are listed here so an agent driving the app does not conclude they must already exist somewhere it has not looked.

| Surface | Where | Reach | Does |
|---|---|---|---|
| Toast layer | `app/components/toaster.tsx`, mounted once in `app/root.tsx`'s `Layout` | no route — a save lands; no route calls it yet | sonner, styled from the tokens at radius 0. Only ever "saved" and "undo" (DESIGN.md §11): `toastSaved(message, undo)` is the one paved path, and the `undo` callback belongs to the caller so undo re-runs the same write path that saved — not a second code path. Any other toast call fails `tests/unit/toaster.test.ts`, which greps `app/` and `workers/` for a `sonner` import or a `toast(` call outside `app/components/toaster.tsx`, and `eslint.config.js` bans the import outside that module. The toaster section carries `aria-live="polite"` and sonner's stylesheet disables all toast motion under `prefers-reduced-motion`. |
| Server error page | `app/root.tsx` ErrorBoundary, `app/components/error-page.tsx` | no route of its own — a route throws, replacing that page | Says the product hit a problem and that we have been told. One action, same destinations as the 404 page. The upstream message is not shown. Proof: `tests/error-page.test.ts` throws from a route, renders the root ErrorBoundary, and asserts the 500 status and the page. |
| Dead-man ping | `app/lib/liveness-ping.server.ts`, called from `workers/app.ts` `scheduled` | no route — the `0509` Worker's cron `*/5 * * * *` fires the `scheduled` handler | POSTs to `LIVENESS_PING_URL`. An external service alerts when the reports stop — the one failure a Worker cannot report about itself. Returns `null` when the var is unset, so no monitor is silence, not a scheduled error. |
| e2e inbox | `workers/e2e-inbox.ts` (`workers/e2e-inbox.wrangler.jsonc`, Worker `0509-e2e-inbox`) | no `0509` route — Email Routing delivers `e2e@0509.io` to its `email` handler (per-run `e2e+<tag>@0509.io` rides the same rule via zone subaddressing); reads are `GET https://e2e-inbox.0509.io/message?to=<address>` | Test mail sink for **J1** (0509#3927, 0509#4210): stores the raw MIME in a SQLite Durable Object, one instance per recipient, and drops it after one hour. A read sees a write that already happened. Reads require `Authorization: Bearer $E2E_INBOX_TOKEN`. The endpoint answers 503 when the secret is not set, so a missing secret fails loudly rather than 404ing. A 404 means no message is stored for that recipient. |
| support inbox | `workers/support-inbox.ts` (`workers/support-inbox.wrangler.jsonc`, Worker `0509-support-inbox-v2`); writer `app/lib/data/support_report.server.ts` | no `0509` route — Email Routing delivers the support address to its `email` handler once the rule is moved (0509#4229 child 4); the `scheduled` handler runs daily at `17 3 * * *` | The user-report door (0509#4229, `docs/USER-REPORTS.md`). Stores the raw MIME in D1 `support_report` (id, received_at, from_domain, subject_sha256, raw), forwards the mail to Nish, and opens a GitHub issue titled `user report <id>` labelled `user-report` and `machine-reported` whose body is only the id, the receipt time, the `0509.io` paths found in the mail and the mail user agent. No customer text reaches the issue. Read the raw with `wrangler d1 execute 0509 --remote --command "SELECT raw FROM support_report WHERE id = ?"` and never paste it into an issue or PR. Rows older than 90 days are deleted by the daily cron. With `SUPPORT_INBOX_GITHUB_TOKEN` unset it still stores and forwards and logs one error line. Proof: `tests/integration/support-inbox.test.ts`. |
| fixture site | `workers/fixture-site.ts` (`workers/fixture-site.wrangler.jsonc`, Worker `0509-fixture-site`) | no route on `0509.io` — `GET https://fixture.0509.in/` is the entry point; the break state is flipped with `POST https://fixture.0509.in/__break?mode=off\|hard\|soft`, the price variant with `POST https://fixture.0509.in/__price?variant=base\|raised` | The **J8** breakable own-site fixture (0509#4046): a separate Worker on `fixture.0509.in`, never on `0509.in` (a live 308 to `0509.io`). Serves a pricing-shaped page — one `h1`, a pricing section with real price tokens, a checkout link. The flip route is `POST`-only, token-guarded with constant-time `Bearer $FIXTURE_SITE_TOKEN`, and answers 503 when the secret is unset; the page is `no-store`. `hard` answers 500; `soft` answers 200 with the pricing section gone, which is the case **P7.5**'s incident round-trip exercises. `POST https://fixture.0509.in/__price?variant=base\|raised` (same token) flips the Pro price between ₹1,299 and ₹1,499 for **J7**, with the variant and the last-flip time in `#price[data-variant][data-flipped-at]`, stored under separate KV keys from the break mode. Both state keys have no TTL and survive deploys: **end every round-trip with `?mode=off` and `?variant=base`**. The checkout href is a markup marker, not a resolving target — `app/routes.ts` has no `/checkout` route yet (J13 unbuilt). |
| send lane | `workers/delivery/consumer.ts`, `workers/delivery/send.ts` — a Queue consumer on the `0509` Worker's `send-email` queue (`wrangler.jsonc` queues block; no route) | no route — a producer writes a `digest` row and puts `{ digest_id }` on `send-email`. The weekly brief's producer is the standing rollover below; the incident email's is engine 4, not built yet | **The one send lane** (0509#3979, `docs/engines/delivery.md` § P7.2): every outbound message goes through here, so there is exactly one `EMAIL.send` call site in the repo (`workers/delivery/send.ts`). Per delivery, in this order: read the work item, look up the `send_target`, check `email_suppression` **before rendering**, insert `send_attempt` with a deterministic `idempotency_key` and `status='pending'` — that insert is the claim and a UNIQUE conflict returns without sending — then send, then resolve the attempt to `sent` or `failed` with the error. An unsubscribed address is skipped with no attempt row and no message. A workspace with no enabled email `send_target` acks with `no_target`, no attempt row and `digest.status` left `'pending'`: a missing target is durable, so a retry cannot repair it, and the nightly sweeper of §7 (`docs/engines/delivery.md`) is the only thing that re-enqueues it — the ack is not success. A stranded `send_attempt` left `'pending'` by a Worker that died mid-send is likewise re-enqueued by that sweeper after an hour; the claim row is what makes it detectable. A weekly digest's `payload_json` is a `BriefPayload` (`app/lib/brief-payload.ts`); the consumer renders it with `renderBrief` (`workers/delivery/brief-template.ts`) at send time, so the stored row is data, never HTML. Queue config is `max_batch_size 1`, `max_batch_timeout 30`, `max_retries 5`, `max_concurrency 2`, `dead_letter_queue send-email-dlq`. `status` is never `delivered` — Email Service has no delivery webhooks, so `sent` is the strongest honest claim. Auth's magic link also routes through `send.ts`, so it shares the one call site rather than being a second one. |
| weekly standing | `workers/standing/nightly.ts`, called from `workers/app.ts` `scheduled` on the `0 3 * * *` cron; the rollover is the `StandingRollover` Workflow (`workers/workflows/standing-rollover.ts`, binding `STANDING_ROLLOVER`, wrangler `workflows` block) | no route — the nightly cron, then one Workflow instance per workspace per week, id `rollover-<workspace>-<YYYYMMDDTHHMM>` | Engine 6 (0509#3978, 0509#4004, `docs/engines/standing-home.md`). Nightly, for every workspace: recompute this week's scores with rank and movement left empty, and make sure next brief's rollover instance exists (`createBatch` skips ids that already exist, so a second run creates nothing). The rollover sleeps until the workspace's brief time in its own timezone (`app/lib/brief-schedule.ts`), then in separate retried steps: final scores, freeze rank and movement (ties by score, then last week's rank), write the `digest` row as `pending` with a `BriefPayload`, put `{ digest_id }` on `send-email`, and create next week's instance. It stands down, writing nothing, when the workspace is gone or has moved its brief time, and writes no brief when fewer than two brands are ranked, because there is nothing to compare. A week whose rollover never ran (scores present, no rank, more than an hour past its close) gets a `-catch-up` instance from the next nightly run. Frozen weeks never change: the score upsert only touches rows with no rank. The brief's why-line is the quiet-week counts line until Jev's D4 `read_this_first` is wired. Proof: `tests/integration/standing/rollover.integration.test.ts` runs the real Workflow binding against local D1 — freeze, digest row and payload, successor, the schedule-moved stand-down, no brief with only the own brand on, and the nightly catch-up; `tests/standing/brief-schedule.test.ts` covers the zone and daylight-saving math. |
| nightly site sweep | `workers/workflows/site-sweep.ts` (the `SiteSweep` Workflow, binding `SITE_SWEEP`, exported as `SiteSweepWorkflow` from `workers/app.ts`) and `app/lib/site/sweep.server.ts` | no route — the Workflow's own `schedules` entry in `wrangler.jsonc` creates one instance every night at 02:00 UTC | Engine 4, site change (`docs/engines/site-change.md`). **Plan:** every brand that is on (competitors and the workspace's own brand) and has a registrable website gets its homepage as a `page` row (role `home`) and a `watch` on the `site.web` source (`migrations/0011_site_source.sql`); handles and brands turned off or taken down are never planned. **Check**, one retried step per page: read the page (`app/lib/site/check-page.server.ts`, fetch first, browser when refused), hash its visible text, and write one `snapshot` row. The first read is the baseline. An identical hash stores nothing else. A different hash stores the new text and a 1440×900 screenshot under `snapshot/site/`. **Publish**, a separate retried step: word-diff the two texts, store the hunks as `<snapshot>.diff.json`, and write one `signal` with `kind = 'change'`, `aspect` = the page role, `dedup_key` = the snapshot id (so a retried step files nothing twice), and `payload_json` naming the before and after snapshots, text keys, screenshot keys, diff key and word counts. A page that cannot be read fails alone and logs `site.check_failed`; a step that still throws after its retries is logged as `site.sweep_step_failed` and the sweep moves on. The snapshot id is `<instance>-<page>` and "previous" means the latest snapshot from before this run started, so a retried check files the same change again instead of losing it. Changes are not yet judged by Jev D3, so the standing's noteworthy bucket does not count them; the brief's counts line does. Proof: `tests/integration/site/sweep.integration.test.ts` and `tests/integration/site/check-page.integration.test.ts`. |
| account delete | `workers/workflows/account-delete.ts` (the `AccountDelete` Workflow, binding `ACCOUNT_DELETE`, exported as `AccountDeleteWorkflow` from `workers/app.ts`) | no route — **Delete my account** on `/app/settings` creates one instance with the account's R2 prefixes (`card/<workspace>/` and `snapshot/site/<watch>/` per watch), read before the rows are gone | Lists each prefix a page of up to 1,000 objects at a time and deletes the page, one retried step per page, until the prefix is empty. Returns the number of objects deleted. Other workspaces' objects are never listed. Proof: `tests/integration/account-delete.integration.test.ts` runs the real Workflow binding. |
| own-site check | `workers/workflows/own-site-check.ts` (the `OwnSiteCheck` Workflow, binding `OWN_SITE_CHECK`, exported as `OwnSiteCheckWorkflow` from `workers/app.ts`) and `app/lib/site/own-site.server.ts` | no route — the Workflow's own `schedules` entry creates one instance every hour, on the hour | Engine 4's own-site lane. Gives every own brand with a registrable website a `home` page (independent of the `site.web` source switch), closes silently any open incident whose page is no longer an own homepage, then probes each own homepage once with a plain `fetch` (redirects followed, 10 s timeout, never the browser), one retried step per page. Broken means a 5xx, 404 or 410, or no answer from both the apex and `www.`; a 401, 403, 429 or a Cloudflare challenge (`cf-mitigated: challenge`) is unknown and never opens anything. A broken page is probed again five minutes later in its own step; only a second broken answer, in a separate `open` step, opens an `incident` (kind `error <status>` or `not loading`; the partial unique index allows one open incident per page), pins an `alert` (`kind = 'own_site_broken'`, severity `high`, title `<domain> looks broken: <kind>`) and puts `{ incident_id }` on `send-email`, which sends the incident email. Each later hour is the re-check that email promises: an open incident whose page reads clean is closed and its `{ incident_id }` goes on `send-email` again for the one "looks fixed" email. A step that still fails after its retries is logged and counted, and the run carries on with the next page. Competitor sites never open incidents. Soft breaks (a 200 page missing its pricing) wait for Jev D3s. Proof: `tests/integration/site/own-site.integration.test.ts` runs the real Workflow binding: healthy, broken then fixed, and competitor-never. |

---

## What is deliberately missing

Five of the seven signed-in surfaces are stubs and this table says so rather
than implying coverage. They fill in with the engine packets under #3842. The
rule that keeps this file honest is the same one that keeps the product honest:
a row describes what a user can do **today**, never what is planned.

The four places — Home, Competitors, Alerts, Settings — are linked from
`app/components/nav.tsx`, drawn by `app/components/app-shell.tsx` once, in the
layout route `app/routes/app-layout.tsx` that wraps every `/app` route in
`app/routes.ts`, so a new `/app` page gets the nav without drawing it itself.
`/app/competitors/:entityId` is opened by tapping a competitor's name on
`/app/competitors`. It is not a nav place.
