fix(pricing): restyle `.f9-growth-pricing` / `.f9-commerce-card` onto v4 tokens

Closes #2318

## What
- `.f9-growth-pricing`: navy `#0b1530` band + purple/teal radial and linear gradients + grid/ring pseudo-decoration → single `background: var(--ld-bone)`, ink top rule, ink text. Zero gradients.
- `.f9-commerce-card`: glassmorphism (22px radius, translucent white gradient fill, 72px blur shadows, white type) → bone/card token surface, 2px ink border, square corners (radius declarations removed — the system is radius 0 per the Evidence Desk brief), `7px 7px 0` hard offset ink shadow, ink text.
- Green per WP-A4: reserved for the Recommended state (3px `--ld-green` outline). Plan badge is now ink-filled on bone — no mint pill, no green fill.
- Feature-list bullets: gradient dot+glow → `→` marker in `--ld-green-ink`, matching the ratified `.f9-home` override.
- CTAs and price-sync: white pill / translucent pill → square ink-on-card ink-outline buttons; blur shadows removed.
- Removed the orphan `.f9-commerce-card:nth-child(2) { animation-delay: -2s; }` — its 7s float animation was already deleted on main; **no `@keyframes` solely used by these classes exist in current main** (the issue's float at :696-703 no longer exists), so nothing was shared and nothing was dropped beyond the orphan declaration.
- `docs/design-system-ratchet.json` regenerated with the existing script (`node scripts/design-system-ratchet.mjs --update`), never hand-edited.

## Judge accept, verified
- `css-gradient`, `raw-hex-color`, `non-token-border-radius` hits inside `.f9-growth-pricing` / `.f9-commerce-card` drop to 0 in the regenerated ratchet (ceiling deltas below).
- The 7s float animation is gone (already absent on main as of a0b037ffa; the orphan `animation-delay: -2s` is removed here).

## Ratchet deltas (from `--update`)
```
raw-hex-color 258 -> 246
non-token-border-radius 158 -> 152
css-gradient 22 -> 12
```

## Verification
- `node scripts/design-system-ratchet.mjs` → `Ratchet clean. Remaining legacy markers: 443.`
- `npx vitest run --configLoader runner --project node --changed origin/main` → 6 files / 27 tests passed.
- `sgscan` → `No new security findings.`
- `/pricing` and every route rendering `pricing-section.tsx` uses `f9-home`, whose ratified overrides now sit on the same language as the base blocks (bone/card on ink rule, square, ink-offset shadow, green-outline recommended) instead of overriding a navy band.

run-proof: single guided run — vitest --project node --changed (+ratchet, sgscan) all green in one session; no timers, no detached jobs; systemd session unit completed with the PR as deliverable.
research: not-applicable — no new bin/ or mechanism; two-file restyle.
help-first: not-needed — no new CLI surface.
organ-heartbeat: app/app.css not-an-organ: landing stylesheet, not an orchestrating organ.
loose-ends: pricing visual review on staging was not performed by this unit (no screenshot capability in the worker seat); reviewer should eyeball /pricing once before merge.
reviewer seat: poolside/laguna-s-2.1-free — verdict PASS, zero Act-on.
finding buckets: Consider: CTA min-height:54px dropped, covered by padding+gap (accepted); Noted: 2.5px border-top weight vs 2px siblings (kept, matches issue's “hard rule” language); Dismissed-with-reason: none.
relates-to: none
Test plan: ratchet gate rerun above; vitest node suite affected-tests green.
