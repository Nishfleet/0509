## What changed

- `app/routes/marketing.tsx` — the deploying hero deck line is rewritten to the judge-approved copy: "Then Five to Nine keeps watching the offer behind those ads. When it moves, you get the before-and-after with the page text and source link — plus a screenshot when the capture includes one." No cadence sentence (per the binding judge edit striking "Weekly on Free, every 3 hours on Starter.").
- `tests/customer-claim-audit-table.test.ts` — "before-and-after screenshot" added to the banned unconditional-phrases list, so the test goes red if any audited surface re-promises a screenshot without the capture-includes-one qualifier.
- `docs/customer-claim-audit-table.json` — AUDIT-SAVES-SCREENSHOTS resultDetail notes the homepage hero deck was the last unconditional surface and now carries the qualifier (issue #2308).
- `tests/homepage-hero-search-promise.test.tsx`, `tests/marketing-rebuild.test.ts` — copy-assertion tests updated to the new deck line and to assert the old cadence sentence is gone. These two files are outside `files:` but they assert the exact deck line verbatim and fail without the update.

## Verification

```
$ npx vitest run --configLoader runner --project node tests/customer-claim-audit-table.test.ts tests/homepage-hero-search-promise.test.tsx tests/marketing-rebuild.test.ts
 Test Files  3 passed (3)
      Tests  45 passed (45)

$ npm test
ci-vitest-run: attempt 1/2: vitest run --configLoader runner --project workers
 Test Files  51 passed (51)
      Tests  247 passed (247)

$ sgscan
Scanning changes since origin/HEAD (0c72f2af)…
No new security findings.
```

All rc=0.

run-proof: no new units, timers, path units, or workflows in this diff (machinery count 0); proof of one real run is the suite above — 51 files / 247 tests passed, rc=0.

## Accept checks

- Banned-pattern gate: "before-and-after screenshot" is now in the banned list of `tests/customer-claim-audit-table.test.ts` — a surface reverting to the old phrasing fails the suite (verified: suite is green with the fix, and the phrase is what the old deck line contained).
- Grep of `app/` for unconditional screenshot promises in the audited surfaces: the hero deck now reads "plus a screenshot when the capture includes one". Two residual phrases outside the audited surface list and outside this issue's `files:` scope were found and are filed separately (see loose-ends): `app/routes/competitor-monitoring.tsx:616` ("with the screenshot and the original link") and `app/routes/auth.login.tsx:164` ("each change with its screenshot proof attached"). Not touched here — `must-not` forbids files outside the issue's list.

## Notes

- Work was salvaged from the first claim run's worktree (StartLimitBurst), rebased onto origin/claim/issue-2308, and completed. Diff is net-negative (6+/11−).
- crgate: SKIP — CodeRabbit CLI is not signed in on this machine; the PR-level crgate still applies.
- No new bin/ files, no new machinery — research-before-build and rebuild-verify gates not applicable.
- The truncated issue title was fixed via `gh issue edit` (binding judge edit).

Closes #2308

loose-ends: residual unconditional screenshot copy in app/routes/competitor-monitoring.tsx:616 and app/routes/auth.login.tsx:164 — outside this issue's files: scope, filed as a separate issue.
net-positive-because: n/a — diff is net-negative.
research: n/a — no new bin/ files or machinery.
help-first: n/a — no new tooling.
