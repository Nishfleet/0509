## digest: re-rank the brief around commercial-field changes — the issue's verify gate + the published weighting

Closes #3016

### What this PR ships

The re-rank itself, the counted churn footnote, the no-solo-instant-alert
delivery rule, and the extraction-funnel counters already shipped on main
(#1451 rerank + ratio guard, #1483 instant-alert privilege, #2148/#2151/#2175
churn footnote + variant counts + change brief, #949/#1401/#1565 funnel
counters, #1960 investigation). This PR ships what was still missing from the
issue's own contract:

1. **The verify gate** — `node --test tests/monitoring-brief-ranking.test.ts`
   now passes. The file is dual-runner by design: under bare `node --test` a
   resolve-only hook (`tests/monitoring-brief-ranking.resolve-hook.mjs`) maps
   the `~/lib` tsconfig alias to real files so node's native type stripping
   loads the production modules (rerank, delivery policy, change brief,
   headline-ratio guard); under the repo's vitest node project the same
   scenarios run via vite aliases and additionally pin the delivered churn
   footnotes + evidence deep links from `digest-email.server.ts`.
2. **The published weighting** — `docs/digest-brief-ranking.md` documents the
   why-this-matters weights (offer 1000 > cta 800 > url 700 > headline 600 >
   form 500; `ad_new`/`ad_inactive` weight 0, never headline, never a solo
   instant alert; website_page_* gate semantics; before/after + evidence
   honesty contract). The test ties the doc to the shipped code
   mechanically (types named in descending weight order; churn rules stated).

Read model only: no event-model refactor, no D1 migration, monitoring cron
path untouched (bullet 5).

### Verification

- `node --test tests/monitoring-brief-ranking.test.ts` → `# tests 8 / # pass 8 / # fail 0`, exit 0
- `npx vitest run --configLoader runner --project node --changed origin/main` → `Test Files 1 passed (1) / Tests 9 passed (9)`, exit 0
- `sgscan` → `No new security findings.`, exit 0
- `crgate` → exit 0 (CodeRabbit runs in CI)
- `bash bin/fleet-no-agent-names-check --pr-body <body> --commit-range origin/main..HEAD` → exit 0

run-proof: unit pi-issue-0509-3016 ran the verify command above to green before this PR; no new units/timers/workflows added

net-positive-because: the deliverable is the issue's named verify test (+ its resolve hook) and the published weighting doc — test-and-doc-only by design, no production code paths changed

organ-heartbeat: tests/monitoring-brief-ranking.test.ts not-an-organ: test file, no org surface; docs/digest-brief-ranking.md not-an-organ: documentation

research: no new bin/ files in this diff
help-first: no new bin/ files in this diff

loose-ends: headline-ratio-guard-consumer — measureDigestHeadline/headlineRatioSignal (#1451) still have no production consumer; wiring them into a scheduled surface is #1451's closed scope, not re-opened here

funnel findings: the CTA-extraction funnel counters are shipped and diagnosed (#1960, closed 2026-09-09); the open counter-bug follow-up (#2443, `diff_computed` counts `baseline_established`) stays the tracking issue — no new follow-up filed to avoid a duplicate.

### Reviewer round (product repo)

Reviewer seat: opencode/nemotron-3-ultra-free (first usable entry of the senior seat lookup; senior ladder exhausted/walled at review time, per find_senior_seat's designed fallthrough)

Review-adjudication (one round, reviewer seat opencode/nemotron-3-ultra-free):
- Act on: none.
- Consider: (1) delivered-footnote/deep-link scenario only runs under the vitest arm, not bare node --test — dual-runner by design, accepted as-is; (2) the balanced-gate 1075 constant pin could name the constant, true/false outcomes still pin behaviour; (3) reviewer noted the pending section must be filled before merge — done here.
- Noted: resolve-hook suffix probe TOCTOU (test-only), doc footnote cosmetic wording, docs-match-code positional check, VITEST env detection convention.
- Dismissed-with-reason: "test-only PR" concern — the issue's remaining acceptance scope was the verify gate + published docs; re-rank, churn footnote and alert privilege are already on main (confirmed in code by the reviewer).
- Verdict: not BLOCKING; 8/8 verify re-run live by the reviewer.

### Test plan

- `node --test tests/monitoring-brief-ranking.test.ts`
- `npx vitest run --configLoader runner --project node --changed origin/main`
