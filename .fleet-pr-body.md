## Summary

The deploy conclusion this issue's required clause asks for is lost to a one-bit regression: #2975's review-fix (bbccf182f) shipped `scripts/commit-deploy-ledger.sh` at 100644 while `.github/workflows/deploy-production.yml:673` runs it as `./scripts/commit-deploy-ledger.sh` — exit 126, Permission denied. Two consecutive deploys died on the same step (run 34778207891, step at 20:19:07Z; run 34779307565, step at 20:56:22Z — both "Record the deploy in the on-main ledger"), and the queued 34780512552 is the third victim: its checkout (bd41132f7) is also pre-fix. This restores the bit (mode-only, zero content diff) and pins the convention with a gate in the existing deploy-production-gate suite.

The issue's original fault — Gate C `proof_email_dispatch_invalid` 503 — is already fixed on main by #3366 (a4d650cf4, merged 12:21:50Z; proof_email green on four production verdicts — the acceptance's "fault moved to a named next check" clause, proven by the 18:05Z unit). What this unit owns is the remaining blocker between a landed deploy and conclusion success.

## What ships

- `scripts/commit-deploy-ledger.sh` 100644 → 100755. Fails-before proven locally (`./scripts/commit-deploy-ledger.sh` → Permission denied, exit 126 — byte-identical to both CI failures) and passes-after (the script executes: it reaches the `PINNED_SHA is required` check, which the Actions step supplies).
- One test in `tests/deploy-production-gate.test.ts`: every deploy-job `run: ./scripts/…` step must point at an executable checkout (mode & 0o111), with the failure message naming the exit-126 mechanism. Red before the bit, green after — the next lost bit fails the affected suite, not a production deploy.
- Lane evidence: `.lane/reports/claim-issue-3330.md`.

## Verification

Real runs, this worktree @ 896793d87 (one heavy toolchain at a time; no coverage, no typecheck — CI owns those):

- Fails-before: `./scripts/commit-deploy-ledger.sh` → `/bin/bash: line 1: ./scripts/commit-deploy-ledger.sh: Permission denied`, exit 126 — exactly the step failure of runs 34778207891 and 34779307565.
- Fails-before (gate): `npx vitest run --configLoader runner --project node tests/deploy-production-gate.test.ts -t "0509#3330"` → 1 failed | 54 skipped — the ledger +x assertion, before the chmod.
- Passes-after: the same -t run → 1 passed; the script then executes to the `PINNED_SHA is required` guard (exit 1, not 126 — the env comes from Actions).
- Affected suite: `npx vitest run --configLoader runner --project node --changed origin/main --reporter=dot` → 1 file / 55 tests, all passed (the mode bit affects no module; the only affected test file is the one this diff extends). No tests removed, renamed, skipped or .only'd (+1 case).
- `sgscan` → "No new security findings." (exit 0).
- `crgate` → "CodeRabbit is not signed in on this machine" — the CLI printed usage, no local review ran (headless, no OAuth session; flagged, not explained away; 0509's substantive review is the senior round below, as in #3371/#3383/#3394).
- `fleet-review-arm-check` → exit 0 (a senior seat is usable — the reviewer round runs).
- Issue acceptance, clause by clause: "one Deploy production run on main reaches conclusion success" (02:52Z required) = the 126 removed from the last guaranteed-to-fail step — the first deploy dispatched after this lands is the witness; the queued 34780512552 still fails by design (it checks out pre-fix bd41132f7). proof_email itself: green on main since #3366 (a4d650cf4 — `git merge-base --is-ancestor` of origin/main = true), proven by the 18:05Z unit's four production verdicts (34756884695, 34759458763, 34759554622, 34770115098 — proof_email green in all four; the issue's 503 / `gate-c-proof-email-target-must-resolve-uniquely` signature occurs zero times after 12:21:50Z).

run-proof: 1 vitest red→green pair (-t 0509#3330) + 1 affected-node pass (55/55) + sgscan exit 0 + fleet-review-arm-check exit 0, this worktree @ 896793d87 (+19/-0: mode bit, one gate test, lane evidence); 0 new timers/workflows/units in this diff — prove-one-run-check

organ-heartbeat: none — mode bit + one test + .lane report only — not-an-organ: no checker process added or changed

research: none needed — no new bin/ files. help-first: n/a — no new tool.

loose-ends: run 34780512552 (queued, bd41132f7) still fails the ledger step — the bit reaches production at the first deploy dispatched after this PR lands (fix on a branch is a PR, not production); production_meta #3392, proof_cleanup #3337, post-Gate-C e2e #3373 and Gate B J2 #2513 still stand between a landed deploy and a fully-green one; CodeRabbit local review unavailable on this box (no headless OAuth); the standing packet's step-8 path `lib/seat-lib.sh` is stale — the real module is fleet-ops `lib/litellm-seat.sh` (`find_senior_seat` lives there; the packet's stale path cost one failed `source` this run — fleet-ops#1097 class). test-removal-justified: n/a — zero tests removed, renamed, skipped or .only'd; +1 test case.

Closes #3330

Relates to #2975 (whose review-fix shipped the 100644), #3181/#3366 (the proof_email fixes this issue opened on), #3392/#3337/#3373/#2513 (the named next checks)

Reviewer round (senior seat): pending — recorded below before arm.

net-positive-because: +19/-0 — the fix is a mode bit on a script that already exists, the gate rides the existing deploy-production-gate suite, the evidence rides the existing .lane convention; no new mechanism, no new process, no new organ.

## Reviewer round (senior seat) — adjudication

Reviewer: the stock reviewer subagent on the senior seat (resolved via fleet-ops `lib/litellm-seat.sh` `find_senior_seat` → `litellm<TAB>senior`; this unit's packet tail declares `difficulty: senior-review`, so the extension's parent-seat inheritance IS the resolved senior seat — proven, not assumed). Verdict: "land it. Nothing blocks #3330."

- Warning (should-fix): the +x gate walked only the `deploy` job — 3 of the 5 unique `./scripts/` refs (ci-bind-remote-restore-candidate.sh :223, ci-prepare-remote-restore-evidence.sh :240, ci-verify-provider-main-cas.sh :321) were never looked at; a lost bit there would pass every suite and still kill production with the same 126. → **Act on**: DONE — the gate now walks every job (`Object.values(deploy.jobs)`) and takes the run's first token (also the Consider-1 fix: a future multiline/argful step now fails with the gate's message, not an ENOENT). Independently verified before widening: exactly 5 unique `./scripts/*.sh` refs in the whole workflow, 100755 except the ledger script this diff fixes; 55/55 on the affected file after the widening. Committed and pushed to `claim/issue-3330-acton` — #3408 itself was already merge-queue-locked when the fix landed (GH006: "Branches that are queued for merging cannot be updated"), so per the reviewer's own ruling ("a one-line follow-up, not a reason to hold the PR") #3408 lands as-is and the widening PRs from `claim/issue-3330-acton`.
- Consider (the run-string first token): → folded into the Act-on edit above.
- Consider (the body draft vs the live PR body): → Noted — this body IS the reviewed draft, adjudication appended.

run-proof, updated: the affected file 55/55 on BOTH the shipped head (896793d87) and the widened acton head — sgscan/crgate/receipt checks unchanged.

Branch CI receipt (this head, run 34782935069): codex-node-checks-shard-4 = fail — 1 failed | 2405 passed; the failure is `tests/no-time-bomb-fixtures.test.ts:78`: 56 absolute timestamp literal(s) in `tests/status.route.test.ts` (first offender `:108: generatedAt: "2026-09-13T16:00:00.000Z"`). PRE-EXISTING vs this base (bd41132f7): both files are untouched by this diff (deploy-production-gate.test.ts + the mode bit + the .lane record are the whole diff) — the fixture damage came in with today's #3197/#3198 material. This is the 02:52Z accept-clause-2 ("no codex-node-checks-shard-4 failure") still standing on main, now with its owner named; it does not weaken any gate or assertion this PR ships.
