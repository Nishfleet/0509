deploy: post_deploy_recovery_failed — the rollback safety net crashed after doing its job; 3rd occurrence 2026-09-13 (runs 34626446693, 34759554622, 34770115098). Resumed this unit's own salvage 8fcd1b252 (judge: resume, not restart), rebased onto current main 65776e216 (clean, 3 commits replayed), finished the acceptance: the 17:36Z failure replayed green + 9 readiness-gate tests moved to the recovered-release contract + lane evidence. This resume also wires the recovered live version into the on-main ledger row (mechanic 4's missing half, below).

The 17:36Z signature (run 34770115098, "Deploy" step, 17:36:42.657Z): the wrangler rollback printed SUCCESS and `Current Version ID: 515fb9d2…`, yet the spawn exited non-zero; the old script's `if (result.status !== 0) throw` fired AFTER the swap landed, the plan wrapper rethrew AggregateError `post_deploy_recovery_failed` (deploy-production-plan.mjs:809, cause = rollback-production.mjs:41:32), and the Deploy Worker job went red on a recovered release. The success-only chain (Worker secrets sync, release-evidence verify, on-main deploy ledger record) never ran, so which Worker version went live was recorded nowhere.

What the fix does — the success path exits 0, four mechanics, no new machinery:

1. Swap-confirmed-despite-non-zero-exit: the rollback spawn's stdout is piped and echoed; `interpretWorkerRollbackSpawn` (worker-rollback-target.mjs) treats wrangler's own final `Current Version ID: <chosen target>` marker as proof the swap landed — `rolled_back_although_spawn_exited_nonzero`, outcome ok.
2. Nothing-to-recover: when the resolved target IS the version the failed deploy already placed at 100% of traffic, the spawn is skipped, the live version recorded, exit 0.
3. The plan continues past a recovered release (stderr: `post_deploy_recovery_complete — <step> failed, last-green restored: <msg>`) so the later release checks and the workflow's success chain run; the AggregateError survives when the recovery itself fails.
4. Which version went live is now recorded — both halves: the recovered evidence (`recoveredLiveVersionId` + recovery outcome) is written back to the rollback-target evidence, and the on-main deploy ledger prefers the recovered live version over the deployed-then-rolled-back id. This resume completed the ledger half: `commit-deploy-ledger.sh` now passes `--rollback-evidence test-results/worker-rollback-target-*.json` (same nullglob pattern as the wrangler output; no file → no flag). Without it the ledger row would name the ROLLED-BACK FAILING version — and that row's version_id is exactly what `readLastGreenLedgerVersionId` anchors the NEXT rollback on, so the safety net would aim at the failing release. The guard is tested: an evidence file without a recovery outcome keeps the wrangler-output id.

Tests — the 17:36Z failure replayed end-to-end (red before, green after):

- ADD tests/rollback-recovery-exit-zero.test.ts: a fake WRANGLER_BIN prints the SUCCESS banner and `Current Version ID: 515fb9d2…`, then EXITS 1 — the exact 17:36Z signature; the script must exit 0 and record the live version. Also: the skip-recovery path records the live version; the ledger prefers the recovered version; the AggregateError survives when recovery itself fails; the recovered-canary continuation runs past the canary instead of rethrowing. Red-before re-proven live this session at origin/main 65776e216: the same file runs 8 failed / 2 passed there (`expected 1 to be +0` — the old exit-1-on-recovered-release, the plan rethrow, and the ledger recording the rolled-back id); green on this branch.
- MOD tests/deploy-production-gate.test.ts: 9 tests moved to the recovered-release contract — `caught === undefined`, the next release check RUNS (that is the point: the ledger record lives downstream), and the post-canary invariant runs inside the recovery AND again when the loop resumes past the canary. Same scoped suite before the contract update: 1 failed file / 9 failed tests, all one shape (`expected undefined to be Error: <step>_failed` — the old rethrow).

Verification:

```
$ npx vitest run --configLoader runner --project node --reporter=dot tests/rollback-recovery-exit-zero.test.ts tests/worker-rollback-target-existing.test.ts tests/verify-post-deploy-release.test.ts tests/deploy-production-gate.test.ts tests/deploy-age-detector.test.ts tests/gate-c-soak.test.ts tests/production-candidate-workflow.test.ts tests/workspace-member-preflight.test.ts
 Test Files  8 passed (8)
 Tests  147 passed (147)
```

`--changed origin/main` (affected-tests mode) also red-flags `tests/d1-remote-restore-evidence.test.ts` (2 failures) — pre-existing at main, the exact failure of CI run 34787898963 (2026-09-13T22:49Z, Test step, migration 0102 vs the test's stale ledger list), tracked as #3314; that test imports none of this diff's files.

sgscan --base origin/main: no new security findings (exit 0).

run-proof: unit pi-issue-0509-3390 (systemd --user), this session — resumed from its own salvage, rebased onto 65776e216, 147/147 above; the 17:36Z-signature replay test is the issue's first acceptance. The second acceptance (the next real Deploy production run completes green, or exits 0 when there is nothing to recover) lands post-merge: the 19:58Z/20:21Z blockers the salvage cited are gone (the ledger exec bit is fixed at main by 896793d87, issue #3330; the no-time-bomb gate passes in run 34787898963), but the LATEST main run 34787898963 still dies at the Test step on the #3314 d1-remote-restore set-equality red — that, not this fix, is what a post-merge run will trip on until #3314 lands.

net-positive-because: 3rd-occurrence production incident (every deploy since 00:25Z 2026-09-13), fixed forward in the same deploy scripts it protects; the +302-line test is the failure signature replayed, so the silent-failure class of the safety net is closed and the ledger finally answers which version went live (#3190's question).

organ-heartbeat: scripts/rollback-production.mjs, scripts/commit-deploy-ledger.sh not-an-organ: product deploy tooling — no timer/exporter/guard/canary in this diff; no config/fleet-organs.json or fleet_rules.yml rows touched.

loose-ends: next-Deploy-production-run-green-or-exit-0 — #3390's second acceptance, post-merge; the pre-merge blockers the salvage cited are resolved at main (ledger exec bit fixed by 896793d87/#3330; the #3412 no-time-bomb gate now passes), so the only remaining pre-merge red is #3314 (d1-remote-restore Test step, live in run 34787898963) — until it lands, the next run can still go red on THAT step, not on this fix.

Closes #3390
