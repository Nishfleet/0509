## Summary

- Delete the dormant Presence Desk claim from the llms.txt intro (`buildLlmsText`): the sentence "Presence Desk tracks your brand and competitors across declared sources with proof-backed briefs" promised proof-backed briefs across declared sources while `wrangler.jsonc` sets PRESENCE_DIGEST/X/REDDIT/LINKEDIN to "disabled".
- Replace the unconditional screenshot promise in the sneaker-resale llms.txt page description, full sentence to full sentence: "English default in the sneaker-resale locale cluster: watch Meta ads and landing-page changes with saved screenshots." → "English default in the sneaker-resale locale cluster: watch Meta ads and landing-page changes with source-linked proof."
- Wire the llms.txt claim strings into the same claim-surface registry test as the pricing bullets: add an `AUDIT-LLMS-TXT-CLAIMS` registry row and tests that evaluate `LLMS_TEXT` from `buildLlmsText` (never hand-copied) and assert every advertised claim resolves to a verified row, plus fail-closed detectors that the disabled-source "proof-backed briefs" sentence and the unconditional "saved screenshots" promise never regress.

Files touched (all named in the issue `files:` / `do:` step):
- `app/lib/public-markdown.ts` — delete dormant sentence; reword sneaker line.
- `docs/customer-claim-surface-registry.json` — add llms.txt claim row.
- `tests/customer-claim-surface-registry.test.ts` — add llms.txt claim-registry coverage + fail-closed detection.

The qualified Presence Desk truth stays in llms.txt: "Presence Desk: website/open-web is the active GA source; social and marketplace sources are gated, planned, or manual-only until provider approval."

## Verification

- `npm test` (node project, the project this change ships in) → green. node project: 662 files / 7887 tests passed on this worktree after merging `origin/main`.
- Targeted `tests/customer-claim-surface-registry.test.ts` → 11 tests passed, including the new "resolves every llms.txt claim string to a verified registry row", "does not advertise disabled Presence Desk sources" (now also pins the sneaker-resale screenshot promise), and the live-string extraction.
- `grep -n "Presence Desk tracks your brand and competitors across declared sources with proof-backed briefs" app/lib/public-markdown.ts` → no match (sentence deleted).
- `grep -n "landing-page changes with saved screenshots" app/lib/public-markdown.ts` → no match (unconditional promise gone).

net-positive-because: the net-positive lines are the claim-registry wiring the issue `do:` step requires — one `AUDIT-LLMS-TXT-CLAIMS` JSON row (the llms.txt claim strings) and the registry tests that evaluate `LLMS_TEXT` live and fail closed on the deleted sentences; the product-copy change in `public-markdown.ts` itself is net-negative (one sentence deleted, one phrase swapped).

## Review round

Reviewer seat: `cursor/cursor-grok-4.6-high` (senior seat via `find_senior_seat`).
- Act on: none.
- Consider — sneaker-resale wording was not fail-closed. **Acted**: added `expect(LLMS_TEXT).not.toContain("landing-page changes with saved screenshots")`.
- Consider — branch behind `origin/main`. **Acted**: merged `origin/main` (clean ort merge) before arm.
- Consider — intro parse is position-based. Noted; matches the pricing-bullet extraction convention and the `advertisedCopy` set-equality pins any drift.
- Noted: scope holds (only the 3 named files); qualified website/open-web truth retained; full judge sentence matched verbatim on both sides.
- Dismissed-with-reason: `text.includes(claim)` looseness (set-equality is the real pin); `PUBLIC_MARKDOWN` other Presence Desk mentions (different qualified copy, out of scope); `verify` grep not run by vitest (matches existing row convention; the test is the gate).

Verify: `run-proof` below.

## run-proof

- units: `pi-issue-0509-2311` (this worker unit).
- command: `npx vitest run --configLoader runner --project node` — 662 files / 7887 tests green in one run; targeted file 11/11.
- No new `bin/` files, no organ files, no migrations touched, no systemd units/timers/workflows added.

## Test plan

- [x] `npm test` (node project) passes.
- [x] `tests/customer-claim-surface-registry.test.ts` covers `public-markdown.ts` llms.txt strings.
- [x] No disabled-source "proof-backed briefs" claim remains in `LLMS_TEXT`.
- [x] Sneaker-resale description no longer promises unconditional "saved screenshots".

Closes #2311