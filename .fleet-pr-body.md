## PR

The 0509-daily-market-signal seat was moved to a dedicated agent-state worktree (fleet-ops#4576, orig issue Nishfleet/0509#2018), but the Hermes market-signal contract still told the agent to run from the shared checkout `/home/nish/workspaces/products/0509`.

This PR updates the contract's workdir reference in `automation/HERMES_MARKET_SIGNAL.md` to the dedicated worktree path `/home/nish/workspaces/agent-state/0509-daily-market-signal-worktree` so a human re-running the report (or the next workdir normalization) is not misled.

Docs-only, no behavioural change — the seat already runs from the dedicated worktree.

Closes #2041

## Verification

Docs-only change; no unit/timer/workflow added in this diff, so `prove-one-run-check` reports `SKIP`. Stale reference removed and new path confirmed present with real `grep`/`diff` runs:

```
$ grep -n "products/0509" automation/HERMES_MARKET_SIGNAL.md
(no match - stale shared-checkout ref removed)

$ git diff
- Run on `netcup-rs2000` from `/home/nish/workspaces/products/0509`.
+ Run on `netcup-rs2000` from the dedicated worktree `/home/nish/workspaces/agent-state/0509-daily-market-signal-worktree`.
```

`run-proof: SKIP (docs-only diff)`

## Review

Reviewer seat: `cursor/cursor-grok-4.6-high` (find_senior_seat #3121 ladder).

- **Critical**: none
- **Warnings**: none
- **Act on**: none
- **Consider**: `scripts/check-market-signal-workdir.sh` header comment still names `/home/nish/workspaces/products/0509` — out of scope for this contract-only issue; filed as Nishfleet/0509#2078.
- **Noted**: `tests/market-signal-workflow.test.ts` pins snapshot/sink wording, not the workdir path, so no assertion breaks; a future workdir-normalization pass may pin the dedicated path (#2078).
- **Dismissed**: none