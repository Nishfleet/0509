## Summary

Closes #3357 — the Offer Timeline (BET 3) provision — by doing exactly what its acceptance says: extend the ONE existing nightly rail, add no mechanism. The sitemap lists 261 published /ads/:domain pages (issue snapshot: 127) while production still serves 9 /timeline/:domain pages: the #1958 sitemap-timeline rail rode the 04:00 cron in deterministic sitemap first-seen order, so the 15-minute Cloudflare scheduled wall killed its tail, the same cohort HEAD got re-captured every night, and starved domains never accrued a dated `landing_page_snapshot`. The #0081 seed (one-shot by design, wiped by #2908) was the only history — snapshots accrue on wall-clock, so every starved day is history that cannot be bought retroactively (§1.7).

The fix is the #1549 publisher's own budget pattern, applied to the #1958 rail. Neither gains a new mechanism, unit, timer, table or flag:

- **Stalest-first, ledger-as-cursor**: the rail's OWN `timeline-<domain>-<day>` row ids are the resume state (the #1549 persisted-cursor, in ledger form — acceptance 4, no D1 migration). One bounded read (`landing_page_snapshot` WHERE `id LIKE 'timeline-%'`, 7-day lookback, 5000-row cap, `loadRecentSitemapTimelineCaptureDays`) orders the cohort least-recently-captured first; ties keep sitemap first-seen order (stable sort — the #1549 lesson: deterministic head-of-queue service is what starved the tail). Within a few nights every published domain holds a proof-complete row.
- **Deadline + honest truncation**: `SITEMAP_TIMELINE_BACKFILL_DEADLINE_MS` (8 min, beside the publisher's own 10) stops STARTING new captures and reports `truncated: true`, so a wall-clock kill provably leaves its tail as the next night's head; the +3 in `workers/app.ts` logs that signal on the rail's existing completion line.
- **Degrade, never zero**: a failed ledger read degrades to cohort order (the no-DB/no-cohort precedent), so a hiccup can never blank the night. #2873 capture validity untouched — every row still requires a real `requireScreenshot` capture and the read-side complete-proof gate still suppresses phantom states (acceptance 2). #1309/#2021 410/noindex shell guards untouched — zero route edits (acceptance 3). No `migrations/**`, nothing expand/contract, nothing follows (acceptance 4). `tests/file-size-ratchet.test.ts` seeds the now-980-line rescued suite (required: over the 800-line ceiling, the list is the allowance) and the suite's clocks are now #3215 fixed-date pinned constants.

Diff: `app/lib/sitemap-timeline-backfill.server.ts` (+214), `tests/sitemap-timeline-backfill.server.test.ts` (+282/−31), `tests/file-size-ratchet.test.ts` (+1), `workers/app.ts` (+3), the `.lane` evidence record, this body. One provisioning path (acceptance 1): the rail the issue's own Prior art names — extend, do not add a third pathway.

net-positive-because: every added line either implements one clause of the issue (the ordering, the deadline, the honest cut signal, the tests that pin them) or documents why the existing machinery now reaches the published surface — no mechanism, no wrapper, no abstraction; machinery count +0 organs, +0 units, +0 timers, +0 migrations, +0 flags; this lane JOINS a shipped rail.

## Verification (final head, this pickup)

- Resumed the fourth banked salvage head, not zero: the packet's stamp `wip/pi-issue-0509-3357-20260913T200724Z` @ `2d3255b9d` (== origin/claim/issue-3357) — the dying run's fix 043fbb8d1 PLUS its salvage-banked, never-run finishing edits (the ratchet seeding, the #3215 fixed-date guard). This lane verified the banked state against the issue's acceptance, then shipped it.
- `git merge origin/main` (cff30e908) — clean, zero conflicts: this diff's `workers/app.ts` hunk (~L837) is disjoint from the #3391/#3201 hunks (L83/L466).
- Rescued suite + ratchet, one invocation: `npx vitest run --configLoader runner --project node tests/sitemap-timeline-backfill.server.test.ts tests/file-size-ratchet.test.ts` → **44/44** (`it(` counts exactly equal the tests that ran, nothing skipped; `expect(` 122 vs 81 at our base, **+41**; the skip/only/`skipIf`/`test.fails` grep over the final diff: zero matches; `--diff-filter=D`: no deleted test files — acceptance 5, no test-removal-justified needed).
- Affected: `npx vitest run --configLoader runner --project node --changed origin/main` → **9 files / 97 tests, exit 0**, first round, nothing to fix (1 of 5 inner-loop rounds spent).
- The issue's verify: `node scripts/check-ads-timeline-links.mjs` → "OK: all 261 indexable /ads pages link their /timeline; 9 qualifying /timeline pages linked from /brands.", **exit 0** — the #1931 sweep stays green at this tranche.
- sgscan → "No new security findings.", exit 0.
- Termination, honest: production sitemap `timelines=9` (< 60) and `git merge-base --is-ancestor HEAD origin/main` is false — this branch is a PR, not production, so no production claim. The 9→60 climb is wall-clock: it needs the next deploy plus nightly accrual (the issue's own rollback/observe framing; #3095's coverage guard watches it daily).
- One suite at a time, `VITEST_MAX_WORKERS=2` respected, `--maxWorkers` never passed, no coverage, no typecheck locally (CI owns both; the PR's affected-tests round-trip is the typecheck). Receipts: `.lane/reports/claim-issue-3357.md` (the dying run's statements + this pickup's corrections, re-verified).

run-proof: prove-one-run-check — this PR adds no unit, timer or workflow; its proof of work is the proven end-to-end affected run (9/97, exit 0, final head), the rescued suite + ratchet (44/44), the #1931 sweep receipt (261/9, exit 0) and sgscan ("No new security findings.", exit 0).

research: the mechanism was verified against the repo's SHIPPED proofs, not memory: the #1958 rail's own cohort derivation, #1549's truncated+deadline+persisted-cursor contract (the publisher's `options.deadlineAt ?? Date.now()+ADS_DOMAIN_PUBLISHER_DEADLINE_MS` — this lane's `options.deadlineAt` mirrors it exactly), #0081's own "live capture, not fabricated transitions" comment, the #2873 requireScreenshot/complete-proof validity pair, #3095's coverage-guard observe-to-close, and the live 0509.io measurements this hour (261/9, vs the issue's 127/9 — the starvation grew while the issue waited).

help-first: no new `bin/` file in this PR; nothing hand-built that already exists — the deadline/truncated/cursor mechanics are the #1549 publisher's shipped pattern, the ledger read reuses the rail's own id space, and the ordering is a pure sort over the existing cohort; no new table, flag, unit, timer or mechanism.

organ-heartbeat: none — this PR touches no organ (workers/app.ts here is the existing 04:00 rail's completion-log line, not a scheduled unit, timer or heartbeat file; the fleet-ops judge/measure cadence is untouched).

loose-ends: the 9→60 sitemap climb is wall-clock — it needs the next green deploy plus nightly accrual, witnessed by #3095's ops/timeline-coverage-guard and the issue's own termination curl; the #1309/#2021 410/noindex guards stay untouched by construction (zero route edits — the #2021 noindex shell on unpublished domains is the designed state, not debris); crgate printed "CodeRabbit is not signed in on this machine" (the same headless-OAuth gap the #3383/#3317 records name — the substantive review is this PR's senior round, adjudicated below); CI owns typecheck + coverage.

## Review adjudication (senior round — one round, before the arm)

- Reviewer seat: `litellm/senior` — the #3121 existing-lookup verdict: `find_senior_seat` → `litellm\tsenior`, exit 0; `bin/fleet-review-arm-check` → exit 0. The round runs on the repo's TRACKED project reviewer def (`.pi/agents/reviewer.md`, `model: litellm/senior`, added by #3317) with project scope — never this worker's own seat.

(Adjudication lands here before the arm.)
