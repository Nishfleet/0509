## Summary

Relates to #3357. Draft recovery of closed, unmerged PR #3395. Not ready to merge.

Extends the existing nightly sitemap timeline capture loop with an eight-minute deadline, a truncated result, and oldest-capture-first ordering using existing snapshot IDs. No new scheduler, migration, watch service, gate edits, or route changes. Removed the saved patch's redundant file-size allowance after its test suite was split below the existing limit. All baseline tests remain.

## Verification:

Runs on 2026-09-17 against base bcf4e8bcb701a261703a6ef720f7bf611c4d523e:

- Focused capture, staleness, and file-size suites: 44 tests passed.
- Final `npx vitest run --configLoader runner --project node --changed origin/main`: 10 files, 97 tests passed, exit 0. Two workers, no coverage or typecheck.
- `node scripts/check-ads-timeline-links.mjs`: exit 0, 288 indexable ads pages and 30 qualifying timelines. Separate sitemap read also counted 30, below the issue's 60 target.
- `sgscan`: no new security findings, exit 0.
- `crgate --agent`: failed with exit 3, local review service not signed in. No review result claimed.
- Routes, migrations and gate paths have no diff against the base. Snapshot capture still requests screenshots; content-key deduplication and proof guards are unchanged.

run-proof: existing capture functions executed in the focused and affected test runs above; the live link sweep passed on 2026-09-17 at 288/30. No units, timers or workflows added. Production capture with this patch has NOT run. This is an undeployed draft, not evidence that the 60-timeline goal is met.

## Review and remaining work

The review availability check exited 0, but its seat lookup returned no selection. Its first listed usable review backend failed before any tool ran because credentials were unavailable. Exactly one independent review was attempted; none completed. Auto-merge stays OFF.

review: skipped, no capable seat
blocked-on: orchestrator

Review must verify starvation handling when early domains repeatedly fail or content-key deduplication prevents a new dated row. The saved tests establish successful-capture ordering but do not prove these resume cases. The seven-day read and deadline accounting also need independent review before this draft is ready. Do not interpret old reports from PR #3395 as approval.

Review adjudication: no findings were returned because the reviewer failed to start. Act on: obtain the required review before marking ready. Consider: none. Noted: production remains below the issue target. Dismissed: none.

research: existing nightly capture loop, publisher deadline/truncated pattern, snapshot write and deduplication paths, and issue #3357's prior art. No new executable mechanism.
help-first: no new bin file or command interface. Existing check usage read before running.
organ-heartbeat: workers/app.ts not-an-organ: only adds a completion-log field to an existing scheduled handler.
loose-ends: issue-3357-review-and-live-goal; draft pending independent review, failure/dedup resume verification and later authorized deployment. Snapshot history is not deleted on rollback.
