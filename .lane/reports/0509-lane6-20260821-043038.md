# Slow resource requests on home (dogfood a08b8427701d) — re-verified already resolved

**Status: already resolved on `origin/main`; this lane re-verifies the live state and records evidence.**

Branch: `0509-lane6-20260821-043038`
Base: `origin/main` at `422fbd55` (#806)

## Item

- [ ] [dogfood a08b8427701d] Slow resource requests on home
  [dogfood 20260808T074205Z-msk2fl3n]

## Verdict

No code change was warranted. The item is already resolved on `origin/main`:

- **PR #603** (`67c11c1c`, merge of `1ced110d`, merged 2026-08-11) — defers the
  Site Rep support widget install (which makes `/api/public/install` +
  `/api/public/config` third-party calls that intermittently exceed 1s) to 5s
  after hydration, per the finding's own "move non-critical requests later"
  guidance.
- **PR #542** (SSR pricing, `3b3c7bba`) — publishes real Dodo prices in SSR
  HTML and defers the client `/api/pricing-preview` fetch until the pricing
  section nears the viewport, with a 10s fallback.

Both commits are ancestors of current `origin/main`:

```
$ git merge-base --is-ancestor 1ced110d origin/main && echo ancestor-of-main
ancestor-of-main
$ git merge-base --is-ancestor 3b3c7bba origin/main && echo ancestor-of-main
ancestor-of-main
```

A prior lane already filed evidence for this same item
(`.lane/reports/0509-lane1-slow-resource-requests-already-resolved.md`, PR
#736, merged 2026-08-15). This report re-verifies against today's `main` and
confirms the fix still holds.

## What the finding was

The SEO Fix Kit dogfood run `20260808T074205Z-msk2fl3n` flagged "Slow resource
requests on home" (`performance-2`, notice): `pricing-preview (2626ms)`, plus
the Site Rep widget's `install (1262ms); config (1262ms)` requests.

## Evidence on current main

- `app/lib/siterep-widget.ts:20` — `SITE_REP_WIDGET_DELAY_MS = 5000`, with a
  PERF comment citing dogfood `a08b8427701d` and the run evidence.
- `app/root.tsx:171-176` — `installSiteRepWidget` schedules the delayed
  install via `setTimeout(install, delayMs)`; `app/root.tsx:194-195` passes
  `{ delayMs: SITE_REP_WIDGET_DELAY_MS }` for the home widget.
- `app/routes/marketing.tsx:562-601` — `/api/pricing-preview` is fetched only
  when the `#pricing` section nears the viewport (`IntersectionObserver`),
  with a 10s fallback timer; it is not fetched on mount.
- Widget scope unchanged: `shouldLoadSiteRepWidget` still limits the widget
  to the documented public paths and it stays disabled for authenticated
  sessions and isolated paths.

## Regression pins (all passing, run in this worktree)

`tests/siterep-widget.test.ts` — "deferred install (dogfood a08b8427701d:
slow resource requests on home)" describe block:

- "still installs synchronously by default and with an explicit zero delay" —
  immediate path preserved for other callers.
- "installs the documented widget script exactly SITE_REP_WIDGET_DELAY_MS
  after the call" — fake timers prove the script is appended exactly at the
  delay boundary with the documented script config.
- "cleanup cancels a pending deferred install so nothing is appended later" —
  no late third-party requests after teardown.

```
$ npx vitest run --configLoader runner tests/siterep-widget.test.ts
 Test Files  1 passed (1)
      Tests  11 passed (11)
```

## Ledger state (external, not in this repo)

`/home/nish/workspaces/agent-state/traction-dogfood/finding-ledger.json`
fingerprint `f15979cfb89e9ce58c73` (patternKey `a08b8427701d`) is
`active: false`, resolved at `2026-08-09T01:30:17.237Z` by dogfood run
`20260809T013017Z-msl4lamt` — the finding no longer appears in later runs.

## Files

- `.lane/reports/0509-lane6-20260821-043038.md` — this evidence record (the
  only file touched by this lane).

## Rollback

N/A — evidence-only lane record; no product code, data, or billing change.
