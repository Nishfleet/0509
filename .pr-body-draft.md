## Summary

Issue #2347. Adds one telemetry log line per Meta Ad Library fallback strategy invocation in `app/lib/meta-library-browser.server.ts`, each tagged with the strategy name, so prod hits per strategy can be counted ahead of the two-week deletion follow-up ticket.

Added a `meta_library_strategy_invoked` line at the entry of each of the three strategy functions:

- `strategy: "session_scrape"` — `searchMetaLibraryViaSessions` (:367)
- `strategy: "quick_actions"` — `searchMetaLibraryByQuickActions` (:1068)
- `strategy: "browserless_bql"` — `searchMetaLibraryByBrowserless` (:1383)

Each line is `console.info(JSON.stringify({ event, strategy, ts }))`, matching the file's existing structured-log convention (`console.warn(JSON.stringify({ event, ... }))`). The three tags are distinct and distinguishable in logs.

The deletion half of the original `do:` is intentionally out of scope here — the binding judge edit replaces it with a separate dated follow-up ticket, which owns the "~500 lines" accept.

## Verification

Affected node test run via vitest affected-changed mode (`--project node --changed origin/main`):

- Test Files 253 passed (253)
- Tests 3354 passed (3354)
- Duration ~77s

strategy tags present (grep): `quick_actions`, `session_scrape`, `browserless_bql`

run-proof: npx vitest run --configLoader runner --project node --changed origin/main (253 files / 3354 tests passed); `git diff origin/main...HEAD` touches only app/lib/meta-library-browser.server.ts

net-positive-because: telemetry instrumentation adds 21 lines with no removal; the two-week deletion follow-up owns the dead-strategy ~500-line shrink.

Closes #2347
