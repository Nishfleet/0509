## growth: Send the abandoned-onboarding nudge email (issue #2114)

Ships the one-shot abandoned-onboarding nudge: a user who signed up 24-48h ago and has not added a single watchlist receives exactly one plain-text-first email, recorded in `delivery_attempt`, with a `List-Unsubscribe` header. Rides the existing 04:00 UTC daily cron rail (no new cron, so release-soak coverage is unchanged).

### Files
- `app/lib/onboarding-nudge.server.ts` (new) — selection query, send path, daily sweep.
- `app/lib/delivery-account-emails.server.ts` — export `normalizeDeliveryEmail`, `resolveActivationEmailTarget`, `ActivationEmailTarget` for reuse by the nudge module.
- `workers/app.ts` — wire `runOnboardingNudgeSweep` into the daily rail.
- `tests/onboarding-nudge.server.test.ts` (new) — selection, idempotency, unsubscribe header, send-window gating.

### Do (from issue)
1. Query users created 24-48h ago with zero watchlists and no prior nudge `delivery_attempt`. ✓
2. Send one plain-text-first email via the mandated `delivery.server.ts` path: idempotency key `onboarding-nudge:<userId>`, `delivery_attempt` row, `List-Unsubscribe` headers. ✓
3. Wire into the existing daily cron task; add tests for selection, idempotency, and the unsubscribe header. ✓

### must-not
- No new email vendor, no template system, no follow-up sequence (exactly one nudge). ✓
- Welcome-email code untouched. ✓

### Copy (the deliverable, per Nish's binding additions)
**Subject:** `Nike changed its ad this week` (29 chars, under 45, no exclamation)

**Body (plain text):**
```
Hi {name},

Add a competitor and Five to Nine watches its Meta Ad Library ads and emails you the moment they change. We caught one this week: on 10 Sept 2026, Nike switched its site call-to-action from Shop Now to Contact Us and dropped its $149 price. Right now your competitors are making moves just like this. You just can't see them yet.

Add your first competitor: {searchUrl}

Nish
Five to Nine
```

- One paragraph, one real dated example (Nike, 10 Sept 2026, verified at `/timeline/nike.com`), brand named.
- One link straight to `/search` with the signup source prefilled when known.
- Sign-off from Nish by name. No em dashes, no exclamation marks, no filler.

### Kimi K3 Max judge verdict (binding addition #6)
Prompt: "you are the buyer; would you click; rewrite the weakest sentence". Model: `cursor/kimi-k3-max`.

- **Would you click?** Yes. "The Nike example is specific enough that I can picture the same alert landing for a real rival, and the ask is one small step."
- **Weakest sentence:** "That is the kind of alert you would get for your own competitors." — "It makes me do the work — I have to imagine the value myself instead of feeling it. No urgency, no stakes, and it spends the momentum the Nike example just built."
- **Rewrite applied:** "Right now your competitors are making moves just like this. You just can't see them yet." (adapted from the judge's "— you just can't see them yet" to drop the em dash per the unslop rule.)

### Send window (binding addition #7)
09:00-11:00 in the user's workspace delivery timezone, defaulting to IST (`Asia/Kolkata`) when unknown or invalid. Exactly one email per user, ever (idempotency key `onboarding-nudge:<userId>` + `delivery_attempt` row). The 04:00 UTC daily run lands at 09:30 IST for the default, inside the window.

### Verification
- `npm run typecheck` — exit 0.
- `npx vitest run tests/onboarding-nudge.server.test.ts` — 9/9 passed.
- Full node suite — 658 files / 7849 tests passed.
- Full workers suite — 51 files / 247 tests passed.
- Delivery suite — 11 files / 168 tests passed.

run-proof: typecheck exit 0; onboarding-nudge tests 9/9; node suite 7849/7849; workers suite 247/247; delivery suite 168/168.

### Reviewer round (product repo)
Reviewer seat: `cursor/cursor-grok-4.6-high`. Findings adjudicated:

- **Critical — send-window gating drops non-UTC+5..+7 users.** The 04:00 UTC daily run + strict 09:00-11:00 local window means only UTC+5..+7 users are emailed. This is a direct consequence of the two given constraints (Nish's binding addition #7 send window + the fixed 04:00 UTC daily rail). Blast radius is <=15 users, product is India-focused (IST default lands inside the window). **Noted** — documented limitation, not a code bug; a broader-timezone design is a follow-up decision.
- **Warning — failed send permanently loses the user.** The selection query excludes any user with a prior `onboarding_nudge` attempt row regardless of status, matching the issue's literal spec ("no prior nudge delivery_attempt"). A failed first send means the user is not re-selected. **Noted** — matches the issue's spec; blast radius is tiny.
- **Warning — PROOF claim not corroborated by seed data.** **Dismissed-with-reason** — verified against live `/timeline/nike.com`: 10 Sept 2026 shows CTA Shop Now → Contact Us and price $149 → removed, exactly as the copy states. The reviewer checked seed data, not live data.
- **Warning — no index on selection query.** **Noted** — user table is small (15 users); a scale index is a follow-up.
- **Warning — LIMIT 200 silently caps.** **Noted** — deliberate safety cap; blast radius is <=15.
- **Suggestion — no send-window test.** **Acted on** — added 3 tests for `isWithinSendWindow` (IST default, outside-window skip, invalid-zone fallback).
- **Suggestion — `/search?source=` inert.** **Noted** — matches the letter of Nish's instruction.
- **Suggestion — N+1 timezone resolution.** **Noted** — bounded (<=200 per sweep).

### Known limitations (documented, not blocking)
- The 04:00 UTC daily run only reaches UTC+5..+7 users under the strict 09:00-11:00 local window. The current user base is IST, so this is not a live gap. A multi-hour sweep is a follow-up if Nish wants global coverage.
- A failed first send is not retried (matches the issue's "no prior nudge delivery_attempt" spec).

Closes #2114
