import type { SourceSnapshotRecord, SourceChange } from "~/lib/sources/types";

/**
 * Hiring (job boards) source section — STUB (seam #2218). Renders nothing
 * until #2199 replaces this file with the real section.
 */
export function HiringSection({
  snapshot: _snapshot,
  diff: _diff,
}: {
  snapshot: SourceSnapshotRecord | null;
  diff: SourceChange[];
}) {
  return null;
}
