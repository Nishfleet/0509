import type { SourceSnapshotRecord, SourceChange } from "~/lib/sources/types";

/**
 * TikTok Ads (Commercial Content Library) source section — STUB (seam
 * #2218). Renders nothing until #2194 replaces this file with the real
 * section.
 */
export function TiktokAdsSection({
  snapshot: _snapshot,
  diff: _diff,
}: {
  snapshot: SourceSnapshotRecord | null;
  diff: SourceChange[];
}) {
  return null;
}
