import type { SourceSnapshotRecord, SourceChange } from "~/lib/sources/types";

/**
 * LinkedIn Ads (Ad Library) source section — STUB (seam #2218). Renders
 * nothing until #2193 replaces this file with the real section.
 */
export function LinkedinAdsSection({
  snapshot: _snapshot,
  diff: _diff,
}: {
  snapshot: SourceSnapshotRecord | null;
  diff: SourceChange[];
}) {
  return null;
}
