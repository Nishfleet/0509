import type { SourceSnapshotRecord, SourceChange } from "~/lib/sources/types";

/**
 * Google Ads (Transparency Center) source section — STUB (seam #2218).
 * Renders nothing until #2189 replaces this file with the real section.
 */
export function GoogleAdsSection({
  snapshot: _snapshot,
  diff: _diff,
}: {
  snapshot: SourceSnapshotRecord | null;
  diff: SourceChange[];
}) {
  return null;
}
