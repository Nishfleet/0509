export function Hero() {
  return (
    <section className="mt-14" id="hero">
      <h1 className="font-display text-[clamp(1.75rem,3.6vw,2.9rem)] leading-[1.15] font-semibold tracking-[-0.02em]">
        Know where you stand. And who's gaining on you.
      </h1>
    </section>
  );
}
