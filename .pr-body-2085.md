## seo: canonicalize /compare alias routes to their sitemap winners

Fixes #2085.

The three non-sitemap `/compare` URL variants (`/compare/visualping-ad-library`,
`/compare/visualping`, `/compare/foreplay`) rendered HTTP 200 with a `<title>`
identical to their sitemap canonical, so two indexable URLs carried the same
title on the acquisition compare surface. They now 301-redirect to the sitemap
canonical (the `/ads` alias canonical-redirect pattern), so only the canonical
serves indexable 200 content.

### What changed

- `app/routes/compare.visualping-ad-library.tsx` — 301 → `/compare/visualping-ad-libraries`
- `app/routes/compare.visualping.tsx` — 301 → `/compare/visualping-ad-libraries`
- `app/routes/compare.foreplay.tsx` — 301 → `/compare/foreplay-spyder`
- `tests/compare-route-canonical.test.ts` — new regression test (prevention mechanism, fleet-ops#366)

The sitemap canonicals (`/compare/visualping-ad-libraries`,
`/compare/foreplay-spyder`) keep serving indexable 200 content. The sitemap is
unchanged. No migrations, no gate-owned paths, no weakening of the live compare
pages/copy — route canonicalization + test only.

### Verification

```
$ npx vitest run --configLoader runner --project node tests/compare-route-canonical.test.ts
 Test Files  1 passed (1)
      Tests  6 passed (6)
```

Full node suite (651 files / 7734 tests) passes:

```
$ npx vitest run --configLoader runner --project node
 Test Files  651 passed (651)
      Tests  7734 passed (7734)
```

sgscan: `No new security findings.`

run-proof: `npx vitest run --configLoader runner --project node tests/compare-route-canonical.test.ts` exits 0 (6/6 tests pass); full node suite 651 files / 7734 tests green; sgscan clean.

Closes #2085

### Reviewer round

Reviewer seat: `cursor/cursor-grok-4.6-high` (senior seat, fleet-ops#3121).

- **Act on:** none.
- **Consider:** stale comment in pre-existing `tests/seo/compare-canonical.test.ts` (not in this diff) still describes the EN losers as "live HTTP-200 pages"; the test itself passes (renders components directly) — comment drift only, follow-up wording fix, not blocking.
- **Consider:** the three new loaders are identical one-liners matching the established `/ads` per-route inline pattern; no abstraction warranted for three routes.
- **Noted:** `runLoader`/`loaderResponse` helpers are clear and correct.
- **Dismissed:** none.

No blocking findings; no `blocked-by-judge` label applied.
