visitor: closing receipts for #3247 — the #3260 -> #3282 fix chain verified live in production; this PR carries the promised lane evidence.

## What this PR is

#3282 (merged 2026-09-12T13:46Z) fixed the issue, but its body carried no closing linkage, so #3247 stayed open through 15 more claim/release cycles (14:03Z -> 04:56Z). The unit that produced the 23:37Z termination line promised a "PR to follow" — this is it. Delta: ONE file, `.lane/reports/claim-issue-3247.md` (lane-evidence report, the tracked `.lane/reports/claim-issue-*.md` convention, #3327 precedent). No code, no tests, no gates touched.

## Verification

- Fresh judge probe (2026-09-13T05:19Z, this box, `bash /home/nish/workspaces/agent-state/fleet-landing-watch/measure.sh`):

  ```
  visitor: https_redirect=301 home_ttfb_ms=80 home_edge=HIT search_ttfb_ms=1163 manifest=200 dup_routes=0 public_repo_leaks=0
  deploy: last_success_age_h=83 merges_since=427 last_failure=failure@34736169021 live_edge_cache=HIT
  ```

  The issue's required clause, satisfied: `home_edge=HIT`, and 80ms is inside 3x of the 319ms prior-HIT bar (and under the 09:26Z note's ~1500ms).
- Anonymous production GET (2026-09-13T~05:22Z): `cf-cache-status: HIT, age: 140`, `cache-control: public, max-age=14400` (accepted zone-rewrite shape 1), `x-0509-edge-cache: HIT`, `x-0509-edge-stored-at: 1789276765` = 2026-09-13T05:19:25Z, ~155s old at the GET — a fresh edge-store served from both layers. Production answers with the `ab810164b` stamp (a header only that commit introduces, 0509#3282's receipt), so production runs this issue's fix even while the deploy-workflow success-age metric reads 83h — that metric's lane is #3264/#3272/#3261, not this issue.
- Fix ancestry: `git merge-base --is-ancestor ab810164b origin/main` -> exit 0. The #3247 fix commits (#3260 `7f76a312d`; #3282 `94729f293` = `ab810164b` + `ed41bb773` + `3d15bd77c`) are on main; this PR adds none of them.
- `sgscan --base origin/main` -> "No new security findings." (exit 0).
- `crgate` -> "CodeRabbit is not signed in on this machine." (exit 3 — the same notice #3327 recorded, there with exit 0; noted, not blocking: the reviewer round below is the substantive review, and the PR's own checks run the workflow gates).
- `npx vitest run --configLoader runner --project node --changed origin/main --passWithNoTests` -> "No test files found, exiting with code 0" — the docs-only delta affects no test; the node suite runs in PR CI (CI owns coverage and typecheck).

run-proof: unit pi-issue-0509-3247 (systemd --user) — this run; receipts: judge `visitor:` line + production header GET above, full transcript in `.lane/reports/claim-issue-3247.md` (final-run section, 2026-09-13); sgscan 0 new findings; vitest changed-mode exit 0.

organ-heartbeat: .lane/reports/claim-issue-3247.md not-an-organ: lane-evidence markdown, no heartbeat owner

net-positive-because: +1 markdown file, zero mechanisms — the report records the root-cause trail (#3260's unreachable window -> the ab810164b stored-lifetime fix -> #3327's zone persistence) so the next `home_edge` regression starts from receipts, not archaeology.

loose-ends: #3247-closeout — none: the 05:19Z judge line above is post-#3327 (merged 2026-09-13T00:31Z) and already shows HIT/80ms; the deploy-chain watch stays with #3264/#3272/#3261.

Closes #3247
