## Summary

Cloudflare enforced D1 free-tier daily limits on 2026-09-01 (5M rows read /
100K rows written per day); 0509 is on Workers Free. This lands the budget
trip-wire from issue #1731 using the orchestrator `decision-resolved` picks
(2026-09-06). It is a re-land of the reviewed-and-closed #3007 onto current
main: main moved ~500 commits since that PR's base, so the three reviewed
commits were rebased (one real conflict — tsconfig.node.json, resolved by
keeping both include entries) and one recovery commit covers the canary that
landed on main since (declaration) plus the conflict.

- `scripts/ci-d1-budget-check.sh` + `.lib.mjs`: applies `migrations/*.sql` to a
  scratch `node:sqlite` database (98 migrations today), runs `EXPLAIN QUERY
  PLAN` over the curated hot-path list in `scripts/d1-budget-queries.json`,
  prices plans against per-table estimates in `scripts/d1-budget-estimates.json`
  (bullet 1, option A — static config, refreshed by hand, dated), and fails
  non-zero past 10% of the daily limit: trip = 500K reads / 10K writes.
- `.github/workflows/ci.yml`: new required `d1-budget-check` job (bullet 2,
  option B) on the same no-skip contract as `codex-node-checks` (step-1
  authorizer, pinned-SHA checkout, no job-level `if:`/`needs`,
  permissions: contents: read), plus a step in `codex-node-checks` ahead of the
  workers-project (real-D1) vitest suite.
- `.github/workflows/deploy-production.yml`: `D1 query budget check` step in
  the deploy job before dependency install (bullet 4, option B). No
  preview/staging deploy exists, so the production gate is where the budget
  blocks a release.
- Bullet 3 (static declarations): every `scripts/*canary*.mjs` entrypoint
  (excluding `*.lib.mjs`) and every `.github/workflows/*canary*.yml` carries
  `d1-budget: reads=<n> writes=<n> runs_per_day=<n>`; the check fails on a
  missing declaration or a per-run overage. Includes the canary main gained
  since #3007: `scripts/canary-timeline-coverage.mjs` (reads=10 — the sitemap
  render it probes is cache-only, one SELECT, app/lib/sitemap.server.ts:421 —
  writes=0, runs_per_day=1, unscheduled today).
- `docs/d1-budget-escalation-runbook.md` (bullet 5, runbook only): alert email
  -> GraphQL Analytics -> throttle the offending cron -> wait-vs-upgrade (money
  decision) -> incident log + estimates refresh. No new timer.

Verification: `bash scripts/ci-d1-budget-check.sh` -> PASS, exit 0
(totals reads/day=329110 vs trip 500000; writes/day=8250 vs trip 10000;
98 migrations applied to the scratch schema). Same check via the lib's CLI
mode, `node scripts/ci-d1-budget-check.lib.mjs` -> PASS, exit 0.
`npx vitest run --configLoader runner --project node tests/d1-budget-check.test.ts
tests/required-context-no-skip.test.ts` -> 2 files / 18 tests passed.
`npx vitest run --configLoader runner --project node --changed origin/main`
-> 25 files / 432 tests passed. `sgscan --base origin/main` -> no new
security findings. The diff touches no `migrations/**` files; the trip-wire's
test applies every real migration to a scratch schema and asserts the read
and write paths against the real 98-migration schema, not a mocked binding.

run-proof: the new ci.yml required context `d1-budget-check` runs the script on
every PR and merge_group; the in-`codex-node-checks` step runs it before the
workers-project D1 vitest suite; the deploy-production step runs it on the
pinned candidate before `npm run deploy`. Local script run: PASS, exit 0. The
reviewer independently re-ran the check as CI does (read-only, in-memory) and
got the same PASS. No new systemd units or timers; the required-context
regression test now enforces the new job's no-skip shape.

research: D1 free-tier limits and the 2026-09-01 enforcement date verified
against official docs
https://developers.cloudflare.com/changelog/post/2026-09-01-d1-free-tier-limit-enforcement/
and https://developers.cloudflare.com/d1/platform/pricing/; the estimator uses
node's built-in `node:sqlite` (repo pins Node 22.22), so the check adds no
dependency, credential, or network.

help-first: `sgscan --help`, the fleet gate checks' `--help`
(fleet-no-agent-names-check, prove-one-run-check, fleet-exec-review-canary,
fleet-organ-heartbeat-check, fleet-token-efficiency-check), and the check
itself (`scripts/ci-d1-budget-check.sh`; the lib's CLI mode was executed
directly) were run; the script header documents the estimator model. No new
`bin/` tool.

organ-heartbeat: .github/workflows/ci.yml + .github/workflows/deploy-production.yml
+ .github/workflows/ads-prog-seo-canary.yml + .github/workflows/meta-discovery-canary.yml
— not-an-organ: existing 0509 product workflows, not fleet-ops organs; the diff
only adds a job/step/declaration comment to them, no new timers or schedules.

Reviewer round (seat reviewer-senior = nebius/zai-org/GLM-5.3-Flash, resolved
via the stock seat lookup `find_senior_seat` after `bin/fleet-review-arm-check`
exit 0; senior ladder reported exhausted/walled, the lookup's documented
capable-seat fallback; one round, verdict: nothing must change to merge):
- Act on: none.
- Consider: (1) writes headroom is 17.5% (8250/10000) and 82% of writes are
  rate-limit bookkeeping — the gate trips by design at traffic growth; the
  upgrade conversation is money, i.e. Nish's, and the runbook covers it.
  (2) invalid `tripFraction`/`dailyLimits`/rows-per-search config values
  silently fall back to defaults instead of hard-erroring (the one fail-open
  spot). (3) the canary declaration scan takes the FIRST `d1-budget:` match
  per file, so a stale commented-out declaration would shadow the real one.
- Noted: the partial-migration abort path has no dedicated test; the
  declaration net keys off "canary" in the filename (acceptance scoped it so);
  cron-vs-declaration drift could be asserted in the existing test.
- Dismissed-with-reason: none — no finding failed to land in a bucket.

net-positive-because: the issue explicitly requires a new detector that does
not exist — there is no existing D1 budget check to extend; the bulk is the
canary declarations, two config files, the estimator, the runbook, and the
test.

loose-ends: d1-budget-check-1731 — (1) `d1-budget-check` must also be added as
a required context in branch protection by a repo admin (GitHub settings, not a
PR; the worker token's protection-API read 403s, integration scopes);
(2) `scripts/d1-budget-estimates.json` stays order-of-magnitude until the first
manual remote COUNT refresh.

Gate-path notice: this diff touches protected verifier paths (`ci.yml`,
`deploy-production.yml`) under `required-verifier-integrity.yml`. The attest
is the orchestrator's (nish3451, admin scope); this PR does not self-attest —
an `attest-requested:` comment names the head sha.

Closes #1731
