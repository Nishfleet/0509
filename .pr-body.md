## What

Deletes the `sendCustomerAtRiskAlert` automation (a pre-revenue at-risk customer emailer for ~15 users with zero signups since June) and its cron wiring. Keeps `sendWeeklyBusinessNumbers` (the founder heartbeat) untouched.

`workers/app.ts` now schedules one fewer task. The task name is also dropped from the release-soak observability contract (`release-scheduled-observation-contract.mjs`, `release-scheduled-observation-contract.ts`, `api.release-soak.ts`, `gate-c-soak.lib.mjs`) and the `classifyScheduledTaskResult` branch, so the soak gate no longer expects an observation that will never fire.

## What changed

- `app/lib/operator-metrics-emails.server.ts`: removed `sendCustomerAtRiskAlert` + its now-unused `getOperatorRiskSummary` import.
- `workers/app.ts`: removed the at-risk alert cron-wiring block (and its `sendCustomerAtRiskAlert` / `resolveOperationalRiskAlertIdempotencyKey` imports).
- `workers/schedule.ts`: removed `includeRiskAlert` from `ScheduledTask` and the `resolveOperationalRiskAlertIdempotencyKey` helper.
- Observability contract: removed `customer_at_risk_alert` task name and its classify branch so release-soak stays honest.
- Tests: deleted tests covering only the deleted function/helper, updated soak/schedule expectations, removed the mock wiring.

## Acceptance

- [x] `workers/app.ts` schedules one fewer task.
- [x] `sendWeeklyBusinessNumbers` schedule and its test are untouched.
- [x] `rg sendCustomerAtRiskAlert` returns zero hits.

## Verification

- `npx vitest run --configLoader runner --project node --changed origin/main` → 10 files / 101 tests passed.
- `npx vitest run --configLoader runner --project node` (full suite) → 664 files / 7913 tests passed.
- `sgscan` → no new security findings.
- `crgate` → skipped (CodeRabbit not signed in on this machine; no human fallback). 

run-proof: `node` vitest project (full + affected), `sgscan`; no `migrations/**` or `tests/integration/**` changed so the `workers` (workerd) project was not run.

## Reviewer round

Reviewer seat: `opencode / nemotron-3-ultra-free` (senior ladder cursor exhausted/walled → fallback capable seat via `find_senior_seat`; `fleet-review-arm-check` exit 0).

Findings (one review-adjudication bucket each; no Act-on required):
- **Consider**: `getOperatorRiskSummary` (workspace-ops.server.ts:151, re-exported from data.server.ts / workspace.server.ts) is now orphaned in production — its only remaining consumer is tests/data.server.test.ts:3780. Keeping it is defensible and within scope; track a cleanup separately. **Noted, out of scope for this delete.**
- **Noted**: removing this automation silences operator eyeballs on scheduled-monitoring degradation (inlineFailures, skippedForBudget, dispatchFailures, stale/trouble watchlists, delivery failures, stuck runs) — the deliberate point of the issue; no other alert currently covers it. Future replacement tracked separately.
- **Dismissed-with-reason**: `migrations/0070` still permits `customer_at_risk_alert` + `signals` — historical DDL, existing rows may legitimately hold them; tightening needs a destructive follow-up migration. `rg` acceptance covers the code token, which is clean.

loose-ends: none.

Closes #2343