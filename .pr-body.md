Closes #2486

## Summary

`detectMoneyPathRegression` sliced `pathRecords` to `minFailures` and checked `.every(isMoneyPathFailure)`, but never verified the slice was actually `minFailures` long. A path with exactly one red record (first run after provisioning, or a newly added `/ads` cohort URL whose first sample is a 500/301) filed a "returned non-200 2x within 10 minutes" issue backed by a single sample. Fix: `if (failures.length < minFailures) continue;` between the slice and the `.every` check — the exact fix from the finding.

## RED → GREEN

- RED (before fix): `npx vitest run tests/search-latency-regression-guard.test.ts` — 1 failed / 26 passed. The new test `does not fire when a path has fewer than minFailures records` failed with `expect(...).toBeNull()` receiving an incident (failures of length 1).
- GREEN (after fix): same suite 27/27 passed; `tests/search-latency-probe.test.ts` also 14/14. Combined run: 41/41.
- Re-ran after rebasing onto current main (e51a7011): 41/41.

## Same-pattern search (step 3)

- `scripts/search-latency-regression-guard.mjs` `detectRegression` — already guarded (`if (runs.length < 3) return null;`).
- `scripts/search-latency-regression-guard.mjs` `detectAuthRegression` — already guarded (`if (runs.length < consecutiveRuns) return null;`).
- `scripts/search-latency-probe.mjs` rate-limit streak detector — already guarded (`if (records.length < consecutiveRuns) return null;` at line 800).
- The money-path detector was the only unguarded instance.

## Verification

- `npx vitest run tests/search-latency-regression-guard.test.ts tests/search-latency-probe.test.ts` → 2 files, 41 tests passed (GREEN).
- `npx vitest run --configLoader runner --project node --changed origin/main` → 1 file, 27 tests passed.
- Typecheck: skipped in-worker per the fleet memory-budget rule (fleet-ops#4891) — CI owns typecheck; the PR CI round-trip covers it.

run-proof: one local run per command above (RED run, GREEN run, affected-mode node run); no workflows/timers.

research: not applicable — no new `bin/` files, existing detector pattern followed.
help-first: not applicable — no new CLI surface.
organ-heartbeat: scripts/search-latency-regression-guard.mjs not-an-organ: standalone guard script, not a heartbeat organ.

loose-ends: none — single-finding defect fix, scope limited to the files: line.

## Review round (review-adjudication)

Reviewer seat: cursor/cursor-grok-4.6-high (senior seat, per find_senior_seat). One round.
- Consider: add a one-word comment explaining the length guard — cosmetic; recorded here, not re-delegated (the adjacent comment block above the loop already explains the pure-flap semantics).
- Consider: add an assert that 3 red records still fire — already covered by the adjacent "3-long red run" test; Noted, no change.
- Act on: none. No blocking findings.
