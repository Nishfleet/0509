## What

Dedupe the website_page importance values (added/removed 80, changed 82) into one shared constant.

- Add `WEBSITE_PAGE_EVENT_IMPORTANCE` to `app/lib/digest-rerank.ts` as the single source of truth.
- Source the evaluator path (`watch-event-evaluator.server.ts` `BASE_IMPORTANCE_BY_EVENT`) from it, so the two can never drift.

The direct emission path (`competitor-site-monitor.server.ts`) already scores website_page events through the change-criticality scorer (issue #1387), so the only hardcoded copy was the evaluator's `BASE_IMPORTANCE_BY_EVENT`; it now reads the shared constant.

## Verification

- `npm run typecheck` exits 0.
- `npx vitest run --project node tests/watch-event-evaluator.test.ts tests/digest-rerank.test.ts tests/digest-ranking.test.tsx tests/competitor-site-monitor.server.test.ts` — 4 files, 92 tests passed.
- `npx vitest run --project workers tests/integration/website-page-change-emission.integration.test.ts` — 1 file, 1 test passed.
- The website_page importance values (80/80/82) now appear exactly once in `app/lib` (in the shared constant).

run-proof: typecheck exit 0; 92 unit tests + 1 integration test green; grep confirms 80/80/82 appear once in app/lib

## Notes

The full `npm test` suite has 4 pre-existing failures on main unrelated to this change (customer-claim-surface-registry, funnel-measurement, verify-claims, and a flaky search empty-state timeout) — confirmed failing on a clean main worktree.

net-positive-because: adds the single shared WEBSITE_PAGE_EVENT_IMPORTANCE constant (16 lines) that replaces the evaluator's hardcoded 80/80/82, so the values appear exactly once in app/lib and the two paths can never drift.

Relates to #2099
